# 更衣柜手机　独立评审　polish-20260823

reviewer_role: 独立评审人（本局未画主 CSS / 主 SVG / 主界面）
producer_role: 制作人机器人（合同所载；与评审人不是同一角色）
contract_status: CANDIDATE（制作人交卷，评审人未改合同文件）
review_status: PASS

genre / genre_profile / distribution_form: html-game-puzzle / html-game-puzzle / OTHER（机壳内多页，不是假官网群）
pipeline_id: VP1　pipeline_override_reason: 合同已写（机内对照，不升搜词顶栏）。本局采信。
space_dimension: 2D　true3d_exception.enabled: false

## 开评范围

只读 `游戏库/更衣柜手机` 实现与 `_视觉美化/` 交卷。只写本文件。不改实现、不补截图、不改合同。

证据根：`visual/html-game-puzzle/polish-20260823-{zh|en}/`  
制作人运行记录：`http://127.0.0.1:8816/` 中、`8817/` 英（截图矩阵 URL）。  
本局独立抽查：`http://127.0.0.1:8916/` 中、`8917/` 英，Chrome headless CDP，非 `file://`。未把抽查帧写入 `visual/`。

## 交卷闸

- 合同必填栏有值；`contract_status=CANDIDATE`；制作人未自填 `review_status=PASS`。
- 许可表非空；通用 12 均标 PRESENT 且磁盘有 PNG；专属 ≥8 有文件或 N/A+理由。
- `五套并排单帧` / `33-five-skins` 合同现标 **PRESENT**。本轮复核中英 `33-five-skins.png`、截图清单与制作人返工记录。
- 体积：中文版 52 文件 140226 B（136.9 KB），英文版 54 文件 156632 B（153.0 KB），证件 SVG 1385 / 1061 B。VP1 媒体锚 8MB。帧率与首包秒表未测，已在制作人 `did_not_prove`。
- 无远程 CSS / `@import` / 远程字。无新 AI 图。

## 逻辑哈希（相对 `_视觉美化工作/基线清单.sha256`）

抽 20 份玩法标本文件，SHA256 全 MATCH，diff=0：

中英 `js/phone.js`、`lock.html`、`backroom.html`、四结局、`album.html`、`photo-zheng.html`、`photo-fu.html`。

与交卷「未改 js / PIN / 四链 href / 存档前缀」一致。玩法回归未发现，不因此 HOLD。

## 8916 / 8917 抽查（本局实测）

| 项 | 中 8916 | 英 8917 |
|---|---|---|
| PIN 3716 | 点 3-7-1-6-开 → `home.html` | 3-7-1-6-OK → `home.html` |
| 错 PIN 0000 | 仍 `lock.html`，句「密码错误，再试」 | （中文步已证可再试） |
| ending-old | 后仓三月链 → `end-old.html`；p1「拍摄停在二十三年三月十四、备注写着入职健康证」 | p1 hire-health 2023-03-14 |
| ending-new | 后仓八月链 → `end-new.html`；p1「拍摄停在八月十二」「系统备注是空的」 | p1 2026-08-12 blank note |
| 两证 | 图库缩略 `id-hire.svg` / `id-later.svg`；详情甲 `.idface`、乙 `.idface.later` | 中英 hire SVG 字节相同 |
| 人脸 | SVG 无 eye 词；甲 5 个 circle 为衣料起球点；乙 0 circle；头块无五官 | 同套几何 |
| flags | `gys_seen0314` 等六键 | `gys_en_seen0314` 等六键，前缀不混用 |
| 旧档 | 六键=1 后 `#how` 报两张白底+备忘/通话/日历/隔板 | 英文 how 同构 |
| 320 | PIN 12 键皆 66×44；图库甲乙 132×126；引言链 44px 但 y=904（视口 h=720，须滚） | 制作人 en-31 同结构 |
| 390 | PIN 66×44，无 <44 | 制作人 08/29/30 有框 |
| 键盘 | 锁屏 `.pad button` 可 focus（12 枚）；引言 `a.enter-link` focus-visible 为 2px 点描边、高 44 | 07 中英 PNG 有点框 |
| 引言搜框 | `input/[role=search]` 数量 0 | 合同 N/A 搜词页 |

## 截图矩阵

路径格式 `visual/{genre_profile}/{build_id}/{nn}-{slug}.png` 成立。中英各 01–32 after + `-before`，另有 `playtest-end-old/new`、`playtest-old-save`。构建闸 URL 为 http。

通用 12：磁盘齐。看过运行帧：01 值班条+拿起；02/16 图库两证轮廓可分；03/playtest-end-old 交三月；04/playtest-end-new 交八月伦理近答案；05 后仓可再拿起；06 320 图库可点；07 开局链点框；08 390 PIN 盘；10 已接圆/未接方；11 空 PIN；12 错 PIN 句；19 禁止页左红条非模态。

复用（MD5 相同，不另算新证据）：01=13；02=09=16；03=32；11=22。09 与 02 同帧，未单独证明 `prefers-reduced-motion` 已开。31-narrow-320-intro 未把开局链收入 720 视口（链在 fold 下），但 06/28 仍能证 320 主操作。

类型追加：搜词三张已 N/A+理由。隐藏页异皮=20。静帧不烧日期=16。两源并排无独立 13 号，但 16 图库甲乙同格可指认。

第 3 节解密 8 张专属：引言无搜框、内嵌信息异皮、禁止页、备忘卷宗、后仓异皮已 PRESENT。**「五套并排可指认」** 本轮补中英 `33-five-skins.png`，合同改 PRESENT。见下节复核。

before/after：14 甲 before 为方块头肩，after 为旧黄纸+绿领+压耳长发、无五官。乙 after 为冷白纸+褐 V 领+短刘海露耳。主循环对象不是灰矩形。

## 烟测 7 步（开评后）

1. 320 主按钮可点：06/28 已拍；8916 实测 PIN/图库 ≥44。PASS  
2. 只键盘到第一个主线点：07 点框；8916 引言链与 PIN 键可 focus。未拍 Tab 走完四结局。记 `did_not_prove`，本步门槛已过。  
3. 只触控同一段：08/29/30 为 390 模拟，非真机手指。`did_not_prove` 真机。  
4. 关声：无音频，N/A。  
5. 减弱动效：CSS 有 `prefers-reduced-motion`；无循环动画。09 与 02 同像素，8916 抽查时 media 为 false。主反馈（证件轮廓）不依赖动画，本步不升 HOLD。  
6. 状态不唯色：甲乙轮廓/衣领/相纸；通话圆/方；错 PIN 有句子。  
7. 无整屏频闪：无循环动画。

## licenses

合同表有 `phone.css` / `intro-manual.css` / 两证 SVG / 引言封面 SVG / 系统字体栈。来源本局、随包、非 AI。未见商标/品牌字当主视觉；状态栏「中国移动」是机内 OS 皮，不升 D12 HOLD。

## dimension_verdicts

D1: PASS  
陌生人 3 秒见货架封面、油印纸条、链「拿起这部机子」。类型不靠 HUD。主动词是翻失物机，不是搜词官网。

D2: PASS  
进机后主操作在图库/列表/后仓四链；纸条不再当驾驶座（01 vs 21/16/20）。

D3: PASS  
抽 22 锁、16 图库、20 后仓：正视 2D；KitKat 塑壳 vs 荧光后仓所有者可分；证件 viewBox 中英同一套。

D4: PASS  
合同资产均有文件与引用。甲乙主循环已换成可辨 SVG（8916 缩略 computed `background-image` 指向两证）。相机卷后四张仍是几何现场，不是证件替身。旧 `.hair/.head/.col` 已 `opacity:0`，不挡 SVG。

D5: PASS  
空 PIN=11；错 PIN=12 有句可再试；成功=03；近答案=04；禁止=19；选中=07。成功/失败不同页。无独立暂停屏，本局无暂停态。

D6: PASS  
点 3716 → 即时 `home.html`；点甲/乙 → flags=1；点三月链 → ending-old 句变；近答案是交八月张，不是死档。反馈有句子与换页，不唯色、不靠音频、不靠 hover。未用毫秒表。

D7: PASS  
三锚与 `material_id=android-kitkat-2014-lcd-plastic` 钉在圣经/CSS。强调色绑未接/拒连，不绑「哪张是证件」。字形非玩法单位，`glyph_legibility_record=N/A`。

D8: PASS  
2D 写死；true3D 关。

D9: PASS  
320 能完成开锁与点图库主动词（06/28 + 8916）。触控目标抽测 ≥44 CSS px。主操作不依赖 hover。31 开局链在 320 需下滚，仍可点，不升本维 HOLD。

D10: PASS  
整包远低于 VP1 8MB；本地 http 有记录。帧率、首包秒表未测 → `did_not_prove`，不把预算锚写成实测帧。

D11: PASS  
7 步有记录且开评后抽查未缺门槛步。静音 N/A。减动后轮廓仍在。无整屏频闪。设置名未写防癫痫安全承诺。

D12: PASS  
许可表非空、可随包、无官方切图/品牌字主视觉、无新远程资源、无 AI 图。

D13: PASS  
本轮只复核新增中英 `33-five-skins.png`、合同截图项与返工记录。通用 12 与既有专属仍齐或 N/A。五套并排现为运行中单帧 PRESENT，见「D13 本轮复核」。D1–D12、D14 沿用上一轮 PASS，本轮未重开。

D14: PASS  
VP1 真实界面 + 书面改道。无新 AI 逐张提示词。未画成搜词顶栏或 VP2 房间。

## D13 本轮复核（只评新增）

路径：`visual/html-game-puzzle/polish-20260823-{zh|en}/33-five-skins.png`  
磁盘：中 403425 B，英 380248 B；均为 RGB PNG **6424×836**。  
几何：5×1280 视口 + 4×6px 灰缝 `(220,220,220)` + 顶栏 36px。与返工记录「1280×800、DPR 1、1×5、顶栏短标签」一致，同 viewport，无拉伸变形。

顶栏短标签（实读）：

| 格 | 中 | 英 |
|---|---|---|
| 1 | `8916/introduction.html` | `8917/introduction.html` |
| 2 | `8916/lock.html` | `8917/lock.html` |
| 3 | `8916/home.html` | `8917/home.html` |
| 4 | `8916/album.html` | `8917/album.html` |
| 5 | `8916/backroom.html` | `8917/backroom.html` |

覆盖 introduction / lock / home / album（图库两证，满足「album 或证件」）/ backroom。标签是端口+页名，不是 `file://` 或设计稿标注。

抽样像素与清单/返工记录逐点相符（页坐标 +36 顶栏）：中文引言 `(640,80)=(42,50,32)`、纸心 `(640,400)=(247,243,228)`；锁屏黑塑 `(40,40)=(29,29,23)`、屏心 `(640,200)=(18,53,58)`；桌面壁纸 `(640,200)=(14,61,66)`；图库黑底 `(640,220)=(26,26,26)`；后仓顶 `(640,40)=(26,26,15)`。英文纸心、图库中心、后仓顶与中文同值；锁屏屏心 `(640,400)=(28,74,79)` 如记录（文案占位不同）。无全局色滤镜：每格独立色数高，缝为中性灰。

格内画面与上一轮已认运行页同构，不是概念图/北星/编辑器：班前油印条「惠邻值班留下的字」/ Huilin duty、「拿起这部机子」/ Pick up the handset；KitKat PIN 盘 3:12、穗三号柜 / Tiansui locker 3；九图标桌面；图库甲乙无五官缩略；后仓「灯管嗡一声」/ The tube hums once 与四口信链。制作人写未点、未改存档；后仓 how 为未翻机默认句，与空档一致。

合同：`33-five-skins` 与 `五套并排单帧` 已 PRESENT；`五套_file` 指向该 PNG。制作人交卷 / 返工记录：只补这两张，未改实现、未改 01–32，`review_status` 未自填 PASS。`reviewer_role ≠ producer_role`。D11 上一轮已跑。非 VP5。

## largest_gap

无

## did_not_prove

- 真机手指触控（390 为 Chrome 模拟）
- 色觉模拟
- 运行帧率、首包可交互秒表
- 只键盘走完四结局全路径
- `prefers-reduced-motion` 开启态的独立截图（09 与 02 同帧）
- Cursor 内建浏览器本局抽查（改用本机 Chrome CDP 8916/8917）

