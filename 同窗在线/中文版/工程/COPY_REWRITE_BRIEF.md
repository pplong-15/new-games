# 同窗在线 文案去AI味重写 施工单

目标文件：
- `/Users/Zhuanz/Desktop/新游戏/同窗在线/工程/js/pages-a.js`（545 行）
- `/Users/Zhuanz/Desktop/新游戏/同窗在线/工程/js/pages-b.js`（437 行）

依据：`DEAI_REPORT.md`（同目录）的 G1（文案）/ G4（第四墙）/ G5（UI 载荷，页脚已由主线程改完，跳过 G4-01/G5-01 页脚那一条）逐条重写。

## 硬约束（违反即返工）
1. **只改可见文案字符串**，不动 JS 结构、变量名、数据键、函数、页面 id、`need`/`unlocks`/`grants`/`keywords` 里的字段。
2. **搜索词与专名的汉字一个字都不能改**：纸马课、沈记纸扎、沈亦舟、陈小北、周棠、江晚晴、沈秀兰、沈连山、林昭弟、沈昭弟、阴册、过继、替身、备用、埠头水库、还阴债、香火、纸人、关站、源码、先上香、最近来访、04计1班、班级公约。这些是 `data.js` 里 `keywords` 表和页面正文的锚点，改一个字搜索就断。
3. **不能删掉玩家读到搜索词的句子**：正文里首次出现某个搜索词的句子（哪怕要改句式）必须保留那个词。词表出处闭合是另一关，改完要能重跑通过。
4. 说明页（index/intro，`pages-a.js` L29-38 附近）可以承认"这是一份说明、这是虚构"，但不要把"游戏壳 / 隐藏页 / file:// / 工程目录 / 商标"堆成一屏，压缩成 身份 + 第一词 + 一次一词 + 帮助有停运客服，法律句放页脚小字。

## 改写原则（去AI味五关，按每条问题对照）
- **元语言不上屏**：主线 / 解锁 / 一并解锁 / 隐藏页 / 禁止页 / 唯一解 / 终局 / 结局 / 关卡 / 附带页 / 不卡主线 —— 这些游戏机制词从可见正文里拿掉。
- **第四墙不上屏**：游戏 / 游戏壳 / 游戏关卡 / 游戏分数 / 本游戏 —— 改成该皮会有的说法（关站、应用、积分、镜像、缓存）。
- **题解腔删掉**：「本页能证明 / 不能证明 / 你能证明 / 本篇能证明」这种设计文档腔整段重写，改成该页虚构皮会有的东西（户口变更一句、讣告一句、店招一行、表格一栏、公告一条），让玩家自己对，不要代做阅读理解。
- **「不是 X，是 Y」对称骨架改掉**：直接说事实主干，不先压后扬。
- **讲教员段删掉**：角色发言之后那 200-400 字第三人称解释段删掉或并入角色口吻；每页只留一种嘴。
- **三段排比/金句删掉**：「局分三层 / 表层·中层·里层」「克制不是温柔」这类，尤其结局页，只写玩家做了什么、站点变成什么样。
- **点名作业删掉**：「请搜这些词」「把 XX 送进搜索框」「搜栏目名才能看见」这类作者提示去掉，词留在像真的字段里即可。
- **禁止页（pages-b L227-230）**：改成 2000 年代校园站真有的话——「你没有权限查看该内容」+ 回首页，不要解释黑底难看、不要讲关卡设计。
- **选择页/结局页眉（pages-b L387-390、L406、L409、L428-429）**：去掉「终局」「结局一」「35/36」「不可用进度条代替」「表层中层里层」，按钮文案改成「打开附录 / 账号处理 / 注销账号 / 我来值班」这类站点语言。

## 已由主线程改过、无需再动的点
- 页脚 footerHtml（「虚构调查游戏」「已打开编号页」「说明」链接已删，现在是 `N/36 · 镜像缓存，勿对照现实机构或个人`）—— 不要再改页脚。
- 空串搜索「隐藏页」、登录「隐藏区」已改。

## 输出格式（重要，主线程要直接替换）
写清单到 `/Users/Zhuanz/Desktop/新游戏/同窗在线/工程/COPY_REWRITE.md`，每条：
```
### R01（文件：pages-a.js）
- old: <文件中逐字存在的一整行或整段原文，含 JS 引号本身，确保唯一>
- new: <替换后的完整文本，同样带 JS 引号>
```
要求：old 必须是文件里**逐字存在、且只出现一次**的字符串（含 `"` 或 `'`、`\"` 转义、`<p>...</p>` 标签都要照抄），否则主线程无法 `replace`。改完你自查一遍：每处新文案不含 主线/解锁/隐藏页/唯一解/终局/结局/关卡/游戏 这些禁用词，且原搜索词的字没丢。

只读 + 出清单，不改任何文件。中文。