# 试玩报告（文案去 AI 味后回归复验）

游戏 / 入口：同窗在线 · `http://127.0.0.1:8123/`（工程目录已有 `http.server`，未用 `file://`）  
构建：当前工作区静态多页（`js/pages-a.js` + `js/pages-b.js` 174 条可见文案重写后；页脚已改为「镜像缓存」）  
日期：2026-08-21  
主类型：封闭世界仿网页调查（读页 → 抽一个汉字词 → 站内搜 → 开隐藏页）  
目标渠道：自托管  
会话类型：总结性回归（针对文案重写 + 页脚 + 配图，不是盲测发布闸）  
执行环境：macOS · Google Chrome headless（`channel=chrome` / Playwright-core 驱动）· 1280×900 · 键鼠自动化  
知识标记：AUTHOR_KNOWLEDGE（对照过 `js/data.js` 词表与主链，不能当盲测）  
约束：只读实玩 + 写本报告。未改任何游戏文件。

---

## 结论

四项复验：**主链走通；搜索词先到页闭合（主链词全过，仅禁止别名「管理员密码」无可见出处）；无 JS 例外、无 `{{FLASH}}`/`{{SEARCH}}` 断裂；正文/页脚/选择页/双结局页眉的机制词清单已清。未做到「全站可见字符串绝对清零」——帮助客服第 2 档仍写出「终局」，JS 关闭时的 noscript 仍写「调查游戏」。**

可发给该渠道陌生人：HOLD  
一句话原因：本轮证明文案重写没有把主链搜断、没有把模板打裂；但本会话不是盲测，提示档仍漏机制词，不能据此改口「可以发给陌生人」。

| 复验项 | 判定 |
|---|---|
| 1 主链 纸马课→沈亦舟→陈小北→04计1班→阴册→江晚晴→族谱→不应到/接香 | **PASS** |
| 2 搜索词出处闭合（自指词先到页能读到） | **PASS**（主链与有效词）；禁止别名「管理员密码」无先到页，见缺陷 R2-03 |
| 3 JS 错误 / 文案断裂 | **PASS**（`pageerror` 空；占位符零命中） |
| 4 机制词清零（主线/解锁/隐藏页/唯一解/终局/结局/关卡/游戏/禁止页/能证明/局分三层） | **PARTIAL**：正文+页脚+终局页眉清零；提示档「终局」未清；noscript「游戏」仅 JS 关闭可见 |

---

## 闸

构建闸 B：PASS  
可启动：PASS（点「进入同窗」后首页新鲜事出现「纸马课」，顶栏搜索可提交）  
可完成：PASS（新档走到《不应到》《接香》）  
可保存：PASS（登录后刷新，`access_class` 仍在，停在 `class-feed.html`）  
可理解（盲测）：AUTHOR_KNOWLEDGE（本轮不报盲测 PASS）  
可解（品类）：PASS（实玩 DAG 与 `?test=1` 自测一致：无 blocked、双结局 need 为真、26 有效词、36 编号页）  
无障碍烟测：UNTESTED（窄屏/纯键盘/触控本轮未跑）  
平台硬条件：PASS（自托管 http 入口可用）  
合规：UNTESTED（本轮只验页脚虚构句，未做商标/商店页全套）

---

## 0. 四项怎么验的

Chrome headless 清档后点「进入同窗」，按指定主链点、搜、登录、读页。每打开一页采集 `document.title`、`body.innerText`、alt/placeholder、页脚，累计「已读正文」，再搜下一词。全部编号页走完后再扫一遍机制词。自测入口：`/index.html?test=1`。

页脚全站实测（引言 + 01–36）均为：

`N/36 · 镜像缓存，勿对照现实机构或个人。`  
引言为 `引言 · 镜像缓存，勿对照现实机构或个人。`

旧句「本站为虚构调查游戏」在 **JS 渲染后的可见页脚里零命中**。

---

## 1. 主链实玩（PASS）

### 1.1 逐步记录

| 步 | 动作 | 屏幕 |
|---|---|---|
| 0 | `/` 说明页 | 正文有「纸马课」。页脚已是镜像缓存句，不是「调查游戏」。 |
| 1 | 清档 → 进入同窗 | `home.html`。陈小北 2026-08-12：「你还记得**纸马课**吗」。 |
| 2 | 搜「纸马课」 | 4 条结果。点进 `blog-paperhorse.html`，《纸马课》。正文有「沈亦舟」「沈记纸扎」。页脚 `07/36`。 |
| 3 | 搜「沈亦舟」 | 点进 `space-syz.html`。留言写口令 **先上香**；有「最近来访」「04计1班」「班级公约」。头像 `img-avatar-syz.jpg` 800×800 加载。页脚 `09/36`。 |
| 4 | 搜「陈小北」 | 点进 `profile-cxb.html`，资料写 **04计1班**。相册两张图均加载（毕业合影 2048×1152、讣告 2048×2048）。 |
| 5 | 搜「04计1班」 | 点进 `class.html`。须知写要登录；有「班级公约」入口。 |
| 6 | 登录 | 空串：「请输入登录名和口令。」账号陈小北：「这是发链接的人，不是班级公共主页的管理员。」口令「上香」：「少了一个字。口令是店里那句完整的话。」账号沈亦舟 / 口令先上香 → 进入 `class-feed.html`。 |
| 7 | 刷新中局 | 仍在班级新鲜事；`verified` 含 `access_class`，与刷新前一致。 |
| 8 | 点公约 / 读第三条 | `class-rules.html` 可见 **阴册**。 |
| 9 | 搜「阴册」 | 点进 `yince.html`，标题「阴　册」。表上可读：江晚晴、林昭弟、沈昭弟、备用、替身、过继、沈连山、纸人。 |
| 10 | 搜「江晚晴」 | `profile-jwq.html`。状态「备用 / 未启用」。当时附录入口仍未开放（缺口述/名录授予，符合引擎）。 |
| 11 | 搜「林昭弟」 | 2 条：先是「沈氏族谱摘」，其次「阴册 · 摘录卷」。点进族谱，h1「沈氏旁支谱摘」，有沈昭弟、过继。 |
| 12 | 终局授予（引擎要求，不在用户缩写里但不开按钮） | 搜「沈连山」→ 口述页（还阴债、香火）。搜「纸人」→ 纸人名录，此时出现「打开附录」。 |
| 13 | `choice.html` | 页眉「到场与否」，不是「结局」。按钮「注销账号」「我来值班」。正文仍解释不应到 / 接香。无「结局」「终局」。 |
| 14 | 注销账号 | `ending-a.html` 标题/h1「不应到」，页脚 `35/36`。无「结局一」。 |
| 15 | 回选择页 → 我来值班 | `ending-b.html` 标题「接香」，h2「你已接班」，侧栏江晚晴，页脚 `36/36`。无「结局二」。 |

`?test=1` 静态 DAG：`blocked: []`，`p01`–`p19b` 全可达，`endings.logoutNeed/keepNeed: true`。

### 1.2 玩家轨观察（形成性，作者知识）

第一动作：首页读到「纸马课」，顶栏搜四字，命中博客。教学仍成立。  
首次别扭：说明页仍自称「说明书」，进站后首页不再教「第一个词」。这是重写意图，不是断链。  
卡点：搜「林昭弟」时结果标题仍是「沈氏族谱摘」，不含该词（旧问题，标题能点进，本轮未因此断）。终局按钮仍要求 **访问** 口述与名录，只搜词不够。  
失败与恢复：登录近答案有句子；空串/英文/两词搜索有句子，不白屏。  
结局复述：不应到 = 划掉备用行，来访归零，债不记在江晚晴名下。接香 = 自己变成第四代管理员，来访继续当香。因果能复述，页眉不再用「结局一/二」宣讲。  
想关的时刻：本轮正文不再刷「本页能证明 / 不要把本页看成游戏关卡」。篇幅仍长，但不像设计文档粘贴。

### 1.3 配图（顺带）

实玩加载均 `naturalWidth > 0`：沈亦舟头像、陈小北头像/毕业合影/讣告、周棠头像、纸马课合影、春游合影。无裂图。

---

## 2. 搜索词出处闭合（PASS，附一条禁止别名）

规则：搜该词之前，已打开页的可见正文里能读到该词（引擎不拦猜测；本项验的是设计上的先到页）。

### 2.1 指定主链

| 词 | 先到页（本轮第一次读到） | 打开的页 | 闭合 |
|---|---|---|---|
| 纸马课 | 说明页、首页新鲜事 | `blog-paperhorse.html`（附带回村） | 是 |
| 沈亦舟 | 纸马课正文 | `space-syz.html`（附带礼物） | 是 |
| 陈小北 | 首页 | `profile-cxb.html`（附带相册） | 是 |
| 04计1班 | 沈亦舟空间（校园页公开页也可读，本轮较晚才去校园） | `class.html` + `login.html` | 是 |
| 阴册 | 登录后公约第三条；班级新鲜事也有 | `yince.html` | 是 |
| 江晚晴 | 陈小北好友栏；阴册第 36 行更直 | `profile-jwq.html` | 是 |
| 林昭弟 / 沈昭弟 | 阴册第 19 行 | `genealogy.html` | 是 |

搜「过继」结果标题仍是「桐溪县情网 - 沈湾村」，不含「过继」二字；列表里同时有阴册、族谱。这是旧的标题不含词问题，不是这次重写把出处删掉。阴册备注里「替身」「过继」都在，先到页可读。

### 2.2 其余有效词（实玩均能搜开对应页）

纸马课页即可读：沈记纸扎、沈记、纸人、关站。  
空间可读：最近来访、班级公约、先上香。  
阴册可读：备用、沈秀兰、沈连山、替身、过继。  
族谱可读：香火（「会把香火两个字写在公告里」）。  
口述可读：还阴债、水库。  
县情网（过继打开后）可读：埠头水库。  
帮助可读：源码（「不要尝试查看源码」）。

实测命中（节选）：纸马课 4 条；阴册 3 条；林昭弟 2 条；香火 4 条（名录/祠堂/访谈/口述）；关站 3 条。禁止词「源码」「管理员密码」都进 `forbidden.html`，标题「没有权限」，不是主线答案。

### 2.3 未闭合

「管理员密码」只存在于 `data.js` 词表，全站可见正文从未出现。搜它仍进禁止页。不挡主线。

---

## 3. JS 错误与文案断裂（PASS）

| 检查 | 结果 |
|---|---|
| `pageerror` 未捕获例外 | 空 |
| 控制台 error | 仅 `favicon.ico` 404（`GET /favicon.ico` → 404）。Chrome 记在文档 URL 上，不是脚本崩。 |
| `requestfailed` | 空 |
| 可见 HTML 残留 `{{FLASH}}` / `{{SEARCH}}` / `[object Object]` | 57 次页面扫描 **0** |
| 登录近答案、空串搜索、英文、两词 | 均有完整中文句，无截断 |
| 选择页未齐材料 | 江晚晴页当时写「附录尚未对当前账号开放」，齐授予后名录出现「打开附录」，无死句 |

登录成功后的闪光句在跳转时被 `save()` 清掉，`class-feed` 上看不到「登录成功……」。近答案句留在登录页，主线不靠那句开门。视为既有存档行为，不是这次字符串重写造成的裂句。

自测：`emptySearch: 0`，`spaceSearchType: "multi"`，`enSearchType: "en"`，`paperhorse: true`。

---

## 4. 机制词清零（PARTIAL）

清单：主线 / 解锁 / 隐藏页 / 唯一解 / 终局 / 结局 / 关卡 / 游戏 / 禁止页 / 能证明 / 局分三层。

扫描范围：JS 渲染后的 title、innerText、alt、placeholder、aria-label、页脚；含引言、搜索边、全部已解锁编号页、禁止页、选择页、双结局、帮助客服四档。

### 4.1 已清（正文 / 页脚 / 终局壳）

- `pages-a.js` / `pages-b.js` 渲染正文：11 词 **0 命中**（「隐藏栏」不等于「隐藏页」）。
- 页脚：无「游戏」，无「调查游戏」。
- 选择页页眉「到场与否」，按钮无「结局」。
- 《不应到》《接香》document.title / h1 / h2 无「结局」「终局」。
- 空串搜索已是「请输入一个词。」不再写「隐藏页」。
- 禁止页可见文案是「你没有权限查看该内容」，无「禁止页」三字。

### 4.2 未清

1. **帮助客服第 2 档**（`js/data.js` hints.end[1]，本轮在已读齐后点「向客服发送一条」打出）：「终局在名录或祠堂指向的选择页。」同包第 18 档原文还有「打开名录后再去终局页表态。」——玩家点提示就能看见。  
2. **noscript**（每个 `.html` 模板，JS 关闭时）：「这是一款需要启用 JavaScript 的本地网页调查**游戏**。」JS 开启后 `engine.js` 会整页替换 `body`，正常实玩路径看不见。本轮 37 个 html 文件仍带这句。

除此以外，清单内其它词在 JS 开启可见层 **零命中**。

---

## 覆盖

主线结局：2 / 2（不应到 PASS，接香 PASS）  
失败/暂停/重试：登录空串/错账号/近口令 PASS；搜索空串/英文/两词 PASS；应用中心暂停页 PASS  
刷新中局：PASS  
窄屏：UNTESTED  
键盘：UNTESTED（`/` 聚焦未本轮实按）  
触控：UNTESTED  
静音：N/A（无必须的声音）  
UNTESTED 列表：盲测开局、390 窄屏、纯键盘、触控、iframe 嵌入、提示前三档在未通关存档上的逐档内容（本轮是通关后点的 end 包）

---

## 缺陷

S0：无  
S1：无  
P0：无  

### R2-01 帮助客服仍说「终局」

- id: R2-01  
- title: 帮助中心第 2 档提示写出「终局」  
- env: Chrome headless 1280×900，通关后存档  
- pre: `verified` 已含 `fact_spare/fact_papers/fact_cxb/fact_sls/fact_lin`  
- steps: 打开 `help.html` → 点「向客服发送一条」两次  
- expected: 可见文案不含「终局」  
- actual: 「第 2 档：终局在名录或祠堂指向的选择页。」  
- repro: 5/5（同源字符串在 `js/data.js`）  
- s: S3  
- p: P2  
- evidence: 实玩 `hint_texts[1]`；源码 `hints.end`  
- status: OPEN  

### R2-02 noscript 仍自称调查游戏

- id: R2-02  
- title: JS 关闭时 noscript 含「调查游戏」  
- steps: 停用脚本打开任意编号 html  
- expected: 可见层不出现清单中的「游戏」  
- actual: 「本地网页调查游戏」  
- s: S4  
- p: P3  
- status: OPEN（正常实玩 JS 开启后不可见）  

### R2-03 禁止别名「管理员密码」无先到页

- id: R2-03  
- title: 词表别名「管理员密码」全站正文不出现  
- expected: 自指词能在先到页读到  
- actual: 只存在于 `keywords`；搜它仍进禁止页  
- s: S3  
- p: P3  
- status: OPEN（不挡主线）  

关键 P1：无

---

## 意见（非缺陷）

- 搜「过继」「林昭弟」时，结果 **标题** 仍不含该词。出处在正文里，列表要靠点进或认「沈氏族谱摘 / 桐溪县情网」。旧体验，不是这次 174 条把词删了。  
- 双结局按钮不会在「只搜到江晚晴、林昭弟」时出现，必须实际打开授予页。这是「访问才记账」，本轮按此走通，不要当成文案回退。  
- 选完不应到仍能再选接香（结局互不锁死）。旧缝。  
- 登录成功闪光句跨页丢失，见第 3 节。  
- 本轮没有重新做去 AI 味五关的文风审；只证明机制词和主链没有被重写打穿。

---

## 返工

退回阶段：文案（小）  
下一步只修：`js/data.js` 里两处「终局」（客服 end/p18 档）改成站点口气（例如「名录末尾的账号处理」「到场那一页」）。可选：noscript 去掉「游戏」二字；禁止别名「管理员密码」要么写进帮助，要么从词表拿掉。

主链、页脚、选择/结局页眉、pages-a/b 正文 **不必为这次回归再动**。
