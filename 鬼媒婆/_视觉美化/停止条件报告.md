# 鬼媒婆　停止条件报告　gate-20260823

process_stop: HOLD
visual_expansion: 停止。不加装饰、不再出图、不扩场景、不改壳。
contract_status: DRAFT（见 `视觉合同.md`）
review_status: （留空给独立评审人；制作人不得填 PASS / HOLD / REWORK）
producer_role: 源文件与发布闸门机器人
reviewer_role: UNSET

## 结论

本局不能进入 VP1 美化。游戏库只有英文 v10.3 生成单文件；合同点名的可构建源（`app.js` / `game-core.js` / `world-content.js` / `styles.css` + `build-single.mjs`）在允许检索根内不存在。中文 v10.1.1 未进入 `游戏库/鬼媒婆/中文版`。英文整包实测 32 344 361 字节（30.85 MiB），超过 VP1「单文件常见 ≤2MB、媒体 ≤8MB」预算锚。合同禁止手改该生成 HTML。本轮未跨目录复制。

## 触发的停止条款（视觉档案部 §9）

- 媒体或整包超出该管线预算（英文单文件 30.85 MiB；内联 18 张 PNG + 1 份字体 data URL）。
- 修画面必须改分发形态才能落到 VP1 预算（禁止手改 30MB 生成 HTML；没有构建脚本就不能把图改成相对路径包）。
- 工作树缺中文运行包，无法按同库其它游戏做中英基线。

未宣称玩法回归、许可盗用或键位丢失。那些维未测。

## 唯一 largest_gap

D4 缺可构建源。

理由：工作流要改画面，必须先有可维护的 CSS/JS 源并经 `build-single.mjs` 再生成单文件。三根检索根内没有这份源。Downloads 的中文 v10.1.1 是生成单文件，不是源目录。英文 v10.1.1 zip 有模块文件但缺 `build-single.mjs`、`tools/`、`package.json`，不能按 README 构建，也不能生成 v10.3 图像版。

并列事实（不另开第二条 gap）：游戏库无 `中文版/`，中文运行包未入库。

## 检索范围（只读）

允许：`/Users/jianglong/Desktop`、`/Users/jianglong/Documents`、`/Users/jianglong/Downloads`。  
禁止：凭据、系统目录、跨目录复制、手改英文生成 HTML。

合同点名：中文 v10.1.1、`app.js`、`build-single.mjs`、含 `STORAGE_KEY` / `PUZZLE_BEATS` 的源目录。

## 检索结果

| 目标 | 结果 |
|---|---|
| `游戏库/鬼媒婆/中文版` | 不存在 |
| `build-single.mjs` | 三根目录内 0 个 |
| `package-cjk-font.mjs` | 三根目录内 0 个 |
| v10.3 模块源（LOC_QA 所写 `Documents/Codex/2026-08-08/new-chat/outputs/…`） | `outputs/` 为空目录 |
| 游戏库英文 v10.3 | 仅生成 HTML 32 344 361 B + 合同/QA/VERSION + logo PNG |
| 中文 v10.1.1 运行时 | Downloads 单文件 519 183 B（见下，本轮不入库） |
| 英文 v10.1.1 模块 zip | 有 `app.js`/`game-core.js`/`world-content.js`/`styles.css`，无构建链 |
| 更旧模块（v4–v8、English v1） | 有 `app.js`；存档键为 v7/v8/v1，不是 midnight-seven schema 11 |
| 中文 v9 zip | 有 `build-single.cjs`（不是 `.mjs`），版本不是 v10.1.1 |

### 中文运行时（未入库）

路径：`/Users/jianglong/Downloads/游戏/近期游戏打包/鬼媒婆/相关素材与可玩版/鬼媒婆游戏/鬼媒婆网页解密游戏_v10_1_1_导航修正版.html`  
`上线版/index.html` 会刷新跳到该文件。体积 519 183 B，低于 VP1 单文件 2MB 锚。这是生成单文件，不是可构建源。本轮禁止复制进游戏库。

### 英文 v10.1.1 残缺源（不可构建）

路径：`/Users/jianglong/Downloads/游戏/近期游戏打包/鬼媒婆/相关素材与可玩版/英文鬼媒婆/Ghost_Matchmaker_v10_1_1_English_Site_Source.zip`  
README 写 `node tools/package-cjk-font.mjs` 与 `npm test`，zip 内无 `tools/`、无 `package.json`、无 `build-single.mjs`。可当多文件站点阅读，不能按合同流水线构建，也不能产出 v10.3 Grok New4 图像版。

## 只读对照（不构成可以改画面）

中文 v10.1.1 HTML、英文 v10.1.1 zip 内 `game-core.js`、英文 v10.3 生成 HTML 三者：

- `SCHEMA_VERSION` 均为 `11`
- 13 条 `PUZZLE_BEATS` id 相同（`puzzle-archive-helishu` … `puzzle-submit-archive-bag`）
- 成功路由模式相同；旧报检索词中文为 `小玲`、英文为 `Xiaoling`
- 中文存档键 `ghost-matchmaker-v10-1-midnight-seven-state`（合同要求永不改）
- 英文存档键 `ghost-matchmaker-en-v10-1-midnight-seven-state`（v10.1.1 与 v10.3 相同）

此对照只说明血缘，不把残缺 zip 或生成 HTML 升格为可构建源。

## 体积（实测）

- 英文 v10.3：32 344 361 B = 30.85 MiB。内联 `data:image/png` 18 处、字体 data URL 1 处。
- 另有 `英文版/Ghost_Matchmaker_Seven_Minutes_Logo.png` 1 721 333 B，不在单文件内。
- VP1 预算锚：媒体 ≤8MB；单文件常见 ≤2MB。未真机测帧率。

## 英文只读启动

- 本地 http：`http://127.0.0.1:8930/`（已关）
- 非 `file://`，非 iframe
- 证据：`_视觉美化/evidence/00-http8930-dir-listing.png`（目录页，无 `app.js` / `build-single.mjs`）
- 证据：`_视觉美化/evidence/01-boot-first-screen-en-http8930.png`（引言「An old printout still has no verified source.」+「Enter Chengnan Q&A」）
- 未点进论坛、未搜词、未写入存档玩法进度

中文未在本游戏库 http 启动（文件不在工作树，本轮不复制）。

## 清理

- 已停 8930
- 已删本局 headless Chrome 用户目录 `/tmp/ghost-matchmaker-gate-8930`、`/tmp/gm-boot-*`
- 游戏库内无测试 profile / localStorage 文件
- 未改英文生成 HTML、未改 Downloads/Documents 源

## 回到工作流的最早坏步

第 1 步基线：英文可启动但整包超预算且禁止手改。  
缺可构建源，无法做第 6–8 步（可运行关键帧 / 最小资产包 / 垂直切片）。  
下一步只允许：把中文 v10.1.1 运行包与完整模块源（含 `build-single.mjs`）按书面方案安全复制进游戏库后再开评。本轮不做该复制。

## did_not_prove

- 中文 http 启动与通用 12 张
- 英文主动词实走、13 beat 通关、存档迁移
- 烟测 7 步、窄屏、键盘、触控、减动
- 独立评审 D1–D14
- 色觉、真机帧率、首包秒表
