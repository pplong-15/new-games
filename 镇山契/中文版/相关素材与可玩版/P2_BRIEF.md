# 镇山契 P2 打磨审查施工单

游戏入口（单文件 HTML，已修 8 个 P1，主链/双结局/反AI感硬伤已清零）：
`/Users/Zhuanz/Desktop/新游戏/镇山契/相关素材与可玩版/镇山契_第九位到访者_v0_3_真实网页与可视证据版.html`

本轮目标：把 grok 试玩标出的 P2 打磨项逐条定位，给出**精确改法**（改成什么文案/值/样式），由主线程用 Python 精确替换实施。**你只读 + 出方案，不改任何游戏文件。**

## 待打磨 P2 项（按优先级，定位用关键词 grep，行号可能已因上一轮改动偏移，以关键词为准）

1. **C5 终局 QA 清单腔**：终局面板有「最窄结论」「不得推出」这类 QA/公文腔。定位关键词「最窄」「不得推出」。改成真人在做处置判断的口吻，不是质检清单。

2. **C6 「镜像未保留」机械重复**：`FORUM_UI_META` 里 author/lastReply/editState 多处都写「镜像未保留」，反复出现。定位 `FORUM_UI_META`。改法二选一：(a) 变化措辞（如「作者佚失」「最后回复时间佚失」等），(b) 明确这是镜像系统的统一样式并减少出现频次。给出具体措辞。

3. **C8 工作台已结束仍写「先搜阿山」**：工作台进度全部结束后，页脚/引导仍提示「先搜阿山再回单」。定位关键词「先搜阿山再回单」。需随状态变化（进度结束时应恢复「继续办理当前项」或中性文案）。给出条件渲染改法。

4. **C9 drawingSet 字段值仍是英文**：geology 站 `drawingSet` 字段已补中文标签「图纸套」，但字段**值**还是英文代码。定位 `drawingSet`。给出把字段值改成中文的精确改法（列出现状值 + 建议中文值）。

5. **C7 「滑坡」vs「坡移」叫法不一致**：滑坡页用「滑坡」，报告用「坡移」，同一现象两个叫法。定位「滑坡」「坡移」。给出统一叫法。

6. **T1 窄屏论坛检索框不在首屏**：窄屏（~390px）下论坛检索框位置太靠下。定位论坛检索框。给出 CSS 改法（只描述改哪条规则）。

7. **T2 落地按钮 aria 不一致**：落地/主按钮 aria-label 与可见文案不一致。定位主按钮。列出不一致处 + 统一改法。

8. **I2 证据图「同件另挂」未标注**：32 个证据引用但只有 12 张独特图，重复引用的证据未标「同件另挂/同原件另存」。定位证据图渲染（`renderEvidenceVisual` 或 `FALLBACK_VISUAL_ASSETS`）。给出标注方案。

9. **I3 图内英文图层代码 + 过精像素**：SVG 证据图内有 AV-04、TEMPLE-SURVEY-1998 等英文图层代码。定位这些字符串所在 SVG。给出中文替换（哪些代码改什么中文标注）。

## 输出要求

写完整方案到 `/Users/Zhuanz/Desktop/新游戏/镇山契/相关素材与可玩版/P2_FIX_PLAN.md`，每条：
- 现状（找到的原文案/字段值/代码 + 大致行号）
- 精确改法（old → new 的明确文本，或 CSS/条件渲染的具体规则）
- 是否需要实玩/截图核验（T1/T2 需 Chrome headless 截图；I2/I3 需视觉核验）

只读 + 出方案，不要改任何游戏文件。中文。