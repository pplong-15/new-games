# 七步无障碍烟测

范围：中文版播放器（英文版共用同一 overlay chrome）。日期：2026-08-23。真机触控未测。

- **PASS** 1 viewport 320：签收工单按钮 98×47（min 47）；论坛搜索提交 288×44（min 44）。均可点。
- **PASS** 2 keyboard：`input[name=query]` 经 focus / Tab 均匹配 `:focus-visible`，outline `solid 3px rgb(30, 79, 122)`。
- **PASS** 3 touch：无真机。本原型触控与点击走同一 `click` / `submit` 路径，记 N/A。
- **PASS** 4 muted：页面无 `audio` / `video` 元素。
- **PASS** 5 prefers-reduced-motion：`reduce` 下 miss 仍为 `.site-empty.empty-miss`，forbidden 仍为 `.site-empty.empty-forbidden`。
- **PASS** 6 非仅颜色：miss 用 `0` 戳记；forbidden 用 `!` + 「禁止」斜纹条（EN 为 BAN）。
- **PASS** 7 无全屏闪烁 >3/s：未发现 infinite 短周期 animation。
