# 风格圣经　替度三名　检查室成组锁定　VP1　2D

本局锁死：正交检查室工作台正视。不是三十六页假官网。不是搜隐藏页。不是边检桌。不是逐行闪绿。

改皮不许改这组字段。禁止逐张自由提示词。本局只新出一张空白计件单（Python 绘碳纸空表），与既有 JPEG 同一相机/三色/材质。

## 相机 / 比例 / 线 / 光 / 材质

- camera_angle: 网页正视。无 2.5D、无透视桌面、无自由旋转。
- character_prop_ratio: 无角色精灵。可读比是「静帧位 : 三份文书材 : 锁定组整表」。
- line_weight_at_1x: 检查室壳 1px 夜绿线；户口左脊 8px 蓝；度牒双边线 3px 褐红；疏纸 2px 朱红框；白表 1px 纸线；霜表 2px 整表青灰，不逐行描。
- light_dir: 顶管日光灯平铺。禁止压暗、雾、全屏 vignette 当难度。
- material_id: 检查室夜绿荧光灯台面 + 悬挂期院子湿石静帧 + 户口卡纸 + 度牒证书纸 + 疏纸黄挂条 + 白表复写纸 / 霜表整组发青。

## 三色锚（强调色只绑锁定句和提交）

| 锚 | hex | 绑什么 |
|---|---|---|
| 主 | `#3d4a3a` | 检查室夜绿底、栏、工牌壳 |
| 次 | `#e8dcc4` | 默认文书纸、短信条、进站钮（进站不是提交） |
| 功能 | `#b54a3c` | 锁定句、交抽查表钮 |

禁止：把强调色绑到当前行、进站、再录、文书标题、工钱数字。禁止逐行闪绿。禁止国徽、边检护照格、tooltip 补不可辨字段。

## 所有者差（不读标题也能指认）

| 所有者 | 底色 | 边 / 脊 | 栏宽与形态 | 字体 |
|---|---|---|---|---|
| 检查室 | `#3d4a3a` 夜绿 | 顶管灯 6px + 1px 暗线 | 三栏 1.1 / 0.9 / 1.0 | 宋 / Songti |
| 户口 | `#eef1f6` 蓝灰卡 | 左脊 8px `#3d5a8c` | 满栏横表，横线格 | 宋 |
| 度牒 | `#e6d2ae` 褐皮纸 | 双边线 `#7a2e28` | 证书横幅，居中题 | 楷 |
| 疏纸 | `#f3e07a` 黄挂条 | 2px `#8b2a22` + 顶孔 | 略窄居中竖条 | 楷 |
| 白表 | `#f4ead4` 复写纸 | 虚线纸边 +「未锁定」字 | 整表，不按行上色 | 宋 |
| 霜表 | `#c5d0d6` 整表发青 | 内阴影青灰 +「已锁定」字 | 成组才换材，不逐行闪 | 宋 |

场所登记、笔记、计件单为桌上附件，不得画成边检证件。

## 静帧合同

- 台上只挂 `00-open` `01-still` 与点开的文书图。
- 未锁计件单只许 `07-form-blank.jpg`（三行空，破折号，无近答案名分/怎么没的）。
- `07-form-white.jpg` 图上已填近答案：不进未锁台面，不进 `#form-img`，不进按住态。
- `09-hold.jpg` 不在按住过程中出示。
- `08-form-locked.jpg` 只在锁定后可挂，不替 HTML 表。
- `11-ending-b.jpg` 乙屏不挂。
- 空白计件单：Python 绘碳纸空表，禁止加人脸、商标、可读门牌、真章机关名。中英同一文件。

## 控件

- 进站、热点、文书钮、下拉、提交、再录、走廊声 ≥ 44 CSS px。
- 热点虚线框常显，不靠 hover 才出现。
- `:focus-visible` 3px 实线，纸色；提交钮用纸色描边压在强调底上。
- 320 / 390：三栏改单列，不丢主动词。窄屏提交钮可换行，高度仍 ≥44。

## 减动

`prefers-reduced-motion: reduce`：无位移动画、无闪。锁定/白表靠字和整表材质，不靠动画。

## AI

本局不对计件单做模型直出。空白表按本圣经字段手绘。禁止逐张自由提示词。
