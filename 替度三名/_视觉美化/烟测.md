# 无障碍 7 步烟测　制作人自记　2026-08-24　http://127.0.0.1:8852 / 8853

1. 缩到 320 CSS px：计件单与锁定组可滚入视口，提交钮实测 288×44，文书钮 288×44，热点 ≥44。截图 `06-narrow-320.png`。无横向关键丢失（overflow=false）。
2. 只键盘：进站钮 / 提交钮可聚焦（`07-keyboard-focus.png`），下拉原生键盘。未做全程只键盘通关到甲。
3. 触控：390 视口主钮 ≥44（`08-touch-targets.png`）。未上真机手指。
4. 关声：本局无音频。主线不依赖声。
5. 减弱动效：`prefers-reduced-motion` 下近失败短信句仍在（`09-muted-or-reduced-motion.png`）。无位移动画。
6. 状态不唯色：白表「未锁定」字 + 霜表「已锁定」字 + 提交钮文案；近失败短信「表还是白的 / The sheet is still blank」。
7. 无整屏频闪。无逐行闪绿。

存档键仍为 `tidu-sanming-v2` / `tidu-sanming-v2-en`。SAVE_VERSION=2。
中英 sim.mjs SIM OK。甲锁 / 错组白表 / 乙截点见 `_视觉美化/play-verify.json`。
