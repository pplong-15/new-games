# 独立评审　替度三名　polish-20260824

review_status: **PASS**
largest_gap: **无**
reviewer_role: 独立评审人（未参与本局 CSS / sheet-stamp / 空白计件单 / 截图制作）
producer_role（合同所记）: 制作人机器人
contract_status（制作人交卷）: CANDIDATE
http 抽查: `http://127.0.0.1:8848/index.html`（中文）`http://127.0.0.1:8849/index.html`（英文）；8848/8849 空闲，未改用 8948/8949。非 iframe。`GET /index.html` 均为 200。
报告路径: `/Users/jianglong/Desktop/新游戏3/替度三名/_视觉美化/独立评审.md`

制作人自报最大缺口为 D1（`00-open.jpg` 墙图假 UI）。本评审按运行开局图判定：**进站钮可在 3 秒内找到**，墙图假控件会造成误点照片，但**不挡主循环**，故 **不升 largest_gap**。

---

## 交卷闸

| 项 | 判定 | 证据 |
|---|---|---|
| 合同 | 齐 | `_视觉美化/视觉合同.md`：CANDIDATE，VP1，2D，成组命运确认切片，未改道 |
| 制作人交卷 | 齐 | `_视觉美化/制作人交卷.md`；`review_status` 留空 |
| 许可表 | 齐 | `_视觉美化/licenses.md`：含 `07-form-blank.jpg`（PIL 手绘、非 AI）、CSS、index sheet-stamp、data.js 仅 img 路径 |
| 体积 | 落在 VP1 | jpeg 约 1335 KB（制作人 1367368 B）；中文整包约 1394 KB / 英文约 1400 KB；CSS 11.3 KB。媒体 ≪ 8MB |
| 烟测 7 步 | 有记录 | `_视觉美化/烟测.md`（制作人自记）。本评审抽查 http 200、截图矩阵、控件 JSON、CSS 44px、引擎哈希 |
| 通用 12 | PRESENT | `visual/html-game-puzzle/polish-20260824-zh|en/01`–`12` 与 `-before` 成对 |
| 解密专属 ≥8 | 齐 | 合同 13 两源文书、14 户口、15 度牒、16 静帧未锁无 07/09、17 白表、18 霜表、19 疏、20 引言无搜；另有 21 乙 / 22 甲 |
| before/after | 同目录成对 | `{nn}-{slug}-before.png` / `{nn}-{slug}.png`；中英两套 |

合同必填栏已填。`reviewer_role` 合同内仍 UNSET（制作人不得填评审），本文件填独立评审人。

---

## 玩法锁（相对基线）

| 检查 | 结果 |
|---|---|
| `engine.js` SHA vs `_视觉美化工作/基线清单.sha256` | 中英均 **一致**（未改判定） |
| `app.js` SHA | 中英均 **一致** |
| 三行 TRUTH / NAMES / FATES / HOLD_MS:1500 / SAVE_KEY | **未改**。中文 `tidu-sanming-v2`，英文 `tidu-sanming-v2-en` |
| 锁定提交 | `app.js` 仍：未锁 `#form-img` hidden 且去 src；锁成才挂 `08-form-locked.jpg` + `show-locked`。近失败 `group-white` 句「表还是白的。不标哪一行。」 |
| `data.js` | **仅** `doc-pay.img`：`07-form-white.jpg` → `07-form-blank.jpg`（及英 caption）。**不是**引擎/三行 value/锁定逻辑 |

此 img 改动是视觉防剧透（空白计件单），不改成组对错。不构成 HOLD。

---

## 抽查项

### 中英户口 / 度牒 / 疏纸可分
- CSS 三材：户口蓝脊横格宋、度牒褐皮双边楷、疏纸黄挂条顶孔楷（`#doc-hukou` / `#doc-dudie` / `#doc-shu` 与预览 `:has(img[src$=…])`）。
- 运行截图 `13-two-sources-papers`、`14-sheet-hukou`、`15-sheet-dudie`、`19-sheet-shu`：列表钮色差 + 预览纸形差，不靠标题才能指认。英文同皮。

### 白表未锁定 / 霜表已锁定，非唯色
- 字标：`未锁定` / `已锁定`（英 `Not locked` / `Locked`），CSS `:has(.lock-table.frost)` 切换显示与戳底。
- 材质：白表 `#f4ead4` 虚线；霜表 `#c5d0d6` 内阴影。`17-white-table-unlocked` vs `18-frost-table-locked`。
- 近失败短信仍写「表还是白的」，不标行。

### 成组锁定成功 vs 白表近失败
- `03-success-feedback` / `22-ending-a-wage`：甲、已锁定、工钱 ¥36、霜表。
- `04-near-fail` / `05-recovery`：三行都写成周阿海后表仍白、未锁定、可改下拉。
- `_视觉美化/play-verify.json`：近失败 `locked:false wage:0 table white`；甲 `endingId:A wage:36 form 08-form-locked`；乙无 `11-ending-b`。

### 320 / 390 / 44px
- `06-narrow-320.png` 文件像素 **320×700**。中文 JSON：提交 288×44，文书钮 288×44，热点 ≥44。
- `08-touch-targets.png` **390×844**；390 JSON 提交 358×44。
- 英文 320：提交 288×44，gossip **88×44**。
- 中文体积 JSON 曾记 `#gossip` 56×25，与**现行** CSS `min-height:44; min-width:88` 不一致（截尺寸早于后补 CSS）。主循环提交/文书/静帧热点均 ≥44。不升 largest_gap。

### sheet-stamp
- `index.html` 只有「未锁定 / 已锁定」，**不写哪一行、不写近答案人名**。
- 切换纯 CSS `:has(.lock-table.frost)`，**不改** `engine.js` 判定。

### CSS 藏 07-form-white / 09-hold
```
#doc-preview img[src$="07-form-white.jpg"],
#doc-preview img[src$="09-hold.jpg"],
#form-img[src*="07-form-white"],
#form-img[src*="09-hold"] { display: none !important; }
```
未锁台面 `#form-img` 默认 hidden、无 src。计件单预览改挂 `07-form-blank.jpg`。`16-still-clean-token` / `token-07-09.json`：`has07/has09 false`，`formShown false`。

### D1 开局（制作人自报点）
- 证据：`01-boot-first-screen.png`（1280×800）。墙图 `00-open.jpg` 内确有假表、工牌、假锁定组。
- **真进站钮在图外**、纸色底+夜绿字、与墙图留出间距，3 秒内可找到。
- 误点墙图只点到静图，不提交、不锁表，随后仍能点进站。**不挡主循环 → 不升 largest_gap。**

---

## D1–D14 正式判定

开评后不留 UNTESTED。

| 维 | 判定 | 理由 |
|---|---|---|
| D1 首帧类型识别 | **PASS** | 3 秒能找到进站并读到「三份不能再是同一个人 / 对了才锁」。类型不靠标题字幕。墙图假 UI 见上，不挡循环。 |
| D2 信息层级 | **PASS** | 进站后三栏：静帧 / 文书 / 锁定组（`::before` 标签）。主循环在三栏，顶栏工牌/短信/钟/工钱为状态。 |
| D3 风格合同一致 | **PASS** | 抽开局、台面、甲屏：同一正交网页、夜绿壳、顶管灯、宋/楷纸材，无第二相机。 |
| D4 资产完整度 | **PASS** | 合同资产均有文件。新增 `07-form-blank.jpg` 为空白碳纸计件单，不是临时代换矩形。原 00–11 像素哈希与基线一致。 |
| D5 关键状态可读 | **PASS** | 空预览虚线句；白表未锁定；霜表已锁定；近失败白表+句；松手句；乙未锁¥0；甲已锁定¥36。加载/暂停 N/A（合同已写）。成功失败不同帧。 |
| D6 动作反馈 | **PASS** | 进站→台面；点文书→预览换皮；按住成组→霜表或白表句。近失败不是正解。双通道：字+材质，不靠声、不靠 hover。 |
| D7 色/光/字形 | **PASS** | 三锚 `#3d4a3a / #e8dcc4 / #b54a3c`。强调色绑锁定句与提交。VP1 无字形单位。 |
| D8 空间维度 | **PASS** | 2D 写死。`true3d_exception.enabled=false`。 |
| D9 响应式可操作 | **PASS** | 320 截图+JSON 主钮 44px；390 同。触控不依赖 hover。走廊声 JSON 旧值见抽查，不挡提交。 |
| D10 性能预算 | **PASS** | 媒体与整包远低于 VP1 8MB。本地 http 进站钮可见。真机帧率未测 → `did_not_prove`，不挡体积闸。 |
| D11 无障碍底线 | **PASS** | 状态不唯色；无音频；减动截图近失败句仍在；无整屏频闪、无逐行闪绿；`:focus-visible` 纸色描边。未做全程只键盘通关、未上真机手指、未跑色觉模拟器。 |
| D12 原创与许可 | **PASS** | 许可表有文件名/来源/许可证/可否再分发/是否 AI。虚构县局。系统栈宋楷，零远程字。无商标主视觉。 |
| D13 截图证据链 | **PASS** | 运行 PNG；路径 `visual/html-game-puzzle/polish-20260824-{zh,en}/`；通用 12 + 专属 ≥8 + before/after。搜词/隐藏页 N/A 已写原因。 |
| D14 管线匹配 | **PASS** | VP1 真实界面。无改道。本局新图仅空白计件单，受同一相机/纸材约束，非逐张自由提示词。 |

---

## 烟测对照（评审抽查，非制作人自证）

1. 320：有 `06-narrow-320.png`（320px 宽）+ `control-sizes-320.json` 提交 44px。
2. 键盘：`07-keyboard-focus.png` 进站钮 focus-visible。未复跑全程只键盘。
3. 触控：390 视口截图 + JSON ≥44。未真机。
4. 关声：无 BGM/SFX。
5. 减动：`09-muted-or-reduced-motion.png` + CSS `prefers-reduced-motion` 去 transition。
6. 不唯色：未锁定/已锁定字 + 霜/白材。
7. 无整屏频闪。

---

## did_not_prove（评审）

- 真机 30fps / 滚动假死毫秒
- 色觉模拟器
- 真机手指
- 全程只键盘通关到甲
- 高精度 Navigation Timing 首包毫秒
- Cursor 内嵌浏览器本评审未挂上 tab（改用本地 http + 运行截图 + SHA）

---

## 观察（不返工、不升 largest_gap）

- `00-open.jpg` 仍是合成检查室假界面；进站钮在图外，可找到。
- 中文 `control-sizes-*.json` 的 `#gossip` 高度与后补 CSS 不一致；英文 320 已是 88×44。
- `play-verify.json`（visual 目录旧探针）曾写未锁 pay 仍为 `07-form-white`；现行 `data.js` 与 `_视觉美化/play-verify.json` 已指向 `07-form-blank.jpg`。以现行实现为准。

---

## 结论

review_status: **PASS**  
largest_gap: **无**  
D1–D14 均 PASS。引擎与三行锁定提交相对基线未改。07/09 不进未锁台面。sheet-stamp 不剧透近答案。
