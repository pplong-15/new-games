# GATE_B　开发验收

## DAG
无环。初始何家已在队列。下一封只在上一封提交后弹入。附件在打开该单后推送。纸扎下层、民俗馆下层跟目录页走。纪单未写则回执不可交（天亮空栏除外）。

## 可点
- 队列链接、推送栏链接、常驻白板／帮助／米穗条／流水：已点。
- 七封建议栏可提交，提交后回队列。
- 皮上检索框一律到 `search-closed.html`，不开启新档。
- 未到号的单、未推送的附件：守卫回值班台。

## 引擎
`js/state.js` 数据驱动：ORDER / PUSH / EXTRA / HINTS。权威只有建议。按钮无批准、核定、准予入龛。

## 语法与实点
`node --check js/state.js` 通过。本地 `http.server:8771` 用系统 Chrome 走完正确分拣与误放行两条，无 `pageerror`。跳号、未开门附件、检索残页均按预期。

## 结论
**PASS**。可进试玩报告。
