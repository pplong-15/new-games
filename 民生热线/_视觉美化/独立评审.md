# 民生热线　独立评审　20260823

reviewer_role: 独立评审人（未参与本局 CSS / 主图集 / 截图制作 / 静帧重编码）
producer_role: 制作人机器人
contract_status_observed: CANDIDATE
review_status: **PASS**
genre / genre_profile / pipeline_id: html-game-puzzle / XP停更片库检索 / VP1
space_dimension: 2D
distribution_form: E（未改）
play_specimen: Her Story（只借一次五条检索；不借 FMV/警局皮）
largest_gap: D13 — `after/12-paper-ending.png`、`15-lock.png`、`16-old-save.png` 三文件同 SHA-256；`03-five-results.png` 与 `17-reduced-motion.png` 同哈希。15 与 16 同为 lock=paper 结局戳，**不能算两个证据项**。去重后专属仍 ≥8，且本评 http 已补测旧档锁与减动，故不据此打回。

本评只读实现与交卷证据，只写本文件。未改 HTML / CSS / JS / 静帧 / ref / 其它证据。

---

## 0. 开评范围与方法

审核对象：`/Users/jianglong/Desktop/游戏美化/民生热线`（中文版 / 英文版 / `_视觉美化/`）。  
对照：视觉合同、制作人交卷、licenses.md、风格圣经、smoke-run.json、d10-measure.json、截图矩阵；基线 `/Users/jianglong/Desktop/游戏美化/_视觉美化工作/基线清单.sha256`；ref `/Users/jianglong/Desktop/游戏美化/_视觉美化工作/ref/民生热线-base/`。

本评自开本地 http（非 file://、非 iframe；与制作人 9004/9005 分离）：

- 中文 `http://127.0.0.1:9104/`
- 英文 `http://127.0.0.1:9105/`

用独立 Playwright + 本机 Chrome headless 走中英主路径。制作人自报只作对照，不以自证代替抽查。交卷后已停 9104/9105。

---

## 1. 合同与交卷完整性

| 项 | 判定 |
|---|---|
| 合同必填字段 | 主循环字段齐（genre / VP1 / 标本 / 三色 / 材质 / 分发 E）。动画表、资产清单、字重记录等栏偏薄，标 UNSET，不升为 HOLD。 |
| 制作人交卷 | 齐。`contract_status=CANDIDATE`。制作人未写 PASS。 |
| 许可表 | 非空。见 §5。 |
| 体积 | 本评实测与交卷一致。见 D10。 |
| 烟测 7 步 | 合同有记录；本评按步复跑。见 D11。 |
| 通用 12 | 01–12 均有 before/after 中英成对文件。 |
| 片库专属 ≥8 | 去重后仍有 03 五条、04 监视器、05 四键禁、06 空井、10 390、11 paper 键、12 paper 结局、13 milk 键、14 milk 结局、18 字条、19 网管。15/16 不计两证。 |

CANDIDATE 前置满足。开评后 D1–D14 不再留 UNTESTED。

---

## 2. 锁文件：相对基线

| 锁 | 结果 |
|---|---|
| `中文版/js/clips.js` | MATCH `70969827c8be1fbacfbbf503673ce404321ebb89904b9687ad5f42da185b0ddb` |
| `英文版/js/clips.js` | MATCH `35ff29dd47aa52a50982d97c8751815b16b241acb625a379201d05f0e0b8b4f2`（**未被覆盖**） |
| `英文版/js/clips.zh.js` | MATCH 中文 clips（源稿，index 未加载） |
| `中文版/js/tokens.js` / `英文版/js/tokens.js` | MATCH；键 `minsheng-v1` / `minsheng-v1-en` |
| `中文版/index.html` `introduction.html` 及英文对应 | MATCH |
| `中文版/js/engine.js` / `英文版/js/engine.js` | 相对基线已改。本评核对仍为 `CAP = 5`、同一检索规则、同一 token 图、同一结局文、同一 lock 字段。新增 `tabIndex=0` 与行上 Enter/Space。现场 Tab/Enter 可用。 |
| 280 片 ID / 正文 / `img` 路径 | clips.js 未改，故 ID、逐字稿、`assets/stills/Cxxx.jpg` 与基线一致。静帧字节因 JPEG 重编码变化，属 D10 允许项。 |
| distribution_form | 合同与交卷均为 E；仍为多页相对路径、中英分目录。 |

`英文版/tools/build_library.py` 写明有 `clips.zh.js` 时不覆盖英文 `clips.js`，BASE 改读 ref。属工具护栏，未改玩法标本。

---

## 3. D10　体积、base 迁出、恢复清单

本评独立 `stat` + SHA-256，不采用制作人 JSON 代替实测。

| 项 | 中文 | 英文 |
|---|---|---|
| 静帧张数 | 280 | 280 |
| 文件名集合 | 与对语种一致 | 同 |
| 逐张哈希 | 280/280 中英相同 | 同 |
| 玩家媒体合计 | **6,166,266 B**（约 5.88MB） | **6,166,266 B** |
| 预算 8MB | ≤ 8,388,608 | ≤ |
| 玩家目录其它 jpg/png/音视频 | 无 | 无 |

**base 迁出：** 中文版/英文版 `assets/` 下现仅 `stills/`。玩家包内无 `assets/base`。http `.../assets/base/clinic-bench.jpg` → **404**。运行代码引用 `assets/base` 仅出现在 `中文版/GATE_C.md`（文档，非运行）。源树在 `_视觉美化工作/ref/民生热线-base/{中文版,英文版}/assets/base/`，各 16 张 JPG。

**恢复清单：** `SHA256.txt` 32 条；ref 内 32 张 JPG；缺 0、哈希不符 0、多余 0。`RESTORE.md` 含可逆 `cp -R`。**是迁出到根 ref，不是删源后仍留玩家副本。** 玩家包未因 base 超预算。

clips 的 `img` 280 条均指向 `assets/stills/C*.jpg`，缺文件 0。

---

## 4. 功能回归抽查（本评 http 9104/9105）

中英同构。检索规则未改：中文去空白、仅汉字、最短二字、按 date/id 排序、`slice(0, CAP)`；英文去空白、大小写不敏感、最短两字母。

| 抽查 | 中文 9104 | 英文 9105 |
|---|---|---|
| paper | 搜「月奖金」→ 看 C044 → `end-paper` 亮、余键仍禁 → 结局「公文成片」+「已交片」戳；存档 `lock:paper` | `bureau` → C019 → Circular cut + FILED |
| milk | 「耳背」「好听」「香灰」各看一条 → `end-milk` 亮 → 「成片过了」 | Duty / folks / reflex → Cut cleared |
| near-fail | 未看带时四键 `disabled` + 删除线 + 斜纹，不是唯灰 | 同 |
| 旧档锁 | 刷新后 `#ending.show`，标题仍「公文成片」 | 仍 Circular cut |
| 五条上限 | 「应声」共 13、只列 5；meta 写后面不亮 | Answer 17/5；Liu 13/5（含 C194 进前五，仍只 5 行） |
| 空结果 | 「喹啉」→「0 条」+ `#results::before "0"` 虚线井 | `zzzz` → 0 hits |
| 过短 / 非汉字 | 「一」太短；`ab`「只有汉字」 | `A` Too short |
| 监视器 | 点首行：`win-view` z=8 > `win-db` z=4；OSD+静帧+逐字稿 | 同结构 |
| 320 | 窗 `position:relative`；检索 44px；五条可点；overflowX=0；四键可见 44px | 同 |
| 390 | 同上，叠后四键可见 | — |
| 44px | `#q` `#go` 高 44；成片键 44；引言进厅 44 | 同 |
| 真实 Tab | `#q` → `#go` → `.clip-row`×5 | `#q` → `#go` → 五行 |
| 真实 Enter | 焦点在 `.clip-row` 上按 Enter → 监视器 `window show` | 检索框 Enter 出五条 |
| 减动 | `html.reduce` 下「应声」仍 5 条；结局戳 `transform:none`（未减动时 stamp 有旋转矩阵） | — |

字条 / 网管 `z=5` 压在片库 `z=4` 上。无音轨。无整屏频闪。

**15/16：** 制作人截图 12/15/16 字节相同，不能证明「交片瞬间」与「旧档开机」是两张不同画。本评用刷新旧档补了行为证据：锁态可恢复。矩阵里仍不得把 15 与 16 算两条专属。

---

## 5. 许可、体积、before/after

- 许可表：CSS 本局随包；280 静帧为基线 JPEG 再编码、本局未新出 AI 图；系统字体不嵌入；无商标切图、无远程字库/远程图。
- 玩家媒体 5.88MB/语种 ≤8MB 锚。截图 PNG 不计玩家包。
- before/after：19 对文件名成对；主循环帧（01–11、13–14）after 与 before 哈希不同，可见 Luna 桌、空井 0、斜纹禁键等差分。before 侧亦有复用（04/05/08 同哈希；12/16/17/18/19 同哈希），before 链不干净，记入 D13，不单独升维。

---

## 6. D1–D14

开评后缺证据不得写 UNTESTED。

| 维 | 判定 | 依据（本评） |
|---|---|---|
| D1 首帧类型 | **PASS** | 引言是县台回执纸、无检索框；进厅后才是 Luna+DigiCut。3 秒能认出主动词是检索片库。 |
| D2 信息层级 | **PASS** | 主循环在 DigiCut 检索/五条；叙事在字条/网管；成片四键在独立窗。 |
| D3 风格合同 | **PASS** | 抽 02/03/04：同一 Luna 蓝桌、同一窗口脸、同一 DigiCut 暗槽。引言故意异皮。 |
| D4 资产完整 | **PASS** | 280 静帧有文件、有 clips 引用、无灰盒主循环对象。base 不在玩家包。 |
| D5 关键状态 | **PASS** | 空井大号 0；过短句在 `.db-meta`；成功 1–5 编号行；近失败斜纹+删除线；锁态双线戳。不唯色。 |
| D6 动作反馈 | **PASS** | 点检索/按 Enter → 同窗列出前 5；点行 → 监视器盖住检索。近答案（四键仍禁）不是正解。 |
| D7 色/光/字 | **PASS** | 三锚 `#3a6ea5 / #ece9d8 / #3c8d0d` 绑桌面/窗脸/开始与已解锁键。无 VP5 字形单位。 |
| D8 空间维度 | **PASS** | 2D 正交桌面。`true3d_exception.enabled=false`。 |
| D9 响应式 | **PASS** | 320/390 主检索可点，窗改 relative 堆叠，四键可见，无横向溢出。触控目标 44 CSS px（本评量盒；真机未握）。 |
| D10 性能预算 | **PASS** | 见 §3。本地 http 200。帧率未测，记 did_not_prove，不挡体积闸。 |
| D11 无障碍 | **PASS** | 七步：320 可点；真实 Tab 到检索钮与结果行、Enter 开监视器；窄屏点同一路径；无音轨 N/A；减动后五条仍出、戳不转；状态不唯色；无整屏闪。制作人 08 曾脚本 focus，本评不以该张代替键盘步。 |
| D12 许可 | **PASS** | 表非空；可随包；无官方切图/品牌字当主视觉。 |
| D13 截图链 | **PASS** | 通用 12 文件齐；专属去重后 ≥8；均为运行 PNG 非设计稿。路径在 `_视觉美化/screenshots/` 而非 skill 示例 `visual/{profile}/…`，本批目录合同如此，不升 HOLD。**15/16 不充两证；17 与 03 同哈希，减动张无效，以本评现场减动补。** |
| D14 管线 | **PASS** | VP1 真网页壳。主循环仍是片库检索不是登山搜词官网。改道理由写在合同。无逐张自由 AI 主视觉。 |

---

## 7. 总评

review_status: **PASS**

主循环可读：Luna 桌、DigiCut 一次五条、监视器压检索、四键近失败斜纹、交片戳。D10 体积与 base 迁出成立。玩法锁（CAP、检索、token、存档键、280 正文、分发 E）未破。英文 clips.js 未被 build 覆盖。

did_not_prove：真机触控、色觉、秒表首包、运行帧率、物理键盘通关全程录像。

返工：无。若制作人自愿补图，只补两张独立运行帧：① 旧档刷新锁态且能与 12 区分流程说明（像素可以相同，但不得再复制文件充 15/16）；② `html.reduce` 下仍能检索的减动帧（不得再复制 03）。此条不是本评打回条件。
