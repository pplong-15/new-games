# GATE_B　开发验收

## 数据与 DAG

- 词与 token 存在 `js/engine.js`，存档键 `zhizha-juzi-v1`。解锁看持有 / 袋词，不看点击次数。  
- 拓扑：页上采词 → 袋 → 句卡六格齐 → `pickEnding`。打开页 `v-*` 只加固。无环。  
- 悬空：句卡选项必须先在已打开页上以 `data-w` 出现。六格未齐返回 `null`，不成组不判。  
- 近答案：落点文具店 / 货主娄石 / 品名童女+混进 / 缺件 / 已焚 走回句卡并给一句冲突，不闪格。  
- `pickEnding`：六格正解 ∧ 建议 → `filed`；童女已焚 / 邵浦已故 / 童女+已焚 → `burned`；准烧或附录扎法 → `bounced`。Node 已跑通。

## 可点

- 引言同屏：人、差事、词袋、空卡、可点「童女」；三处门：夜班台 / 后院 / 浦页。  
- 夜班台接到堆、丧单、发票、句卡、电话条、权限、价目、旧样、娄石、井秋白、县摘、街坊。  
- 查找框 `action="#"`，不开新页。家园 / 导读 / 沿革 / 暂停栏目为死链噪声。  
- 三回单由句卡提交跳入，不挂在公开导航（避免未交先读回单）。  
- 权限恒为建议。回单不批准焚化，不附扎法。

## 皮肤

CSS 从 web-skins 整文件复制。七所有者七皮。引言无站内查找框。假官网不用 intro-manual。

## 脚本

`node --check js/engine.js` 通过。引擎三终点键在 Node 里仍通。

## 结论　PASS

可进试玩。红线未动：只建议、整句才判、limits、禁搜隐藏页、正确收口 `filed`。
