# 清明三门　独立评审　polish-20260823

review_status: **PASS**
contract_status（制作人交卷）: CANDIDATE（制作人未改；评审正式字段见本文件与合同 `review_status`）
genre / genre_profile / pipeline: html-game-puzzle / 三户交班知识留存 / VP1
评审人: 独立评审（未参与本局制作、未画主界面 / 主图集 / 主 SVG）
评审日期: 2026-08-23
复核日期: 2026-08-23（只复核 D13 新运行截图；未改实现、未补图）
构建闸: 首评 `9114`/`9115`；D13 返工拍摄 `9214`/`9215`（还原基线 before）与 `9216`/`9217`（当前包 after），均已停
知识标记: AUTHOR_KNOWLEDGE（读合同/交卷/实现/矩阵；非陌生人盲测）

## 总评

**PASS。** 首评 D1–D12、D14 已过。本轮 D13 新运行截图补齐通用 03/07/10、已开一门 / 满额三门 / 交班留存同屏，中英 before/after 分别来自还原基线与当前包，页面非连接错误；07 为真 Tab。玩家包 156 文件相对基线仅 22 个已知美化文件不同，JS 哈希未变。

首评曾因缺张与 `ERR_CONNECTION_REFUSED` 写 D13 HOLD。该缺口已由本轮矩阵闭合。

## dimension_verdicts

| 维 | 判定 | 证据摘要 |
|---|---|---|
| D1 首帧类型识别 | PASS | `01-boot-first-screen.png`（中英，运行中）三秒可读：夜班接班、本班开三户、写备注。不靠标题字幕冒充类型。 |
| D2 信息层级 | PASS | 值班台：印位条（状态）/ 门列表（主操作）/ `.pad` 叙事。主循环发生在门列表与交班钮。 |
| D3 风格合同一致 | PASS | 抽引言油印、值班青台、园务红头（01 / 02 / 33）相机均为正视 2D 页；所有者 chrome 可分。 |
| D4 资产完整度 | PASS | 八张 `.jpg` 真 JPEG、中英逐字节一致、引用存在且 http 200 加载；无临时代换矩形冒充主循环对象。 |
| D5 关键状态可读 | PASS | 空白板 11；收口 22；近失败/退回 23、24、05。值班台已开▣实框内条 / 满额✕斜纹虚框删除线 / 未开□，不唯色。见附记。 |
| D6 动作反馈 | PASS | http：`openDoor` 第四户 `{ok:false,reason:full}`；值班台立刻 `3/3` + `used`/`locked`。近失败 ZH 23 鬼 / 24 越权与正解 22 不同帧。 |
| D7 色/光/字形 | PASS | 三锚与 `material_id=oilprint-duty-desk-paper` 已钉；强调色 `#7a2a12` 绑热门/警告，不绑四栏正解。无字形单位。 |
| D8 空间维度 | PASS | 合同 `2D`；`true3d_exception.enabled=false`；运行页无透视/3D 操作。 |
| D9 响应式可操作 | PASS | 评审 CDP：320 / 390 CSS px 值班台与备注主控件高度均 ≥44。本轮英文 `06-narrow-320.png` 已换成运行中窄屏值班台。 |
| D10 性能预算 | PASS | 玩家媒体中英各 **1403158 B（1.338 MiB）≤ 8 MiB**。http 引言可点。帧率未测（did_not_prove，不挡本维预算）。 |
| D11 无障碍底线 | PASS | 首评 Tab 实测 + 本轮 07 真 Tab 图：after「钟/Clock」`3px dotted #111`，before 为浏览器默认 `1px auto`。无音频。减动规则在。不唯色见 D5/10。 |
| D12 原创与许可 | PASS | 许可表非空；rollback-img 仅八张原静帧、无 html/js/css 玩家包；无商标/品牌字当主视觉；本局未新出 AI 图。原图生成链 UNSET，见 did_not_prove。 |
| D13 截图证据链 | **PASS** | 本轮中英 before/after 运行页齐；03/07/10 真实；17/18/19/20 已开一门、满额三门、交班清 opened 留 verified 同屏。见「D13 复核」。 |
| D14 管线匹配 | PASS | VP1 真网页壳；无逐张自由提示词新图；八张静帧只重编码。 |

正式判定无 FAIL。D1–D14 均为 PASS。

## largest_gap

无。

## D13 复核（2026-08-23 晚）

只审本轮新运行截图与未改证明。未改玩家包、未补图。

### before / after 来源

- before：`9214`/`9215`，还原树对 `_视觉美化工作/基线清单.sha256` 中 `清明三门/中文版|英文版` **156/156**。桌面 zip 当时缺失，用基线 SHA + `rollback-css` + `rollback-img` 还原，口径可接受。
- after：`9216`/`9217`，当前 `中文版/` `英文版/`。
- 相机：默认 1280×800；06=320×640；08=390×844；20=1280×1400（对内 before/after 相同）。
- `D13-capture-log.json` 84 条：磁盘 PNG 的 SHA256 与字节 **全部与日志一致**；`html-game-puzzle` 与 `三户交班知识留存` 副本字节相同。无 32670/24449 连接拒绝体积。

目视：before 值班台为基线壳（满额主要靠绿底/灰底）；after 为现包（▣ 实框内条 vs ✕ 虚框斜纹删除线）。07 before 为浏览器默认蓝框 `1px auto`，after 为合同 `3px dotted #111`。引言 before 体积更大（基线伪 JPEG 静帧），after 更小（真 JPEG）。23/24 中英 before==after（结局页本轮无壳差），仍为运行页，不是错误页。

### 三态同屏

| 帧 | 中英 after 看见 |
|---|---|
| 17 已开一门 | 第1班 **1/3**；邻穴 ▣「本班已开 / open this shift」；其余未开未锁 |
| 18 满额三门 | 第1班 **3/3 已采信3句**；tousu/lie/song ▣；其余 ✕ 斜纹虚框 |
| 19 交班柜 | 第2班 **0/3 已采信3句**；三户「曾经见过 / seen before」，opened 清空 |
| 20 交班板 | 同档：见过三户名 + 三句 verified 仍在 |

03/10 after 与 18 同 SHA（同一次满额值班台），画面同时满足成功反馈与非唯色。允许。

### 通用 03 / 07 / 10

- **03** PRESENT：满额值班台，印位与门态相对空桌已变。
- **07** 真 Tab：日志 `Input` Tab×1，`hv:true`，焦点 `A.ghost-btn`「钟/Clock」；图上可见点线框。非 `element.focus()`。
- **10** PRESENT：▣/实框/内条 vs ✕/虚框/斜纹/删除线，不只靠颜色。

曾为拒绝页的 09/12/26/28 与英文 06/23/24/32 现为运行页（26 引言夜路+印位；英文 06 为 320 值班台）。搜词 13–16 已标 `N/A`+`na_reason`。

### 156 文件未改证明

评审独立对账（不依赖已删的 `/tmp/sanmen-d13/impl-pre.json`）：

- 基线清单 `清明三门/中文版|英文版` **156** 条；现盘玩家树 **156** 条，集合相同。
- **134/156** SHA256 与基线逐文件相同（含全部 HTML 与 `js/sanmen.js`）。
- **22** 处不同且仅限本局已审美化：中英 `game-extra.css` / `intro-manual.css` / `service-cyan-desk.css` + 八张 `.jpg`×2。
- `js/sanmen.js` 仍为 `98838108…a384` / `22f2ad3b…1ea4`，与 `js-hashes-before.txt` 一致。

结论：本轮只补证据链，未改玩法实现。

## D13 首评记录（已闭合，保留备查）

首评为何曾 HOLD：

技能口径：

- 开评后缺张：截图项仍 UNTESTED 或 N/A，**D13 与 review_status 写 HOLD**。
- 没有运行中游戏截图，任何维开评后最高 HOLD。本局并非零张运行图，但**合同主循环态**与**通用必拍格**仍缺。
- 截图项 status 只许 `UNSET | UNTESTED | N/A | PRESENT`，不创造截图 HOLD；缺张记 UNTESTED，维上 HOLD。

制作人 `截图清单.md` 自承：缺 07 焦点实拍，缺注入存档后的已开/满额/交班留存同屏；并写明早期 32KB 图是服务未起，**before 不能当视觉 before**。

评审核文件：

| 通用 nn | slug | 中文 after | 英文 after |
|---|---|---|---|
| 01 | boot-first-screen | PRESENT（运行） | PRESENT（运行） |
| 02 | core-verb | PRESENT 但是空桌 `0/3` 全「未开」，看不出主动词已发生 | PRESENT 同病 |
| 03 | success-feedback | **缺文件** | **缺文件** |
| 04 | near-fail | PRESENT 但是空四栏，与 21 字节相同，不是近失败结局 | 缺文件 |
| 05 | recovery | PRESENT（thin 可回柜） | 缺文件 |
| 06 | narrow-320 | PRESENT（空桌 320） | **ERR_CONNECTION_REFUSED（24449 B）** |
| 07 | keyboard-focus | **缺文件** | **缺文件** |
| 08 | touch-targets | PRESENT（空桌 390） | 缺文件 |
| 09 | muted-or-reduced-motion | **ERR_CONNECTION_REFUSED（32670 B）** | 缺文件 |
| 10 | non-color-state | **缺文件** | **缺文件** |
| 11 | empty-or-loading | PRESENT（空白板） | 缺文件 |
| 12 | error-or-pause | **ERR_CONNECTION_REFUSED（32670 B）** | 缺文件 |

另：`26-intro-no-search.png`、`28-clock.png`、英文 `23-ghost-nearfail.png` / `24-overbook-nearfail.png` / `32-skin-desk.png` 均为 32670 B 连接拒绝页。ZH 23/24/22 为运行中结局页，可作近失败/收口，**不能**填 已开/满额/交班留存。

路径：合同 `genre_profile=三户交班知识留存`，技能要求 `visual/{genre_profile}/{build_id}/{nn}-{slug}.png`。磁盘上 `visual/三户交班知识留存/polish-20260823-{zh,en}/` 与 `visual/html-game-puzzle/...` 字节相同副本，路径本身可接受；**内容仍缺上述帧**。

解密专属 ≥8：皮 31–40（中文）多数为运行页，可凑「异皮」张数；第 3 节搜词专属（命中另开页 / 未命中句子 / 禁止页 / 五套并排）未标 `N/A`+`na_reason`。本局标本是班次知识留存而非登山搜词，允许 N/A，但未标不等于已齐。专属里同样没有已开/满额/交班留存。

**与「现场已跑通」的关系：** 评审 http 抽查见下，三态在代码与值班台 DOM 上成立。D5/D6 可 PASS。D13 问的是矩阵是否交出运行截图。未补进 `visual/` 的现场观测不能把 D13 写成 PASS。

## 八张 `.jpg`

口径：玩家媒体 = 各语种 `img/` 八张。不含 `_视觉美化/`。

| 文件 | 魔数 | PIL | 像素 | 字节 | 中英逐字节 |
|---|---|---|---|---|---|
| ash-soil.jpg | FFD8 JPEG | JPEG RGB | 1536×1024 | 432462 | 一致 |
| board-empty.jpg | FFD8 JPEG | JPEG RGB | 1536×1024 | 67359 | 一致 |
| cabinets-three.jpg | FFD8 JPEG | JPEG RGB | 1536×1024 | 78972 | 一致 |
| offerings.jpg | FFD8 JPEG | JPEG RGB | 1536×1024 | 123404 | 一致 |
| old-ticket.jpg | FFD8 JPEG | JPEG RGB | 1536×1024 | 233725 | 一致 |
| path-night.jpg | FFD8 JPEG | JPEG RGB | 1536×1024 | 174753 | 一致 |
| stone-path.jpg | FFD8 JPEG | JPEG RGB | 1536×1024 | 243018 | 一致 |
| wall-clock.jpg | FFD8 JPEG | JPEG RGB | 1024×1024 | 49465 | 一致 |
| **合计** | | | | **1403158（1.338 MiB）** | 中英合计相同 |

- 真 JPEG：是（非 PNG 冒充）。http `Content-Type: image/jpeg`，引言四图 `naturalWidth` 与上表一致。
- 中英 `img/` SHA 一致。
- HTML 引用的八条路径中英均存在、无断链。
- 每语种媒体 ≤8MB：是。
- rollback-img：仅上述八文件名；魔数为 PNG（`89504e47`），即重编码前伪 JPEG 原件。**无 html/js/css/玩家包。** 与现包 JPEG 字节不同，符合回滚用途。

## 许可 / 回滚 / 合同 / 交卷 / 烟测 / before-after

- **许可表：** 有文件名（八图可视为一组同源）、来源、许可证、可否再分发、AI 栏。原图生成链 UNSET，本局未新出图。够本维 PASS，不升 D12 HOLD。
- **rollback-img：** 明确排除玩家包（目录内 0 个 html/js/css）。
- **rollback-css：** 中英 `game-extra.css` / `intro-manual.css` / `service-cyan-desk.css` 与现包哈希不同，回滚点存在。
- **合同：** 必填字段大体齐；`contract_status=CANDIDATE`。CANDIDATE 前置要求通用 12 齐或每张 N/A+理由——**实际不满足**，开评后以 D13 HOLD 处理，不追改制作人字段。
- **交卷：** 改动范围声明为中英 img×8 + 三份 CSS + `_视觉美化/` 文档；声明未改 `js/sanmen.js`、门 id、radio value、KEY/MAX/VERIFY_DOOR/openDoor/handover/submitNote、seen/verified。与抽查一致。
- **烟测 7 步（制作人）：** 320/390 有图但均为空桌；键盘未拍；09 为拒绝页；满额/已开缺注入图。评审补跑 Tab 与 320/390 几何（D9/D11 PASS），**不填补 D13**。
- **before/after：** 01/02/04/06/08/11/26 的 `*-before.png` 为连接拒绝页。制作人已书面作废。技能要求同相机同输入态成对——**本局无合格 before 对。** 记入 D13 HOLD，不另开 largest_gap。

## http 抽查（9114 / 9115）

Chrome headless CDP，非陌生人盲测。中英对称，结果同构。

| 项 | 中文 9114 | 英文 9115 |
|---|---|---|
| KEY | `qingming-sanmen-v1` | `qingming-sanmen-v1-en` |
| 三户上限 | 开 tousu/lie/song 后第四户 xiao → `reason:full`；印位「已开 3/3」 | `opened 3/3`，第四户 full |
| 已开 | 门文案「本班已开」，class `used`，`::before "▣"`，3px 实框 + 内条 | `open this shift` + 同套形状 |
| 满额未开 | 其余门 class `locked`，「未开」，`::before "✕"`，虚框 + 删除线 + 45° 斜纹 | 同 |
| 采信 | 开户后 `verify('ash-neighbor'|'code-martyr'|'deliver-12')` ok；`cross-hexiao` 因 xiao 未开 → `why:closed` | 同 |
| 交班 | `handover`：`opened=[]`，`round` 1→2，`seen`/`verified` 原样。值班台「第2班　已开0/3　已采信3句」，门「曾经见过」无 used/locked。白板仍列出三句采信 | 同（`seen before`，3 sentences kept） |
| ending-note | `submitNote(cross,martyr,plot12,note)` 在四句 verified 后 → `note`；页题「备注已收下」 | `Remark accepted` |
| ghost 近失败 | `nature=ghost` → `ghost`；页「把单子甩回来」 | `threw the form back` |
| overbook 近失败 | `act=change` → `overbook`；页「越权」 | `Overreach` |
| 旧档 | 只写 `{round,opened,seen,verified}` 无 thin/ending，`load()` 可读；值班台「寒食旧单　本班已开」+ 邻穴「曾经见过」 | 同 |
| 320/390 44px | 主钮/门链/备注 label 高度 ≥44，below44=[] | 同（英文 tab 在 320 会折行，高度仍 ≥44） |
| 真实 focus | Tab 12 步 `matches(':focus-visible')===true`，3px 点线 #111 | 中文值班台已证；英文同源 CSS |
| 非唯色 | 已开/满额靠框、纹、▣/✕、字重、删除线，不只靠色 | 同 |
| radio value | nature `cross/ghost/private`；codeTo `martyr/plot12/none`；goodsTo `plot12/martyr/none`；act `note/change/pass` | 同 value，仅标签翻译 |
| 门 id | tousu, qu12, lie, song, xiao, kao, han, shuo | 同 |

门页 `fullbox`：满额时点击开户，内联脚本先 `fullbox=block` 再 `show()` 把它设回 `none`，门页满额文案**不会留在屏上**。这是原 `door-*.html` 内联逻辑，不在制作人声明的改动文件里，**不是本条 largest_gap**。满额可读性以值班台 `locked` 行为准（已测）。

## sanmen.js 核对

`_视觉美化/js-hashes-before.txt` 与现文件一致：

- 中文 `988381081bf64a808771c237860f035a51060587f15017fbd911342b72a0a384`
- 英文 `22f2ad3b97312747f222a4679b3c3f44c6f846b1a6f61e8e502a32e046391ea4`

交卷所写桌面 zip 在评审时未找到，哈希以制作人基线文件 + 现盘字节为准。

未改字段：`KEY`、`MAX=3`、`VERIFY_DOOR` 八对、`empty()` 含 `seen`/`verified`、`openDoor`（满则 `full`，已开则 `already`）、`handover`（只清 `opened`、升 `round`）、`submitNote`（ghost / overbook / note / thin）、`load()` 缺省补 `seen`/`verified`。门页 `var id="..."` 与 radio `value` 未改。

## 截图项 status（D13 复核后）

只使用规定四值。

中英通用 01–12：均为 PRESENT（运行页）。02/17 已开一门；03/10/18 满额三门同屏；07 真 Tab。04 近失败入口仍偏空表，近失败结局由 23/24 承担。

搜词专属 13–16：N/A（本局非登山搜词，`na_reason` 已写）。

合同三态 17–20：PRESENT。

## did_not_prove

- 真机手指、色觉模拟、帧率秒表、首包可交互秒表
- 只键盘走完正确四栏通关（只证到值班台 Tab 焦点）
- 陌生人盲测
- 原八张静帧的完整 AI/库存生成链（许可表 UNSET）
- 交卷所述桌面备份 zip 现场未找到
- `/tmp/sanmen-d13/impl-pre.json` 拍摄机已删；156 未改以基线清单独立对账代替

## 附记（禁止当作第二条返工）

`game-extra.css` 把见过/采信双边栏写在 `.sm-board #seenbox|#verbox`。`introduction.html` 有 `.sm-board`，引言空板样式成立。`board.html` / `note.html` 的盒子在 `.notice` 下，计算样式左框为 0——**玩的时候那块板没有合同写的双栏形。** 20 交班板已拍出：见过/采信靠标题与句子可分，双边栏形仍未挂到 `board.html`。不升为本轮 largest_gap（已无）。下局若单开可读性维再点。

## 服务

首评 `9114`/`9115` 与 D13 返工 `9214`–`9217` 均已停。玩家包未被本评审修改。
