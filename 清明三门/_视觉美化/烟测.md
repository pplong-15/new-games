# 烟测 7 步　清明三门　制作人

环境：本机 Python http.server 127.0.0.1:9014（中）/9015（英），Chrome headless --no-sandbox。不是陌生人盲测。

1. 缩到 320：`06-narrow-320.png` / `27-notes-320.png`。门链与交班钮仍在视口可点（CSS min-height 44、窄屏 100% 宽）。真机手指未测。
2. 只键盘：规则写了 `:focus-visible` 3px 点线。未拍 Tab 走完交备注。
3. 只触控：390 `08-touch-targets.png`。模拟视口，非真机。
4. 关声：无音频。N/A。
5. 减弱动效：`prefers-reduced-motion` 下 animation/transition none。本局无循环动画。
6. 状态不唯色：已开 inset 条+▣；满额斜纹虚框+✕；采信实栏 vs 见过双线斜纹纸；备注勾选实边内条。满额/已开运行态缺注入存档的同屏图。
7. 无整屏频闪：无循环动画。

存档：键仍 `qingming-sanmen-v1` / `qingming-sanmen-v1-en`。制作人未改 js。旧档字段 round/opened/seen/verified 仍由同一 load() 读取。

控件：门 id、radio value（cross/ghost/private、martyr/plot12/none、note/change/pass）dump-dom 仍在。Sanmen.openDoor / handover / verify / submitNote 未改。
