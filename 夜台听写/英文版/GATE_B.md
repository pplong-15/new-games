# 夜台听写　闸 B（开发验收）

验收时点：可玩静态页齐，入口 `introduction.html`。

## 项

### DAG 无环　PASS
开局桌面与时间轴八段均可点。收藏页从图标进入，不靠搜词开门。借带/粮通知为可选。标注单无前置锁。四建议都可交。锁频不是主线门。

### 点时段可读听写　PASS
TunerLog 绿条八段各开一页，听写在黑底框。不是片库检索，没有「搜一个词亮五条」。

### 交叉页可点　PASS
礼簿四页、讣栏两篇、村栏三张、医院打卡与留言、槐记落款与铁丝、班线时刻与字条、带柜两篇，HTTP 抽链全部落到现存文件。坏链 0。

### 标注可提交　PASS
三项表单可勾。判定优先级 true → erfang → tape → hold。Node 抽 `grade()` 六组断言通过。刷新后 `yt_end` 仍显示已交。作废重开清本机键。

### 皮肤合同　PASS
十一所有者十一皮。班前纸无时间轴。win7 补滚动。页脚各站自写。

### 语法 / 载荷　PASS
`js/seen.js` `node --check` 通过。屏幕未见主线/搜词/通关/本游戏。权威恒为建议。

### 体量　PASS
dupcheck：html 42，hanzi 10033，dup40 0，trip24 0。imgcheck：11 unique_md5。

## 结论

**PASS**，可进试玩。未在 Cursor 内嵌浏览器用键鼠走完四结局画面（浏览 MCP 本环境不可用），该项交给 PLAYTEST 标未覆盖，不在本闸假装点过。
