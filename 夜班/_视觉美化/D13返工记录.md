# 夜班 · D13 返工记录 · 2026-08-23

制作人只修 `largest_gap D13`。未改中英 `夜班.html`、工作台 CSS/JS/媒体、玩法、存档键、`licenses.md`、D12。`contract_status` 仍为 `CANDIDATE`。`独立评审.md` 的 `review_status` 未改。

## 方法

- HTTP：`http://127.0.0.1:9216/夜班.html`（中）与 `http://127.0.0.1:9217/夜班.html`（英）。无 `?debug`。无 iframe。
- 入口从标题「去上夜班 / Take the night shift」开始。只点运行时渲染的选项、进货 `+/−`、开始值班、短信「知道了 / Got it」、规/收好、结算「先睡到傍晚 / Sleep till evening」「看看后来 / See afterward」。
- 打字用空格点按 + 点 `#line` 跳过（公开「点击跳过 / Tap to skip」）。未按 debug「跳过本晚 / 直接天亮」。未调 `__ns.finish` / `getState` / `jumpNight`。未用 Runtime 写 flags/night/结局。
- 读档仅 `localStorage.getItem` 只读，用于记下 `night` 与 flags。
- 每帧断言：URL 不含 `debug`；`#debug` 不可见；`.choice.warn` 数量为 0。
- 旧 after 帧（含跳过钮 / 伪 warn / `__ns.finish`）已移入 `_视觉美化/rejected-evidence/d13-debug-contaminated/`，不计矩阵。`*-before.png` 保留未动。

## 通关结果（各 1 次成功，未 HOLD）

| | 中文 | 英文 |
|---|---|---|
| 结局标题（DOM `#ending h2`） | **正常下班** | **Clock out** |
| 结局 id（由公开七晚 + `pickEnding` 自然走到） | dawn | dawn |
| 通关后 `hengdeng-cleared` / `-en` | 1 | 1 |
| 存档 night 逐晚 | 1→2→3→4→5→6→7 | 同 |
| 第7晚结算 flags | `leaveAtDawn` `walkOut` `refusedJoss` `gotFlashlight` `powerOut` `lookedWet` | 同构 |
| 未走 | `soldJoss` `helpedBai` `stayOn` `closedLast` `catTakesShop` `__ns.finish` | 同 |

`pickEnding`：纸钱拒收、不送小雅过水线、第7晚放钥匙+走到门口；电费连欠但收下吴保安手电 → 仍是 dawn，不是 fired。

## 逐晚可见状态（中文；英文同构）

存档键 `hengdeng-nightshift-v2` / `hengdeng-nightshift-v2-en`，结算屏读取。

| 晚 | HUD 结算 | save night | 神智 | 现金 | 店誉 | 要点 flags |
|---|---|---|---|---|---|---|
| 1 | 天亮结算 · 第1晚 | 1 | 85 | 65 | 61 | lookedWet, servedLin, wangRegular, foundRules, powerMet |
| 2 | 第2晚 | 2 | 89 | 76 | 69 | baiDenied, refusedIncense, threwIncense |
| 3 | 第3晚 | 3 | 72 | 84 | 75 | refusedJoss, gotFlashlight, wuStay |
| 4 | 第4晚 | 4 | 62 | 92 | 76 | blockedWet, powerOut, refusedAsh |
| 5 | 第5晚 | 5 | 57 | 106 | 84 | gotPeachFruit, zhouLeft；（中文此局暴雨卖了两桶面） |
| 6 | 第6晚 | 6 | 39 | 106 | 84 | refusedTwice, knockedWu, sawLedger |
| 7 | 第7晚 | 7 | 24 | 0 | 87 | leaveAtDawn, walkOut, leftCash；点「看看后来」出结局卡 |

第7晚点「看看后来」后 `finish(pickEnding)` 清掉当日 SAVE（游戏原逻辑），故结局后 `save_after=null`。这是公开 UI 闭环，不是内部 API。

## 点击序列（中文公开控件）

1. 去上夜班 → 进货（牛奶/面包/烟/茶/面/电池各 +1）→ 开始值班 → 知道了
2. 规（省略）收好 → 卖给她（牛奶+面包）→ 卖给他（烟+冰红茶）→ 展开纸条 → 规（全文）收好 → 卖方便面
3. 湿发：多看一眼 → **还是卖给他**（近失败）→ 请他喝瓶水 → 把灯调亮，等天亮 → 先睡到傍晚
4. 晚2 进货 → 老样子 → 说没有草莓味 → 卖水+烟 → 不卖香 → 扔掉断掉的那根 → 送她到门口
5. 晚3 进货 → 让她早点回去睡 → 给她矿泉水 → 卖水+烟 → **不卖。请把钱收回去** → 规（墨色+铅笔）→ 不去数 → 请他天亮再送外卖 → 记下，收下他的备用手电
6. 晚4 → 不让。后间不对外开放 → 擦柜台 → 不热。请她拿走 → 说那是反光 → 卖给他水和烟
7. 晚5 → 收下桃子 → 祝她一路顺利 → （暴雨则卖两桶方便面）→ 卖烟+冰红茶 → 坐着等
8. 晚6 → 把账本合上 → 把纸条按回去 → 仍然不卖 → 短信知道了 → 不开。敲三下玻璃叫保安
9. 晚7 → ……不说话 → 五条原文都留下 → 把钱留给下一班 → 把钥匙放下，准备天亮走 → 说还在，快天亮了 → **走到门口，等天亮** → 看看后来 → 返回标题

英文同一条路径，标签见 `d13-play-log.json`。

## 每张 after：URL / 输入 / 画面变化

全部 URL：`http://127.0.0.1:9216/夜班.html` 或 `…9217/夜班.html`，**无 query、无 debug**。

| 文件 | 输入 | 变化 |
|---|---|---|
| 01-boot-title.png | 清档打开 390 标题 | 夜班/Night Shift + 去上夜班；身份仅夜班员可选；无跳过钮 |
| 01-boot-title-320.png | 同页缩到 320 | 主钮仍可点；before 成对保留 |
| 11-empty-or-loading.png | 无存档标题 | 结局图鉴未通关故隐藏；空态不是白屏 |
| 12-error-or-pause.png | 点锁定「猫视角 / Cat View」 | 锁态文案/toast：通关后才能用；可恢复（仍在标题） |
| 17-group-submit-prep.png | 新档进到第1晚进货 | 「第1晚 · 进货 / Night 1 · Stock」+ 开始值班 |
| 02-core-verb.png | 值班后林阿姨选项出现 | 售卖选项同屏，主动词进行中 |
| 08-touch-targets.png | 同机位 390 | 选项与货/规可点 |
| 13-person-job-rule.png | 同屏 | 林阿姨 + 熟客/Regular + 规钮 |
| 20-shift-flags-hud.png | 同屏 | 时间·第1晚·神智，无 XP |
| 07-keyboard-focus.png | Tab 落到选项 | 第一项可见焦点描边 |
| 09-muted.png | 点静音控件 | 中文钮面「静」；英文「✕」 |
| 06-narrow-320-in-shift.png | 值班中 320 | 选项仍在 |
| 19-night1-rules.png | 展开纸条前点「规」 | 五条「……」省略；「还没来得及看完」 |
| 03-success-slip.png | 点卖给林阿姨 | 小票：牛奶+面包 收了 ¥11 |
| 14-rules-listed.png | 展开纸条后点规 | ≥4 条全文（第1晚真实 foundRules） |
| 18-near-answer-text.png | 同一张规 | 正文可回指灯/纸钱 |
| 10-non-color-state.png | 第1晚规页 | 编号+文字，不唯色 |
| 04-near-fail-options.png | 湿发男人选项出现 | 卖电池 / 不卖 / 多看一眼；**无 .choice.warn**（矩阵外辅助） |
| 04-near-fail.png | 点「多看一眼」再点「还是卖给他 / Sell to him anyway」 | then：「好看吗」+干纸币；小票电池 ¥12；仍第1晚 |
| 05-recover.png | 继续到吴保安 | 仍在值班，三选项可走 |
| 15-ink-fresh-pencil.png | 第3晚真实点规 | 今晚异常：第二条墨未吃进纸；铅笔「纸钱不算」 |
| 16-fake-mutex-qa.png | 同第3晚规页 | **N/A**：未把同一段改成两个标题；诚实第3晚帧 |
| 21-clerk-ending-dawn.png | 七晚后点「看看后来」 | 结局卡 dawn；已收集 1/14；无跳过钮 |
| 22-role-line.png | 返回标题 | 图鉴亮「正常下班 / Clock out」；他身份仍锁；猫视角因通关解锁。未写 `unlockedRoles` |

## 近失败

不是 `classList.add('warn')`。运行时选项只有 `choice`。危险选择是湿发「还是卖给他」，反馈是 then 文案 + 小票，flags `lookedWet`。之后吴保安仍在班（05）。

## 拒绝计入矩阵的旧帧

`rejected-evidence/d13-debug-contaminated/20260823-{zh,en}/` 各 23 张旧 after（01–22 与 320 after）。含 debug 跳过钮、伪 warn、或 `__ns.finish('dawn')`。

## 未做

- 未改 D12 许可 UNSET
- 未假互斥换标题 QA（16 仍 N/A）
- 真机触控 / 色觉 / 首包秒表仍未测
- 制作人不得改 `review_status`
