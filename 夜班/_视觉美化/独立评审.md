# 夜班·恒灯便利　独立评审　2026-08-23

- 角色：独立评审人（未画本局主界面 / 主图集 / 主 SVG；未改 HTML / 证据 / ref）
- 制作人合同：`_视觉美化/视觉合同.md`　`contract_status=CANDIDATE`
- 模型：Grok 4.6 最高档
- **review_status: PASS**
- **largest_gap: 无**
- D13 复评：2026-08-23 晚（只复核 D13 返工；未改实现、未补证据）
- D12 复评：同晚按逐项规则重判（见下）

`reviewer_role ≠ producer_role`。D11 烟测已跑。非 VP5。总评 **PASS**。

---

## D13 复评（本轮唯一复核项）

制作人只修 D13：无 `?debug` 的 9216/9217 http 重拍；旧 after 迁入 `rejected-evidence/d13-debug-contaminated/`。`夜班.html` / CSS / 媒体 / `licenses.md` 未改。

| 检查 | 结果 |
|---|---|
| 入口 URL | 日志与每帧记录均为 `http://127.0.0.1:9216/夜班.html` / `9217/夜班.html`，**无 query、无 debug** |
| 公开 UI 七晚 | 中英各 1 次 OK。点标题「去上夜班 / Take the night shift」→ 进货 `+/−` → 开始值班 → 真实选项 → 结算「先睡到傍晚 / Sleep till evening」→ 第7晚「看看后来 / See afterward」 |
| night 1→7 | 结算读档 `save_night` 1…7。HUD 第1晚…第7晚 / Night 1…7。22 帧通关后标题出现第1–7晚重开钮（游戏原逻辑，不是 debug） |
| 未用跳晚 / `__ns.finish` / Runtime 改结局 | 脚本禁 URL debug、禁 `#debug` 可见、禁 `.choice.warn`。`evaluate` 仅只读 `localStorage` 与滚动复位。无 `jumpNight` / `getState` 写入 / `__ns.finish`。`#nextNight` 是公开结算钮 |
| 结局 | DOM `#ending h2`：**正常下班** / **Clock out**；`cleared=1`；文案走 `leaveAtDawn` 支（钥匙放下）。21 帧无跳过钮 |
| 近失败 | 湿发「多看一眼」→「还是卖给他 / Sell to him anyway」。源码 `afterLook.then`：「好看吗」+干纸币 / `Like the view?` + dry bill；小票电池 ¥12。`lookedWet`。**无临时 class**。04-options 三选项无叹号虚线。05 吴保安仍在班 |
| 旧污染隔离 | rejected 中英各 23 张旧 after（含伪 warn / finish / 跳过钮）。矩阵当前帧与 rejected **分目录** |
| 当前矩阵画面 | 抽看 01/02/04-options/04/21/22 中英：无「跳过本晚 / 直接天亮 / Skip this night / Skip to dawn」 |

D13 **PASS**。通用 12 + 怪谈 13–20 齐（16 N/A 假互斥未做，理由成立）。路径 `visual/html-game-rule-horror/20260823-{zh\|en}/`。before 仍仅标题 390/320，不挡本维。

---

## D12 逐项（本轮按同批规则重判）

「原来就在包里」**不自动 PASS**。逐项：

| 行 | 判定 | 理由 |
|---|---|---|
| 中英 `夜班.html` 逻辑/文案/工作台 CSS | 可随包 | 本局壳，非第三方图 |
| 雨/雷 AAC | 可随包（按注释） | Mixkit License 源码注释 |
| 系统字体栈 | N/A | 不随包发字形 |
| 营销图 ref | N/A（不进玩家包） | SHA 与基线 MATCH，HTML 零引用 |
| 店景 JPEG / 柜台 WebP / 人物·猫 WebP | **UNSET 来源链** | 表已登记文件名/来源未核/许可证 UNSET/AI UNSET。无商标、无官方切图、无品牌字、本局无新 AI。同批（清明三门 / 夜台听写 / 门坎试法）：诚实 UNSET + 无商标主视觉 + 本局未新出图 → **本维 PASS**，来源链进 `did_not_prove`，不升 HOLD |

D12 **PASS**。发行渠道若要可再分发证明，UNSET 图仍须另取授权；那是渠道闸，不是本维 largest_gap。

---

## 抽查运行（首评仍有效）

首评 9116/9117 + 进程内 Chrome：默认无 debug；进货/林阿姨/第1晚省略规/320≥44/Tab 3px/音→静/减动雨停。`?debug=1` 才会出跳过钮。该抽查证明 **玩家默认壳干净**；旧矩阵污染已隔离，不再计入。

体积 / 单文件 / 远程 / 营销 SHA / 玩法锁：见首评，本轮 HTML 未改。

---

## 合同 / 12+8 / 烟测

- 合同齐。`genre=html-game-rule-horror`，VP1，2D。制作人未写 PASS。
- 当前 after：中英各 26 张（含 01-320、04-options 辅助）。污染旧 after 不计矩阵。
- 14/15/18 现为第1晚展开纸条 / 第3晚真实点规，不是 `getState()`。
- `smoke.json` 7 步仍成立。

---

## D1–D14（正式）

| 维 | 判定 | 依据 |
|---|---|---|
| D1 | **PASS** | 标题 3 秒可读夜班柜台值班 |
| D2 | **PASS** | 店景/选项主操作，HUD 状态，纸在柜台层 |
| D3 | **PASS** | 正视 2D、油纸、夜黑+灯管一致 |
| D4 | **PASS** | 18 枚内嵌 URI；无灰盒冒充主循环 |
| D5 | **PASS** | 空标题、锁猫 toast、进货、规省略、结局卡可分 |
| D6 | **PASS** | 卖出有小票。近失败改为真实湿发 then+小票，不是正解、不死档。warn CSS 仍是死规则，不再当证据 |
| D7 | **PASS** | 三锚+材质；强调色绑后添墨；非唯色纸张 |
| D8 | **PASS** | 2D |
| D9 | **PASS** | 320/390 可点，≥44 |
| D10 | **PASS** | 媒体 0.90MB、单文件 ~1.49MB、无远程；秒表未测 → did_not_prove |
| D11 | **PASS** | 7 步烟测+抽查 |
| D12 | **PASS** | 逐项见上 |
| D13 | **PASS** | 本轮复评 |
| D14 | **PASS** | VP1；无本局 AI 出图 |

---

## did_not_prove

- 真机触控 / 色觉模拟 / 运行帧率 / 首包秒表
- 备份 zip 二次开包（说明路径当时不在桌面）
- 假互斥「同一段换标题」QA（16 N/A）
- 店景/人物完整库存或拍摄授权链（许可表 UNSET）
- 评审未亲手打完七晚；以制作人 `d13-play-log.json` + 脚本只读约束 + 源码 then 对照 + 当前帧目视核验

---

## 首评记录（已关闭，2026-08-23 下午）

当时 **HOLD / largest_gap D13**：after 带 debug 跳过钮；04 临时 `.choice.warn`；21 用 `__ns.finish('dawn')`。D12 曾写成 HOLD（UNSET 自动升格）。本轮 D13 返工关闭伪证；D12 改逐项，不再因「来源未核」单独 HOLD。

旧污染帧只存在于 `rejected-evidence/d13-debug-contaminated/`，不作通过证据。

---

## 回传

- **状态：PASS**
- **证据：** `visual/html-game-rule-horror/20260823-{zh,en}/`（当前 after）；`rejected-evidence/d13-debug-contaminated/`（隔离旧帧）；`screenshots.md`；`D13返工记录.md`；`d13-play-log.json`；`playtest-run.json`；`licenses.md`
- **报告路径：** `/Users/jianglong/Desktop/游戏美化/夜班/_视觉美化/独立评审.md`
