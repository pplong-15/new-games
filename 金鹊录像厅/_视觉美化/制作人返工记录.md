# 金鹊录像厅　制作人返工记录　20260823-d9　D11

contract_status: CANDIDATE（未升 PASS）
review_status: 不改。独立评审文件仍 REWORK；制作人未填评审结论。

largest_gap 本轮只修：D11 交卷 `07-keyboard-focus` 不是「只键盘走到搜框」的运行帧（曾用 CDP 强制 `:focus-visible`）。

## 未改

游戏 HTML / CSS / JS / 图片、玩法、存档键、既有 01–06 与 08–35 截图（含全部 `-before.png`、含 `02-core-verb.png`）一律未动。未强制伪类、未脚本加 class、未后期绘制焦点。

## 做了什么

沿用已有 `http://127.0.0.1:8928/`（中文版根）与 `http://127.0.0.1:8929/`（英文版根）。Chrome headless CDP，profile `/tmp/jinque-d11-tab-profile`，viewport 1280×900、DPR 1。首页正常加载（`localStorage.clear` 后打开 `index.html`，`readyState=complete`），再用 CDP `Input.dispatchKeyEvent` 发送真实 Tab（`rawKeyDown` + `keyUp`，键码 9），按文档顺序走焦：

| 语 | Tab× | 落点 | 计算样式 | `.dead` 包厢 |
|---|---|---|---|---|
| 中 | 2 | `#search-input` | `outline: rgb(26, 26, 26) solid 3px`；`:focus-visible` 匹配 | 不是 `activeElement`；虚线仍是常驻禁用形 |
| 英 | 2 | `#search-input` | 同上 | 同上 |

Tab 1 均为店名链（金鹊录像厅 / Jinque Video Hall），Tab 2 为搜框。截完终止 Chrome，删除 `/tmp` profile。

重拍（仅这两张 after）：

- `visual/停更录像厅搜词调查/20260823-d9-zh/07-keyboard-focus.png`
- `visual/停更录像厅搜词调查/20260823-d9-en/07-keyboard-focus.png`

与同构图 `02-core-verb.png` 并排：可见差分（通道差 ≥20）bbox 中文 `(966,6)–(1197,61)`、英文 `(950,6)–(1181,61)`，均在搜框外圈；导航/包厢区相对 02 无可见差。

## 交卷栏

- `screenshots.md` 07 行改为真实 Tab×2；运行口改为 8928/8929。
- `contract_status` 仍 CANDIDATE。
- `review_status` 不改。

制作人不得写 D11 PASS。本文件只证明 07 已换成键盘走到搜框的运行帧。
