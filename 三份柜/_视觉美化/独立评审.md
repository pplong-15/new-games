# 独立评审　三份柜 polish-20260824

reviewer_role: 独立评审人（未参与本局 css / 装饰 html / 主图集）
producer_role: 制作人机器人（改 css/sanfen.css 与装饰 class 的人）
review_status: **PASS**
contract_status（制作人交卷）: CANDIDATE（制作人未写 PASS；CANDIDATE ≠ 通过，只表示可开评）
genre / pipeline: html-game-puzzle / VP1 / 2D / distribution_form A
http 抽查: `http://127.0.0.1:8840/index.html`（中文，目录 `中文版/`）与 `http://127.0.0.1:8841/index.html`（英文，目录 `英文版/`）。8840/8841 已在听，未改用 8940/8941。页面无 iframe（磁盘 `index.html` 与运行 DOM 均为 0）。

largest_gap: 无

---

## 本轮只核上轮 largest_gap（D13）

上轮总评 REWORK，缺口仅 D13：07 不是进站层 Tab 焦点；09 与 19 同字节。制作人声称只重拍 07/09（含 before）。本评审不采自证，按下表复核。

| 检查 | 结果 |
|---|---|
| 07 是否进站层 `#btn-enter` 金线 | **是。** 中英 after 均为 1280×800 视口（非 full_page）。画面是 `#title-layer` 未进站：静帧 + 虚线/夹钉形差 + 红钮「进站 / Clock in」。钮外圈为 3px 金线，不是值班台 `.door.lit`。07-before 同层、钮上无该金线（金像素来自右侧夹钉盒，不是按钮 outline）。 |
| 运行时 Tab | 8840/8841 清档后第一次 Tab 即 `document.activeElement.id === "btn-enter"`；`outline: rgb(214, 181, 106) solid 3px`（即 `--gold: #d6b56a`）；`#title-layer` 仍盖满视口。 |
| 09 sha256 ≠ 19 | **成立。** 中文 09 `450030f15aaea4d223cf0fdfe96e0077627b4e737c4f297968c2bbcc6e5f33b5` ≠ 19 `78160669a3e93e658780709e8263ee50ad99b8410b6e2b5e23b80cb479fff1bb`。英文 09 `6cc7cd0303447c18c160130a7a9b258d1c54135b894b4426d24ae2b994d703ea` ≠ 19 `a2e742d2ded1ee18b7a7fb64bef413b140c66c71213c8a4bf3f1502dcc1f258c`。09 画面是方笺写入后右栏钉条（`Rx 今日坐堂方` / sitting-hall prescription +「坐堂名与备案名同一栏」）；19 仍是账第一层空账页。 |
| 锁：js MATCH | 中英 `js/app.js` `js/engine.js` `js/data.js` 相对 `_视觉美化/rollback/before.sha256` **六文件 MATCH**（见下表）。 |
| STORE_KEY | 中文 `sanfen-cabinet-state`；英文 `sanfen-cabinet-state-en`。进站后 localStorage 只写对应键。 |
| 制作人自报 D4 木牌 | 00-open.jpg 墙上「温馨提示」仍在。两栏不在 jpeg 里。不升为最大阻断（见 D4）。 |

`js` SHA256（与 rollback 一致）：

| 文件 | sha256 |
|---|---|
| 中文版/js/app.js | `e65da71bae8b49d5dd08d5994bf9e37ade02d5394af17c34ec2bfc020425df1a` |
| 中文版/js/engine.js | `a3d8f99b5b333fc5410164c1b7b227d5e8b886cc90b6432d5680dd8778110d19` |
| 中文版/js/data.js | `bfe0bceb404aca839d7ef4a5785d366aae4b76b71212517bec14b6087fbda3d8` |
| 英文版/js/app.js | `5cf2d564a6b5987a245d55422b24cdcb1d59d0a9091ddd54ff99a7841de4ef54` |
| 英文版/js/engine.js | `e24aaa2c1f91c9d04d9534994e63cdc1d4369416fe18935977e65f3091653200` |
| 英文版/js/data.js | `e414cc5c8a6045e8cb8c54d43faf655c423a9fe4ad2d432797dc42bac00e87c3` |

---

## 双份交卷

`_视觉美化/制作人交卷.md` 与根目录 `制作人交卷.md` **逐字节相同**，无冲突。`_视觉美化/合同.md` 只指向 `视觉合同.md`，不另立字段。

制作人自报 largest_gap 曾为 D1（进站静帧占屏）或「无」。**本评审不照抄。** 见 D1。`视觉合同.md` 截图清单里 07 仍写「焦点在交班」——与现拍 07 不符，属合同正文过期，不单独升 D13（以矩阵 PNG + `截图说明.md` + 运行核对为准）。

---

## 基线完整性（JS / HTML / 体积 / 许可）

| 检查 | 结果 |
|---|---|
| 六 JS vs rollback | **MATCH** |
| `index.html` vs rollback | 不止装饰 class：进站层 `.title-split`（aria-hidden）；`#desk` / 两栏加 `duty-board` `tray-loose` `tray-pinned`。未改按钮 id、未改脚本钩子。记录，不升最大差距。 |
| 中英 `css/sanfen.css` | 本局改动（预期）；中英 CSS **现已同哈希** `56ba6e76010e498d8a1f7f336b717c26ba731a32f9e0016d572bc1a79d9ca372` |
| jpeg vs rollback | `01/04/05/06` 一致；`00/02/03/07/08/09/10` **七张 SHA 已变**。许可表写「本局不改 / 未重生成」不准确。未见商标、国徽、品牌字。MIT `LICENSE.txt` 仍覆盖整包。不升 HOLD，写入 D12 备注。 |
| 中文整包 | 1 186 206 B（约 1.13 MB）；jpeg 1 119 289 B；css 16 587 B |
| 英文整包 | 1 194 043 B（约 1.14 MB） |
| VP1 媒体预算锚 | jpeg ≪ 8 MB |
| 许可表 | 非空；MIT；系统栈不嵌入；本局无新远程字体 |

---

## 截图矩阵（提交包）

路径合规：`visual/html-game-puzzle/polish-20260824-zh|en/{nn}-{slug}.png` 与 `-before.png`。

- 通用 01–12 after/before：**文件齐**。
- 解密专属 13–20 after/before：**文件齐**（另有 21–24 与 extras）。
- before/after 成对存在。大量为 `full_page`（如 01 中文 after 1280×993），进站层真实视口是 `#title-layer` 盖满 800px；D1 不以全页拼图当第一屏。
- 可接受的同帧兼用：02=14=17（方笺+#desk）；03=10（写入后两栏形差）；04=05（近失败后栏未毁）；15=20（死搜即挂号残页）；12≈21（拒条）。
- 上轮不可接受项本轮已改：07 现为进站层 Tab 金线；09≠19。

---

## http 烟测（评审人，8840/8841，视口实测）

未把评审截图写入游戏目录。

| 步 | 结果 |
|---|---|
| 1. 320 主钮可点 | 进站后交班/交条/批准高 44；门热区 49×71 ≥44；两栏上下叠、不重叠。320 宽可点 `#door-fang` 进 `fang-today`。390 进站钮 92×48。 |
| 2. 键盘到进站 | 第一次 Tab 即 `#btn-enter`；`outline: rgb(214, 181, 106) solid 3px`；进站层仍在。 |
| 3. 390 触控目标 | 主钮高 44；门 60×88。未真机手指。 |
| 4. 关声 | 无音频，N/A。 |
| 5. 减动后写入 | `prefers-reduced-motion: reduce` 下写入，右栏 `.chip.ver` 仍出现；钉文「坐堂名与备案名同一栏」。 |
| 6. 状态不唯色 | 打开过：2px dashed + 斜纹；已证实：3px double + 顶夹；折角条 vs 钉形 chip。 |
| 7. 无整屏 >3 次/秒闪 | 无循环动画。 |

其它抽查：

- 点方 → `data-skin=fang` 顶栏 `rgb(122, 46, 36)` / 宋 / 纸 max-width 640。
- 写入 `claim-same-column` → `#msg`「已写入：坐堂名与备案名同一栏」，工钱可见，右栏钉条。
- 批准进销存 → `#view` 拒条「临聘不能批…」非模态（值班台仍在）；无 `dialog`/`[role=dialog]`。
- 死后搜 `zzzz-no-hit` → `search-closed` / 夜间检索关停句；皮为 book，不是新结果页。
- 英文 8841：Clock in 金线；减动写入 pin「Clinic name and filing name share one field」；Night search 关停句。
- 首包可交互：本机约 0.09 s（发布线目标 ≤3 s；未作冷缓存/真机）。

走正确留条：矩阵 24「留下的门」、¥36、三钉三勾（JS 未改，逻辑与基线同一 SHA）。本评审未再打完整 ending A 操作链，收口以 24 运行图 + JS 冻结为证。

---

## D1–D14 正式判定

开评后不得留 UNTESTED。

### D1 首帧类型识别　**PASS**

不采用制作人「静帧仍压过选份钉条还柜」的自报。

依据：8840 上 **1280×800 进站层**（不是矩阵 01 的全页拼图）。三秒内不靠读长句可以指认：墙上三门（方/柜/账）= 选哪份；虚线纸篓 vs 夹钉托盘 = 打开过 ≠ 已证实；左托盘写交班清空 = 还柜；第一下点红钮进站。类型不靠标题「三份柜」声明。静帧约占视口四成，没有盖住两栏形差。英文 Clock in 屏同样是 dashed Opened vs pinned Proven。

矩阵 01 因 full_page 把下层值班台露出来，会夸大「静帧+桌面双重占屏」；那不是玩家第一屏。

### D2 信息层级　**PASS**

主操作在门热区与两栏；顶栏检索降权（说明书隐藏；死框只到残页）；叙事在 `#view`。主循环大部分时间在值班台两栏与写入钮。

### D3 风格合同一致　**PASS**

抽方 / 公开 / 柜（兼账）：相机同为正视网页；无角色比（合同 N/A）；材质仍是值班台纸皮。换所有者至少三项：顶栏色、字体栈、栏宽/边（方红宋 640 Rx；公开绿雅黑 960；柜褐木条 720；账蓝 860；残页 HIS Tahoma 凹凸）。

### D4 资产完整度　**PASS**

合同清单内 css/html/jpeg 均有文件与引用。无临时代换矩形冒充门或两栏。jpeg 七张相对回滚已换皮，仍是三门静帧，不冒充 DOM 两栏。00-open 墙上「温馨提示」木牌与 DOM 锁句部分同义，但是环境标牌，不是假两栏。不升最大差距。

### D5 关键状态可读　**PASS**

空：左「空。交班会清。」右「空。写下才留。」+ 形差。加载 N/A。错误：拒条 / 无此值班页。暂停 N/A。成功：钉条 +「已写入」。近失败：点打开过有句子且不死档。选中：亮门实线 / 暗门虚线点线。禁用：ended 钮 opacity（矩阵 24 交班/批准灰）。成功失败不同帧。

### D6 动作反馈　**PASS**

点写入 → 即时右栏 `.chip.ver` + `#msg` + 工钱。点打开过 →「打开过不等于证实了。」条仍在左。交班 → 左清右留。近答案不是正解。不唯色、无音频依赖、不靠 hover。

### D7 色/光/字形　**PASS**

三锚 `#3d4a3a / #e8dfd0 / #b54a3c`（红绑写入/交条/钉）。焦点金 `#d6b56a` 绑键盘可见焦点，不绑情绪。`material_id` 已钉。字形不是玩法单位（VP5 N/A）。

### D8 空间维度　**PASS**

2D 写死。`true3d_exception.enabled=false`。

### D9 响应式可操作　**PASS**

320 宽可点进站、可点方门完成打开这份；两栏叠。390 主钮 ≥44。门百分比未改。主操作不依赖 hover。真机手指未测。

### D10 性能预算　**PASS**

媒体与整包落在 VP1 锚内。本地 http 有记录。首包本机约 0.09 s。运行帧率未测，见 did_not_prove（预算锚，未真机证明）。

### D11 无障碍底线　**PASS**

评审人已跑 7 步烟测（见上）。关键状态不唯色。静音 N/A。键盘到进站：行为与矩阵 07 现已对齐（进站钮金线）。减动后钉条：行为与矩阵 09 现已对齐（方笺写入，非账页拷贝）。无整屏频闪。设置名未冒充防癫痫安全。色觉未测。

### D12 原创与许可　**PASS**

可随包分发（MIT）。无商标/官方切图/品牌字当主视觉。许可表有文件名/来源/许可证/AI 栏。

备注（不升最大差距）：七张 jpeg SHA 与 rollback、jpeg-src 均不一致，表文「本局不改静帧 / 未重生成」不成立。下一轮只改表、不改玩法。未见盗用迹象，故不 HOLD。

### D13 截图证据链　**PASS**

通用 12 + 专属 ≥8 齐，路径合规，运行中 http 截。上轮两处缺口已补：07 现为进站层 `#btn-enter` 3px 金线（非值班台门框）；09 sha256 不等于 19，画面为减动后方针写入钉条。before 已按同输入重拍。

### D14 管线匹配　**PASS**

网页解密默认 VP1，无改道。壳是值班台+假页，不是登山顶栏搜词主循环。检索不是模态。AI 一致性：本局主工作是 CSS 壳；静帧替换未填 `ai_consistency` 锁字段，记入 did_not_prove，不单独升差距。

---

## 制作人自检对照

| 项 | 制作人 | 评审 |
|---|---|---|
| D1 | 自报未过（静帧占屏） | **PASS**（视口进站层三秒可读选份/两栏/还柜） |
| D4 木牌 | 自报可能缺口 | **PASS**（非最大阻断） |
| 最大差距 | 本轮自报无 / 只补 D13 | **无**（D13 返工成立） |
| JS / STORE_KEY | 未改 | 六文件 MATCH；键名未改 |
| 双交卷 | 两份同文 | 无冲突 |
| contract_status | CANDIDATE | 合格交卷档；不等于总评通过 |

---

## did_not_prove

- 真机触控 / 色觉模拟 / Safari / iframe
- 真机 30fps 与滚动 >100ms 假死
- 全程只键盘通关（只证到进站焦点）
- 冷启动首包秒数（仅本机已开服约 0.09 s）
- 评审人未逐步点完 ending A（24 运行图 + JS 冻结）
- 七张 jpeg 的生成提示词与是否本局 AI（表文与 SHA 矛盾）
- VP5 字形十局（N/A）

---

## 本轮结论

D13 返工成立。D1–D14 均为 PASS。`reviewer_role ≠ producer_role`。D11 烟测已跑。VP5 字形 N/A。总评 **PASS**。无新的 largest_gap。
