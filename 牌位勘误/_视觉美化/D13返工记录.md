# D13 返工记录　牌位勘误 polish-20260823

日期: 2026-08-23
性质: 只补基线证据。未改 中文版/英文版 玩法、CSS、HTML、engine.js。
contract_status: CANDIDATE（制作人不得 PASS）
review_status: （留空）

## 原因

上一交卷 D13 HOLD：指定回滚 zip 当时不在盘上，before 0 张。备份已恢复 `游戏美化-视觉美化前备份-20260823.zip`（347392502 B）。

## 做法

1. 从 zip 只解出 `牌位勘误/中文版` 与 `牌位勘误/英文版` 到 `/tmp/paiwei-kanwu-before-origin`。
2. 旧版 http：`127.0.0.1:9000` = 备份中文，`127.0.0.1:9001` = 备份英文。HTTP 200。
3. 按现有 after 38 个同名 slug 逐帧拍 `*-before.png`。同 URL、同 viewport、同输入态。
4. 21：聚焦 `#hui`。22：`#auth=approve`。10/12：输入 `WRONG-0000` 并提交。07：Tab + 焦点进台链。09：`prefers-reduced-motion=reduce`。31–38：与 after 同页同尺寸。
5. after `*.png` 未覆盖。当前玩家实现未改。
6. 备份解出 150 个基线文件 sha256 与 `基线清单.sha256` 全对；engine 中英与现包一致。
7. 停 9000/9001/9333；删 `/tmp/paiwei-kanwu-chrome-profile` 与解压目录。

## 核对

- 来源：backup-zip-http，不是 file://、不是设计稿、不是连接错误页。抽查：引言旧圆角白卡、登录 WRONG-0000 +「号对不上」、方案卡权限「批准撤下该纸位」。
- 日志：`_视觉美化/D13-before-log.json`（每帧 url / viewport / input / title / sha256 / bytes）。
- 数量：before 中 38 + 英 38；after 中 38 + 英 38。
