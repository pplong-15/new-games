# 牌位勘误　独立评审　polish-20260823

reviewer_role: 独立评审人（Grok 4.6；未参与本局主界面 / 主图集 / 主 SVG / CSS 制作）
producer_role: 制作人机器人（交卷最高 CANDIDATE）
contract_status_seen: CANDIDATE
review_status: PASS

genre / genre_profile / distribution_form: html-game-puzzle / 多源著录建议 / E（已核对：静态多页相对路径，中英分目录）
pipeline_id: VP1（类型默认；改道理由 N/A，成立）
play_specimen: 镇山契方案卡多源核对（只借一源一皮、不看标题分所有者、六栏只提交建议）。本局未升第二份网页解密标本，未见搜词 HUD。
space_dimension / true3d_exception.enabled: 2D / false

判定日期: 2026-08-23
运行抽查: `http://127.0.0.1:9100/` 中文、`http://127.0.0.1:9101/` 英文。非 iframe。交卷后已停 9100/9101 与评审用 Chrome profile。

---

## 总评

合同、许可、体积、中英各 38 对 before/after、D13-before-log、锁文件与 http 烟测均达到开评后可写 PASS 的标准。主循环仍是夜岗六栏建议；所有者 chrome 可分家；错号 / 选中 / 越权 / 四回单不唯色。制作人未自填 PASS。

largest_gap: 无

---

## dimension_verdicts

| 维 | 判定 | 依据（运行截图或本局实测） |
|---|---|---|
| D1 首帧类型识别 | PASS | `01` after：柜门夜灰 + 胶带格子纸，正文写清「一张卡，六栏，建议」与进台；不是搜词站、不是边检亭。3 秒能说出今夜主动词是填著录建议。 |
| D2 信息层级 | PASS | 主操作在方案卡六栏（`02`/`20`）；来源在九所有者页（`14–19`/`25–27`）；胜负在四回单（`03/04/23/24/30`）。台面书签表是导航，不是搜词驾驶座。 |
| D3 风格合同一致 | PASS | 抽引言 / 夜岗桌 / 馆站 / 晚报 / 宗亲 / 卷宗 / 帖 / 邮箱 / 红头：底材质、导航、栏宽、字体栈至少三项可分。相机正视 2D。未见边检桌、全站圆角卡、搜词模态。`15` before/after 像素相同，属楼层皮本就成型、本局只加无障碍，不否定九皮分家。 |
| D4 资产完整度 | PASS | 九套 CSS 中英各一份且互为同一副本；8 张 jpg 与备份 zip 字节一致；无临时代换矩形冒充主循环对象。 |
| D5 关键状态可读 | PASS | 空：登录未提交 / 邮箱先点左列（`11/16/29`）。错：`.hint.is-err`（`10/12`）。成功：hold 实线匣+■（`03/30`）。近失败：half 虚线骑缝（`04/05`）。失败/越权：over 双线+×（`23`）；print 点线（`24`）。选中：序号方块+内凹左条（`21`）。禁用：删除线+虚框+not-allowed（政务/邮箱停用链，http 实测）。loading/pause = N/A。 |
| D6 动作反馈 | PASS | 点「交建议」→ 立刻 `result-{hold\|over\|half\|print}.html`（9100 实测四条）。近答案 half 与正解 hold 不同框。不靠音频、不依赖 hover。 |
| D7 色/光/字形 | PASS | 三锚与 `material_id=night-shift-paper-ledger-volume-2026` 钉死。强调色绑提交蓝 / 越权褐红。非 VP5，字形 N/A。 |
| D8 空间维度 | PASS | 合同写死 2D；无 WebGL / 真 3D 操作。 |
| D9 响应式可操作 | PASS | 320 方案卡 `overflowX=false`，六栏与提交钮高 44px，提交在折下但可滚到（`06/31/35/37/38`）。390 同（`08/33/34/36`）。公开壳 320 导航折行，不停用链被横滑藏死（`28`）。真机手指 did_not_prove。 |
| D10 性能预算 | PASS | `du` 中文版 2028 KB、英文版 2048 KB、img 各 1720 KB，VP1 锚 8192 KB 未超。本地 http 引言就绪约 0.6s。无远程字/图。帧率秒表 did_not_prove。 |
| D11 无障碍底线 | PASS | 见下节 7 步烟测。开评后已跑。 |
| D12 原创与许可 | PASS | `_视觉美化/许可.md` 有文件名 / 来源 / 许可证 / 可否再分发 / 是否 AI。CSS 本局手写；图原包；无 AI 新图；无远程字库；页脚写明无商标。 |
| D13 截图证据链 | PASS | 路径 `visual/多源著录建议/polish-20260823-{zh\|en}/{nn}-{slug}[.png\|-before.png]`。中英各 38+38。见 D13 专核。 |
| D14 管线匹配 | PASS | VP1 真网页 2D。无逐张 AI 提示词出图。纹理为 CSS gradient。 |

---

## D13-before-log 专核

日志 `_视觉美化/D13-before-log.json`。评审人复算 PNG sha256：76/76 与日志一致。

| 项 | 结果 |
|---|---|
| zip | `/Users/jianglong/Desktop/游戏美化-视觉美化前备份-20260823.zip` 存在，347392502 B，与日志一致 |
| origin | 76 帧全部 `origin=backup-zip-http`；url 为 `127.0.0.1:9000`（中）/ `9001`（英） |
| http_status | zh/en 均为 200 |
| 连接错误 / 设计稿 | `ok_not_error_page=true` 76/76；`bad=[]`；title 均为游戏页名；body_chars ≥140；before 最小 26969 B。抽看引言旧圆角白卡、登录 `WRONG-0000`、「号对不上」、方案卡权限「批准撤下该纸位」——是旧运行页，不是空白/连接失败/北星稿 |
| 基线 150/150 | 日志 `baseline_entries=150`、`checked_files=150`、`mismatch_count=0`、`missing_count=0`。评审人另核**现包**对基线：120 锁文件全对；30 处差恰好是九套 CSS×2 + 六份 desk HTML×2（授权画面层） |
| engine 现包 | 中 `2eb2799a…5852bf`、英 `fe375026…16afc0`，与基线及 zip 抽取一致 |
| 21 同聚焦 | 日志 input=`focus #hui (first field)`。before 与未聚焦卡同哈希（旧皮无选中形）。after 与 `20` 不同哈希，画面为 1 号栏 3px 点线 + 内凹 |
| 22 auth=approve | 日志 input=`set #auth=approve + change`。before 第六栏可见「批准撤下该纸位」。after 整卡 3px 双线 + × 章 |
| 10/12 真实错号 | 日志 input=`type WRONG-0000 + submit #in (real wrong id)`。before 输入框为 `WRONG-0000` + 灰字「号对不上」。after 左 6px `#7a1c12` + `■` 前缀 |
| 07 真实 Tab | 日志 input=`Tab key + focus enter-link`。脚本先 focus `.enter-link` 再 `Input.dispatchKeyEvent Tab`。after 为「进著录台」3px 点线。before 焦点落在下一枚钮（Tab 后移），仍是运行中键盘态，不是设计稿 |
| 09 减动 | 日志 input=`Emulation.setEmulatedMedia prefers-reduced-motion=reduce`。after 与默认可动卡同帧（卡上无旋转章）。http 抽查：`#auth=approve` 时 `::after` content=`"×"` 且 reduce 下 `transform=none`，章仍在 |
| 31–38 同页同尺寸 | 中英 31–38 before/after 均为 320×640 或 1280×800 或 390×844，与清单 URL 一致 |

`07/10/12/21/22` after 高度 713 或 657（短于 before 的 800），属裁切，宽仍 1280；不判连接错误。不升为 largest_gap。

---

## 锁文件（未未授权改）

对照备份 zip 与基线：

| 锁 | 结果 |
|---|---|
| `js/engine.js` KEY / `pickEnding` | 中英与 zip 字节一致。KEY=`paiwei-kanwu-v1` / `paiwei-kanwu-v1-en`。pickEnding 仍：`auth≠recommend → over`；六栏齐 → hold；`shouchun+hesheng_dead` 或 `hesheng+peiOk` → half；其余 recommend → print |
| 登录串 | 仍 `卫-0821` / `Wei-0821`。错号文案未改，只 `classList.add("is-err")` |
| 六栏 option value | hui=`hesheng\|shouchun\|yao`；pei=`jiang\|qiuping_dead\|qiuping_note\|blank_hold`；kan=`east_sheng\|east_dead\|west`；alive=`hesheng_dead\|hesheng_alive\|unknown`；proof=`print\|fax\|weituo_obit\|forum`；auth=`recommend\|approve\|dianzhu\|edit` |
| 四结局 href | `location.href = "result-" + end + ".html"`。9100 实测 hold/over/half/print 四条落地 |
| storage | 旧键可读。`{in:1}` 进 `desk/home.html`。英键不污染中文 origin |
| 8 图 jpg | 中英各 8 张与 zip 字节一致 |
| HTML | 相对 zip 只动 12 个 desk 页：`catalog-card`、`body.slip-*`、`hint.is-err`，以及方案卡内联 44px 装饰样式。无玩法字段改写 |

---

## 许可与体积

见 `_视觉美化/许可.md`。评审人 `du -sk`：中文 2028 KB / 英文 2048 KB / img 各 1720 KB。无 `@import` / Google Fonts / CDN 字图。

---

## 7 步烟测（D11）

| 步 | 结果 | 证据 |
|---|---|---|
| 1 320 主钮可点 | PASS | 进台链 / 六 select / 提交 / 登录钮实测 min-height 44px；`06/28/31/38` |
| 2 只键盘 | PASS | `07` after「进著录台」3px 点线；方案卡 `#hui:focus` 内凹+左 6px 实条（CSS `#cardform select:focus`） |
| 3 只触控 | PASS（桌面代理） | 390 台面与方案卡目标 ≥44px（`08/33`）。真机手指 did_not_prove |
| 4 关声 | N/A | 无音频 |
| 5 减弱动效 | PASS | CSS `prefers-reduced-motion` 关 transition；越权章 `transform:none` 且 `content:"×"` 仍在。系统开关实开 did_not_prove |
| 6 不唯色 | PASS | 错号左条+■；越权双线+×；停用删除线+虚框+not-allowed；四回单线型差（实线匣 / 双线 / 虚线骑缝 / 点线） |
| 7 无整屏频闪 | PASS | 无循环动画 |

---

## http 抽查（9100/9101）

| 抽查 | 结果 |
|---|---|
| 登录对 | 卫-0821 → `/desk/home.html`；Wei-0821 → 英 home |
| 登录错 | 留 login；`.hint.is-err`；border-left `6px solid rgb(122,28,18)`；`::before "■ "`；底 `#f6ebe6` |
| hold | `hui=shouchun pei=qiuping_note kan=east_dead alive=hesheng_alive proof=weituo_obit auth=recommend` → `result-hold.html`；`.slip-hold` 左 10px 实线 + `■` |
| over | 同上但 `auth=approve` → `result-over.html`；3px double `#7a1c12` + `×` |
| half | `hui=shouchun alive=hesheng_dead auth=recommend` → `result-half.html`；左 10px dashed |
| print | 其余 recommend → `result-print.html`；1px dotted |
| 旧档 | `localStorage paiwei-kanwu-v1 = {in:1}` 仍进台 |
| 九皮 | intro-manual / service-cyan-desk / shop-local-2010s / news-portal-163 / corp-table-2005 / archive-simsun / tieba-floors / mail-web-2010 / gov-redbar 与页面 class 同名；bg/font/栏宽可分；馆站焚香褐方签不是餐馆橙；晚报门户红不是红头；红头金线公文不是热榜 |
| 方案卡非唯色 | 1–6 序号方块；选中点线+内凹左条；`#auth=approve` 时 3px double + 旋转 ×；recommend 不镀正解绿 |
| 320/390/44px/focus | 见 D9/D11。`#search-form { display:none }`。`:focus-visible` 3px 点线 |

---

## did_not_prove

- 真机手机触控 / 色觉模拟 / 帧率秒表
- 操作系统级「减弱动效」开关（09 与本评审均用 CDP Emulation）
- 字形十局（N/A，非 VP5）

---

## 合同完整性（开评门槛）

通用 12 + 专属 ≥8 齐；许可表非空；制作人 D1–D14 非全 UNTESTED；`contract_status=CANDIDATE`；`reviewer_role ≠ producer_role`。`first_interactive_s` 制作人标 did_not_prove；本评审本地 http 实测约 0.6s。

---

review_status: **PASS**
largest_gap: 无
服务: 评审用 9100/9101 与 Chrome debug 已停；未改实现 / 未改证据文件。
