# GATE_B　开发验收

## DAG

无环。引言工号 → 登录 → 台面灵位单 → 各源页均可直接打开（书签），不依赖隐藏检索。方案卡随时可交，错选走独立回单，不悬空。暗线（一九九九通报、霍勉自发信）非必持。

## 可点

- 入口 `introduction.html`。首目标「进著录台」。
- 站内 207 条相对链接，缺页 0。
- 登录接受条子上的「卫-0821」。
- 方案卡六栏 `js/engine.js`：`hold / print / half / over` 矩阵手测通过。
- 不做灰栏目搜隐藏页，无 `search-and-forbidden`。
- 权威恒为建议。越权三档进退回回单。

## 皮

九所有者九皮，CSS 整文件复制。引言无检索框。假官网不用 intro-manual。

## 图

八张 JPEG，md5 不重复。无字无台标无人脸遗容。传真机那张因键盘/屏显弃用。

## 结论

可进试玩文档。红线仍守：只 recommend，不对照台，不登山搜词。
