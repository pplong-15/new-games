# UI 修改独立复核

结论：本轮未发现需要返修的回归或遗漏。对照修复前完整备份，检查当前 `app.js` 与 `physical-skin.css`，并以独立 Chromium 无头浏览器加载实际 `file://` 入口完成定点复核。游戏文件只读，未修改其他代理负责的文件。

## 源码复核

- R01：`app.js:45–57` 统一处理判断提交，先保存、重新渲染；归零后清除 modal 与 selection 并显示失败页。`app.js:95` 同样覆盖失败返回，避免旧牌桌滞留。费用提示与当前引擎每次误判扣 2 心神一致。
- R03：`app.js:123` 卡面只触发 inspect，出牌由卡尾显式按钮或详情按钮触发。`app.js:261–265` 只取最近的 `data-action`，一次点击不重复落到父卡面；手牌索引分别由牌面或比较页查看按钮提供，图鉴不附手牌索引。
- 目标与弹窗：`app.js:249–265` 选目标不先调用 play，取消只清 selection。详情保留 back 与 handIndex；从插画返回仍使用父详情对象。正常出牌后清选中状态，重绘最新手牌，未发现索引错位。
- R12：`physical-skin.css:906–937` 比较页使用独立查看/出牌按钮，小屏设置 46px 高，牌桌小卡隐藏直接出牌入口，详情使用整宽按钮。实测浏览器层叠结果符合预期。
- 首页与图鉴：`app.js:115–116` 首页表述改为五案；`app.js:71` 图鉴列表与总数使用当前 playerCards，未继续从旧 playerCards600 截取。当前运行总数 764，界面显示一致。
- 输入、键盘、旧存档：`app.js:269–271` 搜索重绘后恢复焦点与光标；卡面 Enter/空格进入查看；Escape 关闭弹窗或取消目标。导入确认仍走 migrate/validate，再清 modal 与 selection 后重绘。

## 实际覆盖

共 51 项定点检查，51 项通过，页面脚本错误 0 个。

1. 六张起手牌逐张点击标题：牌局完全未变化，详情使用按钮的手牌索引正确；完整插画返回保留索引；关闭详情回到比较页。
2. 比较页显式出牌一次，实际出牌统计增加一次；目标牌进入选择时不扣费、不弃牌，取消后牌局完全未变化。
3. 合法基础行动达到判断条件，误判扣 2 心神并更新牌桌与弹窗；连续误判归零后立即显示失败页，不留材料弹窗；重载后仍可重试，返回原场准备并重新开局恢复 30 心神。
4. 390px、760px、761px 宽度下比较页无横向溢出；操作按钮分别为 46px、46px、44px 高。
5. Enter 查看和 Escape 关闭；牌组搜索重绘保留输入与焦点；图鉴详情没有误出现手牌出牌按钮。
6. 从当前合法牌局构造 version=4 的兼容性样例，通过菜单导入。确认框说明进行中牌局将重试；迁移后保留已完成进度、显示本场准备页，并可正常开始。这是兼容性样例，不冒充历史用户真实存档抽样。

边界：本轮是所列改动的源码与隔离浏览器定点复核，没有重复全 108 场游玩，没有覆盖真实触屏设备、Safari 或所有历史损坏存档。主审的完整实机与战役验收仍独立成立。

证据：`ui-code-check.py/json`、`ui-code-edge-check.py/json`。检查时文件 SHA-256：

- app.js: `769824cbeae9c4b653e3e25d8c4066f87b31adc9fbc81bb0b94d8164790c8ecc`
- physical-skin.css: `3f87d6145b1c3c04d17c56ffa62bd976ceb0db9b1416d3a2b111b539bd5c3174`
