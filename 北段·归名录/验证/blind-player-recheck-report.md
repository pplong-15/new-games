# V2 UI 修复复核

2026-09-08，五分钟内有界复核。新建干净 Playwright browser context，1440×900；未复用旧存档。只读可见 UI、截图并操作界面，未读源码、skill、答案、内部状态或 localStorage。

本轮结论：下列已实际检验的修复通过；这是 UI 修复复核，不是全游戏上线验收，也不要求或声称覆盖第 3–5 场。

| 项目 | 实际证据 | 结果 |
| --- | --- | --- |
| 初始手牌可操作性 | 第一场初始牌桌第一批 4 张完整卡牌的费用、作用、按钮同屏；打出按钮约 y=840，位于 900 高视口内。后续手牌使用横向滚动。 | 通过，比旧版手牌从 y≈930 才开始明显改善 |
| 主角心神与护身排版 | 可见「心神 30」「护身 0」分行，不再挤在数字旁。 | 通过 |
| 重复筹码 | 只见一组「专注 0/5、信任 0/5、脚步 0/5」。 | 通过 |
| 抽 2 弃 1 的回执 | 起手自然有「翻找行囊」。打开详情并正常打出，回执为「『翻找行囊』生效：行动已记录。；抽到 2 张牌；弃去 1 张牌」。使用前后手牌均为 6，符合使用 1、抽 2、弃 1。 | 通过；句号后接分号稍生硬，但数量已清楚 |
| 阻力术语 | 点击基础「压制危险」后，单个目标自动处理；回执为「伸向鞋盒的手阻力 2 → 0」。 | 通过，没有再把障碍称作生命 |
| 调查干扰术语 | 第一幕写「反光打断：本幕首次结算时调查 −1」。 | 通过，与目标名称一致 |
| 迎面逼退新图 | 卡面及详情显示雨中人物持械迎挡的动作，已不是记话簿/校记册。 | 通过本张的动作图修复 |
| 目标提示 | 卡面和详情均写「选择一个危险或对手」，无目标后写「没有可选择的威胁」。本次基础压制只有一个目标，自动执行，未出现多目标选择提示。 | 卡面用词通过；「多目标选择分支」本轮未实际覆盖，不能据此单独验收 |

已知未复核：第二场通路与柜前待交物反馈矛盾、全部器物图（初始短木棍仍见待替换图，负责人已明确在修）、600 卡触发可达性、故事后段、移动端、存档恢复。没有把正在修的工具图记成已通过。

截图：

- `blind-player-recheck-01-first-table.png`：1440×900 初始桌面与手牌、心神/护身、单组筹码。
- `blind-player-recheck-02-draw-discard.png`：实际抽 2 弃 1 后的回执。
- `blind-player-recheck-03-basic-target.png`：单目标基础压制后的阻力回执；文件名不代表出现了多目标选择。
- `blind-player-recheck-04-attack-art.png`：迎面逼退详情及新动作图。

本代理脚本 `blind-player-recheck-session.cjs`；复核停在首场第一幕的迎面逼退详情，截图保存后已请求关闭新浏览器会话（exec session_id=60401）。旧盲玩报告的历史观察仍保留，不能用它覆盖本轮已通过项目。
