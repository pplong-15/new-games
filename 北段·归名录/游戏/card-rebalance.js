/* V2 rules and faces share the catalog source. This final loading-stage pass also removes any stale V1 art-layer copy. */
(function(root){'use strict';function apply(D){if(!D)return [];const changed=[];for(const [id,face]of Object.entries(D.canonicalCardText||{})){const c=D.cards[id];if(!c)continue;for(const [key,value]of Object.entries(face))if(c[key]!==value){c[key]=value;if(!changed.includes(id))changed.push(id);}}D.cardRebalanceVersion=2;return changed;}const api={version:2,apply};api.changed=apply(root.BDData);root.BDCardRebalance=api;if(typeof module!=='undefined')module.exports=api;})(typeof window==='undefined'?globalThis:window);
