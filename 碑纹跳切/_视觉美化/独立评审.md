# 碑纹跳切　独立评审　polish-20260823

reviewer_role: 独立评审人（未画本局主界面 / 主图集 / 主 SVG）
producer_role: 制作人机器人（本局画主 CSS / 交运行截图）
review_status: PASS
contract_status_seen: CANDIDATE
reviewer_role ≠ producer_role: 是

抽查运行：http://127.0.0.1:8912/ （中文版根）与 http://127.0.0.1:8913/ （英文版根）。HeadlessChrome CDP。Cursor 内嵌浏览器本局无法开页（与制作人 did_not_prove 一致），不计入制作证据，只作评审抽查失败记录。制作人截图来自 8812/8813；本局抽查改用 8912/8913，同源目录。

---

## 交卷门

- 视觉合同存在，必填字段有值或 N/A+理由。`visual_capabilities` 填的是「不用悬停识别可检查物件 / 从轮廓差读跳转」，不是氛围词。`pipeline_id=VP1`，与 `html-game-puzzle` 默认一致，无改道。`space_dimension=2D`。`contract_status=CANDIDATE`。`review_status` 制作人留空。
- 制作人交卷为 CANDIDATE。通用 12 均为 PRESENT；专属 PRESENT 超过 8 张；16 搜索命中与禁止页有 N/A+理由；许可表非空；D1–D14 制作人写自检待评，未写 PASS。
- **20260823 复评：** 合同项「五套并排单帧 / 31-five-skins」已改 PRESENT。制作人交卷有返工记录，未改游戏实现、未写 PASS。本文件据此只改 D13 与总评。
- 制作人不得写、也未写总评通过。

## 许可

许可表非空：local.css 本局制作、随包、非 AI；JPEG 旧包 PIL 生成脚本仍在包内 `_mkimg.py`，像素 SHA256 与基线一致，本局未改图；系统字体不内嵌。无商标切图、无品牌字当主视觉、无远程 CSS/字体/图。旧 JPEG 的「是否 AI」合同写未重证——生成链可在包内核对为程序噪声，不升 HOLD。

## 体积

JPEG 中英各 943834 字节。中文目录 `du` 1280KB，英文 1296KB。`local.css` 10312 字节。远低于 VP1 媒体 8MB 锚。首包秒表与运行帧率未测，进 did_not_prove，不把预算当实测。

## 逻辑哈希（基线对照）

与 `/Users/jianglong/Desktop/github游戏/_视觉美化工作/基线清单.sha256` 对照，抽查：

| 文件 | 结果 |
|---|---|
| 中英 `js/beiwen.js` | MATCH |
| 中英 `desk/submit.html` | MATCH |
| 中英 `introduction.html` | MATCH |
| 中英 `rub/now.html` `rub/y1986.html` | MATCH |
| 中英 `img/*.jpg` | MATCH |
| 中英 `css/local.css` | CHANGED（预期） |
| 中英 `desk/find.html` 与三张 `result-*.html` | CHANGED（仅 body `slip-*` class，预期） |

中英 radio 仍为 `recarve|same|approve`。引言抹痕与现扫描四热区百分比与 class 未改。包内 html/css/js 无业务远程 URL。

## 通用 12 + 解密专属 + before/after

路径：`visual/物件跳切网页档案/polish-20260823-{zh|en}/{nn}-{slug}.png`，成对 `-before`。中英各 after 29、before 29；无 16 文件（N/A）。截图清单写明 http 与 HeadlessChrome，非 file://。抽看 01/02/03/04/05/06/07/08/10/11/12/13/14/15/19/20/21/22/23/25/28 及英 03/04：是运行页，不是设计稿。

- 01 开局人+查阅单+拓+朱红虚线抹痕可点。
- 02/22 现扫描 quiet 点线+裁切，无悬停仍见（02 与 22 字节相同，同页同态，算一张证据复用）。
- 03 改刻实线实章；05 已更名虚线虚章+作废链；28 越权双线斜章。三回单不同帧不同形状。
- 04 勾 `same` 未交，左墨条，不是正解。
- 06/26 320；08/29 390。
- 07 与 01 像素差落在抹痕热区 bbox，约 2.1% 像素变，焦点框在图上。
- 09 与 01 字节全同：减动证据拍在无章的引言，截图本身不能证明章不转。减动章由本局 http 抽查补看（见下），不把 09 当章证据。
- 11/17 空查找点线斜纹+「抽不出来」句（有无 q 同页，字节相同合理）。
- 12 空交左朱红条。
- 13 说明书无搜框；14 土导航+死查找；15 房头长文异皮；18 卷宗；19 红头；20 石作蓝表；21 博客；25 黄页。
- 16 搜索命中 N/A 理由成立。禁止页 N/A 理由成立。
- 类型追加「静帧不露未获 token」未单列。本标本无 token 静帧，语义可 N/A，制作人未标。不单开第二 gap。
- 五套并排：见下方复评。31-five-skins 中英 PRESENT。无 31-before（本轮 HOLD 只缺 after 同帧，不另开 gap）。

## 复评　31-five-skins（只核此项）

路径：
- `visual/物件跳切网页档案/polish-20260823-{zh|en}/31-five-skins.png`（合同 genre_profile 目录）
- `visual/html-game-puzzle/polish-20260823-{zh|en}/31-five-skins.png`（同字节副本）

几何：6424×836 = 五格 1280×800 + 间隔 6px×4 + 顶栏标签 36px。同 viewport。中英各一份。

顶栏短标签（评审读图，非制作人补拍）：
- 中：`8912/desk/home.html` · `8912/rub/now.html` · `8912/gov/index.html` · `8912/shop/index.html` · `8912/introduction.html`
- 英：`8913/desk/home.html` … `8913/introduction.html`

五格像素与既有运行 after 同页对齐为 **0 差**（desk=14，gov=19，shop=20，rub/now=18 顶 800，intro=13 顶 800）。无滤镜、非设计稿。8912/8913 现仍 200 回这五页，html class 五套不同：`skin-service-cyan-desk` / `skin-archive-simsun` / `skin-gov-redbar` / `skin-corp-table-2005` / `skin-intro-manual`。格间像素差 91–100%。涂黑标题后仍能指认青台、拓纸卷宗、红头、石作蓝表、值班条纸块。

制作人返工记录写明只补 D13、未改 HTML/CSS/JS/图。与哈希锁文件上一轮 MATCH 不冲突。本轮未重跑玩法抽查。

## 7 步烟测（制作人记录 + 本局 8912/8913 抽查）

1. 缩到 320 主操作可点：06/26 已拍。本局 320 现扫描四热区高均 44；引言抹痕约 86×44；进台/浅磨链高 ≥44。
2. 只键盘第一个主线点：07 已拍焦点差。CSS `:focus-visible` 在 `:focus{outline:0}` 之后，特异度相同、后者覆盖。本局用脚本 `.focus()` 不触发 `:focus-visible`（符合浏览器启发式），不以脚本 focus 代替 Tab 截图。未拍 Tab 走完交单（制作人已写入 did_not_prove）。
3. 只触控同一段：08/29 为 390 模拟。390 现扫描约 120×44 / 60×44 / 93×44 / 53×44。真机手指未测。
4. 关声：无音频，N/A。
5. 减弱动效：09 截图不能证明章。本局 `prefers-reduced-motion: reduce` 下 `.stamp` transform none，「库内」字仍在；越权 `::after` transform none、仍 44×44。无减动时库内章为旋转矩阵。主反馈仍在。
6. 状态不唯色：选中左墨条+加粗；三回单实/虚/双线+章形；quiet 点线裁切 vs 教学朱红虚线。
7. 无整屏频闪：无循环动画。

开评后 7 步有记录且本局复跑关键步，D11 不因 09 拍错页而 HOLD。真机触控与只键盘通关全路径仍进 did_not_prove。

## 抽查：中英 recarve / near-fail

| 步 | 中文 8912 | 英文 8913 |
|---|---|---|
| 点引言抹痕 | → `rub/y1986.html` | → `rub/y1986.html` |
| 勾 recarve 交单 | → `result-recarve.html`，`choice=recarve`，键 `beiwen-tiaqie-v1` | → `result-recarve.html`，`choice=recarve`，键 `beiwen-tiaqie-v1-en`；中文键未被写入 |
| 勾 same 交单 | → `result-same.html`，作废链在 | 同上 |
| 顶栏查找 | `find.html?q=纪闻山` 有「抽不出来」 | 制作人英截 17 已拍 miss 句 |

玩法未回归。不 HOLD 玩法。

## 抽查：quiet 热区常态可见

8912 `rub/now.html` 1280、无 hover：四枚 `.hot.quiet` `opacity=1` `visibility=visible` `border=dotted 1px rgba(92,62,38,0.78)`。截图 02/08/22 能看到点线框。不依赖 hover。螭首在深色颗粒上对比偏低（制作人自陈），框仍在、高 44。此残差不升为唯一 gap（唯一 gap 已被 D13 占用）。

## 抽查：320 / 390 44px

本局实测与制作人 `measures-after.json` 一致：现扫描四热区高度在 320 与 390 均为 44；匣沿 320 为 44×44。交单钮、页签、查找钮 390 高 44。真机未测。

## 抽查：键盘 focus

CSS 有 `.hot:focus-visible` 2px 墨点框。07 相对 01 的像素差在抹痕热区。未独立完成一次真实 Tab 遍历（内嵌浏览器开页失败）。不把 D11 判 HOLD，但全路径键盘通关仍未证明。

## 抽查：旧档

写入 `{seen:{intro:1,smear1986:1,zhi:1}}` 后刷新台面，中文 `BW.all()` 读回 smear1986+zhi；英文键 `beiwen-tiaqie-v1-en` 同样读回。存档键未改。27 与 14 截图像素相同：台面不把 seen 画成另一张壳，截图不能单独证明读档；读档以本局 localStorage 抽查为准。

---

## dimension_verdicts（正式，仅评审人）

D1: PASS　开局人/查阅单/第一张拓/可点抹痕同屏，不靠标题声明类型。证据 01/13。
D2: PASS　主操作在拓片热区与交单三行；台面驾驶座；顶栏查找死摆设（11/14/17）。
D3: PASS　正视 2D 锁死。引言纸块 / 卷宗拓纸 / 青台 / 红头 / 石作 / 房头 / 博客 / 黄页可分家。未要求本局改所有者骨架。
D4: PASS　无新临时代换矩形。热区仍叠旧 JPEG。本局资产即 local.css + slip class。
D5: PASS　空查找、空交、改刻、已更名、越权、选中、焦点、减动章各有形状。无暂停无加载，合同已 N/A。成功失败不同帧。
D6: PASS　点抹痕立刻换页；交 recarve 回单；same 近答案可作废。quiet 不靠 hover。反馈不唯色、无音频依赖。螭首远看偏弱，不阻断主循环，不单列 gap。
D7: PASS　三锚已钉；朱红不绑正解三选；字形非玩法单位。
D8: PASS　2D；true3d_exception false。
D9: PASS　320/390 模拟下主动词热区高 44、可点、不靠 hover。真机触控未测 → did_not_prove，不升 HOLD。
D10: PASS　包体远低于 VP1 锚；有本地 http 记录。帧率/秒表未测 → did_not_prove。
D11: PASS　7 步有记录；本局复跑 320/热区/减动章/不唯色/无频闪。静音 N/A。未证明只键盘走完交单、未真机触控。
D12: PASS　许可表非空；随包可分发；无商标主视觉；无新远程；无新 AI 图。
D13: PASS　通用 12 齐、专属 ≥8 齐或 N/A、路径合规。31-five-skins 中英为 8912/8913 同 viewport 实页横拼，五套可指认。合同该项 PRESENT。
D14: PASS　VP1 真网页多所有者壳；无新 AI 图；未画成胶片台或一套圆角卡。

---

## largest_gap

无。上一轮唯一 gap（D13 五套并排 UNTESTED）已用 31-five-skins 闭合。残留螭首对比偏低不升为本轮 gap，制作人已声明本轮不得修。

## did_not_prove

- 真机手机触控（仅 Chrome 320/390 模拟）
- 色觉模拟
- 运行帧率 / 首包秒表
- 只键盘走完正确交单全路径（07 只证明引言第一热区焦点差）
- Cursor 内嵌浏览器开页（制作人与评审人均失败）；评审抽查用 HeadlessChrome + 8912/8913
- 类型追加「静帧不露未获 token」未在合同标 N/A

## 下一步

PASS。不要扩面，不要顺便修螭首。
