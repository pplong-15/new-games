# 碑纹跳切　闸 B（开发）

验收时点：静态 HTML 已生成，入口 `introduction.html`。

## 专章 B

- [x] 谜题节点化：物件跳转写在页上热区，提交写在交单三选，不是每屏手写 if 迷宫。  
- [x] token 以 `BW.mark` 落 `localStorage` 键 `beiwen-tiaqie-v1`。提交不强制齐套，错选走独立回单。  
- [x] 来源带 limits：拓/回单/志石/螭首页用局内口气写能证什么、不能证什么。  
- [x] 权限 recommend。批准行可勾，勾了走 `approve` 退回。  
- [x] 近答案显式：same / approve / 并碑，各有回单或页上顶回。  
- [x] 解锁靠已打开材料，不靠「第几步」。  
- [x] 查找框落地 `desk/find.html`，不按姓名抽页，不误开主线材料。  
- [x] 皮：intro-manual / service-cyan-desk / archive-simsun / gov-redbar / corp-table-2005 / wechat-mp-article / blog-personal-2008 / classified-yellow。  

## 体量（生成后实测）

见 `VOLUME.md`。dupcheck / imgcheck 须 PASS。

## 结论

**PASS**。可进试玩。开发期红线：跳转键保持物件；收口键保持 recarve；不要做成姓名检索。
