# 改动摘要 · 第二批最终发布总审 · 2026-08-23

本审未改任何游戏文件或单局评审。下列「改动」是对照备份 zip / 基线清单，归纳制作局已做的**画面层例外**；玩法锁保持原样。

总审产出仅四份：

- `_视觉美化工作/最终验收总表.md`
- `_视觉美化工作/最终验收总表.json`
- `_视觉美化工作/发布文件清单.sha256`
- `_视觉美化工作/改动摘要.md`

判定：**PASS 9 / REWORK 0 / HOLD 1**。阻断只报告不修。

---

## 发布清单口径

`发布文件清单.sha256` 共 **1659** 条，路径相对项目根，UTF-8 字节序。

**计入：** 各款中英玩家树内真实运行文件。

**排除：** `_视觉美化*`、`visual/`、`rollback*`、`rejected*`、`generated-img`、`screenshots`（工作证）、根 `ref/`、镇山 `_loc`、`.DS_Store` / `._*`。

**镇山契例外：** 只列 `_视觉美化/玩家包白名单.sha256` 的 **7** 文件（两份 v0.3 HTML、两份上线版跳板、中英 `怎么玩.txt`、中文 `VERSION`）。设计稿 md 与 `_loc` 工具不进玩家包。

**民生：** 591 条 = 基线玩家树减去已迁出的 32 张 `assets/base`。

**夜班：** 40 条 = 中英 `夜班.html` 及原目录文案；营销图不在玩家树。

---

## 主动词 / 结局 / 控件 / storage

对照备份 zip 抽查锁文件：

| 款 | 未未授权改动的锁 | 视觉 HTML/CSS 例外（允许） |
|---|---|---|
| 牌位勘误 | `js/engine.js` MATCH；KEY / pickEnding / 六栏 value / 四结局 href | desk 页 class；select 44px 内联样式；九套皮 CSS |
| 夜台听写 | `grade()` 与 checkbox/select value；KEY 仍 `yt_seen` / `yt_en_seen` | 四份 CSS；`seen.js` 只加 heard/unheard；annotate `end-*` |
| 民生热线 | `clips.js` MATCH；CAP=5；`minsheng-v1` / `-en` | Luna CSS；engine 仅 tabIndex+键盘开片；280 静帧重编码；base 迁 ref |
| 门坎试法 | engine/HTML/jpg 全 MATCH | 十套 CSS × 中英 |
| 桥头信息港 | tokens/keywords/HTML MATCH；`rollback.html` 基线已有 | 十套 CSS × 中英 |
| 同窗在线 | `engine.js` / `data.js` MATCH；saveKey 中英分键 | `common.css`（含 D9 min-width:44px）；7 张几何 jpg |
| 纸扎组句 | `engine.js` MATCH；SLOTS/CORRECT/pickEnding | 新 `pick-overlay.css`；HTML 挂 overlay；三回单 `end-*` class |
| 清明三门 | `sanmen.js` MATCH；KEY/MAX/openDoor/handover | 三份 CSS × 中英；八张 jpg 重编码 |
| 夜班 | SAVE 仍 `hengdeng-nightshift-v2` / `-en`；`pickEnding` / `leaveAtDawn` 仍在 | 单文件内工作台 CSS + 内嵌媒体重压（体积 12 MB→1.5 MB） |
| 镇山契 | storageKey / hash 路由 / ENDING-A/B / 18 拍仍在 | 内联 overlay CSS；空态 class |

中英玩家入口分目录；存档键均带语种后缀或独立 KEY，未见交叉污染。

同窗 / 镇山 `上线版/index.html` 为跳板，真入口分别是 `工程/index.html` 与 v0.3 单 HTML。

---

## 媒体与隔离

- 九款 VP1 玩家媒体均 ≤ 8 MB。
- 镇山玩家 HTML &lt; 2 MB；619 真站参考只在 `_视觉美化工作/ref/镇山契参考归档/`。
- 民生 base 只在 `_视觉美化工作/ref/民生热线-base/`。
- 夜班营销图只在 `_视觉美化工作/ref/夜班营销图/`；单文件预算成立。
- 夜班 rejected 七晚污染帧不计矩阵、不进清单。

---

## 唯一 HOLD（不修）

**夜班 D12。** `licenses.md` 对店景 / 柜台 / 人物·猫仍 UNSET。本审不接受「原来就在包里」作为可再分发证明。largest_gap 只此一条。雨雷 Mixkit 注释与营销图迁出不改变本维。

其余九款正式独立评审均为 PASS（非 CANDIDATE），本审抽查后维持 PASS。
