# GATE_B　开发验收

## DAG

无环。引言工号 → 登录 → 台面法与待记 → 试记打回留下 verified.broke，或打开记录页留下 seen.record。丧证来自短信 / 钥匙簿 / 巷帖，至少一处。建议四栏随时可交，缺证退回，错选走 let-in / rewrite。暗线（返工单、阙禾自发）非必持。刷新不清已证实。

## 可点

- 入口 `introduction.html`。首目标「进夜岗台」。
- 相对链接抽检：缺页 0。
- 登录接受门框上的「柴-0821」。
- 试记：踩踏 / 擦杠 → `verified.broke`；正常跨不给 broke。
- 建议 `js/engine.js`：`hold-funeral / let-in / rewrite` 与退回码矩阵手测通过。存档键 `menkan-shifa-v1`。
- 不做灰栏目搜隐藏页，无 `search-and-forbidden`。不做关站，不做队列弹单。
- 权威恒为建议。批准与改板另走。

## 皮

九所有者九皮，CSS 整文件复制。引言无检索框。假官网不用 intro-manual。

## 图

八张 JPEG，md5 不重复。无字无台标无人脸。钥匙匣用钩子特写，避免柜门英文。

## 体量

`dupcheck`：html 44，hanzi 10005，dup40 0，trip24 0，PASS。  
`imgcheck`：unique_md5 8，PASS。

## 结论

可进试玩文档。红线仍守：只 recommend，不搜隐藏页，不巡站，不队列，正确收口 `hold-funeral`。
