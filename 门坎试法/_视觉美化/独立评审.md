# 门坎试法　独立评审　polish-20260823

reviewer_role: 独立评审人（未参与本局 CSS / 主图集 / 主 SVG / 截图制作）
producer_role: 制作人机器人（本局画 CSS / 交运行截图；八张 JPG 本轮不改像素）
contract_status_observed: CANDIDATE
review_status: **PASS**
genre / genre_profile / pipeline_id: html-game-puzzle / 撞法留证建议 / VP1
space_dimension: 2D
largest_gap: 无

本评只读实现与交卷证据，只写本文件。未改 HTML / CSS / JS / 图片，未补制作证据。

---

## 0. 开评范围与方法

审核对象：`/Users/jianglong/Desktop/游戏美化/门坎试法`（中文版 / 英文版 / `_视觉美化/` / `visual/撞法留证建议/polish-20260823-{zh|en}/`）。

对照文件：视觉合同、制作人交卷、licenses.md、风格圣经、playtest-run.json、measures-after.json；基线 `/Users/jianglong/Desktop/游戏美化/_视觉美化工作/基线清单.sha256`。

本评自开本地 http（非 file://、非 iframe）：

- 中文 `http://127.0.0.1:9106/`
- 英文 `http://127.0.0.1:9107/`

用独立 Chrome headless CDP（调试口 9366，与制作人 9006/9007、9346 分离）走中英主路径。制作人自报烟测与 playtest-run.json 只作对照，不以制作人自证代替本评抽查。

---

## 1. 合同与交卷完整性

| 项 | 判定 |
|---|---|
| 合同必填字段 | 齐。`genre` 网页解密，`pipeline_id` VP1，有柜台但书面钉死不改道岗位模拟。`contract_status=CANDIDATE`。制作人未写 PASS。 |
| 制作人交卷 | 齐。改动声明仅 CSS 十套×中英；锁文件清单与回滚点写明。 |
| 许可表 | 非空。见 §5。 |
| 体积 | 实测与交卷一致。见 §5。 |
| 烟测 7 步 | 合同与交卷有记录；本评按步复跑。见 §6。 |
| 通用 12 | 中英 `01–12` 均有 `-before` 与 after，路径符合 `visual/撞法留证建议/polish-20260823-{zh\|en}/{nn}-{slug}.png`。 |
| 解密专属 ≥8 | `13–20` 齐；另有 `21–34`。搜词/禁止页/暂停/加载按标本 N/A 且有 `na_reason`。 |
| 五套并排单帧 | 合同标 UNTESTED。未合成蒙太奇。本评以 `14/15/16/19–23` 序列 + 现场九 `html` class 指认代替，不升为缺证 HOLD。该项本应标 N/A 而非 UNTESTED。 |

CANDIDATE 前置满足：通用 12 齐或 N/A；专属 ≥8 齐或有理由 N/A；许可表非空；D1–D14 开评后不再留 UNTESTED。

---

## 2. 锁文件：相对基线未被未授权修改

基线 152 项，缺失 0。相对基线 **只改 20 个 CSS**（中英各十套，中英同字节）。其余锁定项哈希一致：

| 锁 | 结果 |
|---|---|
| `中文版/js/engine.js` | MATCH `6bd08570f0b36c7e…` KEY=`menkan-shifa-v1`；`pickEnding` / `BOUNCE` / `verified.broke` / `seen.*` / `token-in` 仍在 |
| `英文版/js/engine.js` | MATCH `b803015ffa4c8802…` KEY=`menkan-shifa-v1-en` |
| 全部 HTML | 88/88 MATCH |
| 全部 `img/*.jpg` | 16/16 MATCH；中英八张同字节，合计各 2 269 204 |
| `_gen_pages.py` / `_gen_img.py` | 中英均 MATCH；本轮未跑生成器 |

现场抽查（中英一致）：

- 工号 `柴-0821` / `Chai-0821`（错号停登录页，正号进 `home.html` 并写 `token-in`）
- `select option value`：`wedding|funeral|rewrite` / `clean|stepped` / `letin|hold|rewrite` / `recommend|approve`
- `try` radio value：`step|pole|clean`
- `a[href]` 台面书签：`try.html` `record.html` `advise.html` `board.html` `shift.html` `memo.html` `hint.html` 与七站相对路径未改
- 存档键：`menkan-shifa-v1` / `menkan-shifa-v1-en`

玩法标本未改。若日后发现 HTML/JS 被改，应立即 HOLD。本评未见。

---

## 3. 功能回归抽查（本评 http 9106/9107）

中英同构，均 `ok=true`。

| 步 | 中文 | 英文 |
|---|---|---|
| hold-funeral | 登录 → 试记踩踏 → `phone/mu.html`（`seen.sms`）→ 四栏丧+已踩+不放行+建议 → `desk/result-hold.html`；回单 h2「建议已收下：不放行进堂」，`::before` 为「■ 」 | 同路径 → `result-hold.html`；「Recommendation taken: do not let them enter the hall」 |
| let-in 近答案 | 喜+已踩+放行+建议 → `result-letin.html`；存在 `a[href=advise.html]` 可改四栏 | 同；recovery 链在 |
| bounce | 丧+干净+不放行+建议 → 仍停 `advise.html`；打回匣 `display:block`，双线 `4px double rgb(90, 48, 48)`，`::before`「× 」，句为过门栏对不上 | 英文对应 bounce-record 句 |
| 旧档 | 只写 `{verified.broke, token-in, seen.law}` 刷新台面，`.mk-chips`「已证实：法被撞过一次」+「■ 」仍在 | 「Verified: the rule was broken once」 |

错工号：中文「工号在门框内侧。」留在登录页。引言无 `input[type=search]` / `#search-form` / `#search-input`。九皮现场均无搜框。

---

## 4. 截图矩阵（before/after）

根：`visual/撞法留证建议/polish-20260823-zh/` 与 `-en/`。各 34 after + 34 before。运行 URL 记在 measures-after.json，为制作人当时 `9006/9007` 的 http CDP，不是 file://。本评抽看了运行帧，不是设计稿。

| nn | slug | 中英成对 | 本评看见 |
|---|---|---|---|
| 01 | boot-first-screen | 有；**像素与 before 相同** | 巷口静帧 + 门框字 +「谁把我叫来」；无搜框。1280 引言主视觉本轮几乎不动，窄屏差在 34。 |
| 02 | core-verb | 有且有差 | 试记三选；已选实线+左条，未选虚线；木色「交试记」 |
| 03 | success-feedback | 有且有差 | hold 回单实线匣 + ■ |
| 04 | near-fail | 有且有差 | let-in 双线 ×，可改四栏 |
| 05 | recovery | 有且有差 | 演练点线 ○，链回试记 |
| 06 | narrow-320 | 有且有差 | 320 三选与交钮可点，无横溢 |
| 07 | keyboard-focus | 有且有差 | 踩踏 3px 点线框（中英均见） |
| 08 | touch-targets | 有且有差 | 390 试记三选 / 交钮 / 回台 |
| 09 | muted-or-reduced-motion | 有且有差 | 钥匙室未归还红戳；本局无音频 |
| 10 | non-color-state | 有且有差 | 已选实线左条 vs 未选虚线 |
| 11 | empty-or-loading | 有且有差 | 四栏空虚线 +（空）；无加载屏，N/A 成立 |
| 12 | error-or-pause | 有且有差 | 错工号句留在登录方框；无暂停页，N/A 成立 |
| 13 | intro-no-search | 有；与 01 同帧 | 说明书无搜框 |
| 14 | duty-board | 有且有差 | 横纹木条值班板 vs 差事青条 |
| 15 | embedded-phone | 有 | 240 黑屏黄链功能机 |
| 16 | archive-dossier | 有 | 宋体卷 + 未归还红戳 |
| 17 | try-reject | 有且有差 | 打回双线 × + 已证实仍在 |
| 18 | advise-four | 有且有差 | 四栏编号 1–4 |
| 19 | two-source-public | 有 | 米色对外 + 灰圆顶 Tab，无搜框 |
| 20 | two-source-forum | 有 | 蓝头楼层帖 |
| 21–23 | 黄页 / 邮箱 / 百科 | 有 | 黄顶左栏；三栏邮箱；靛蓝词条 |
| 24–34 | 短信 / 记录 / rewrite / bounce / 空交 / 旧档 / 320 台面建议 / 390 下拉 / 登录 / 引言窄屏 | 有 | 与合同描述相符 |

中英 `01` 与 `13` after 与 before 哈希相同（引言 1280）。其余 32 对有差。不据此判缺证：窄屏引言 `34` 已证明进台钮块级 44。

---

## 5. 许可、体积、九皮

许可表非空，可随包分发：

- 本轮 CSS：本局叠皮，非 AI
- 八张 JPEG：旧包已有，本轮未改像素；AI 血统本文件未重证（记入 did_not_prove，不升 HOLD）
- 字体：系统栈，不内嵌
- 无远程 CSS/字体/图；无商标图形、无品牌字当主视觉

体积（本评复测，与交卷逐字节一致）：

- media JPEG 中英各 2 269 204（约 2.16MB）≪ VP1 媒体锚 8MB
- 中文目录（不含 visual/_视觉美化）2 505 922；英文 2 531 731
- 中英 CSS 十套同字节

九皮现场（涂黑标题仍可分；至少底色 / 导航 / 标题色 / 栏宽 / 字体栈三项差）：

| 所有者 | html class | body 底 |
|---|---|---|
| 门框字 | skin-intro-manual | `#f4f4f4` |
| 夜岗台 | skin-service-cyan-desk | `#c5dbe3` |
| 对外修缮 | skin-shop-local-2010s | `#f9ebde` |
| 钥匙室 | skin-archive-simsun | `#f7f7f7` |
| 功能机 | skin-wap-phone-2007 | `#444` 壳 / 240 黑屏 |
| 黄页 | skin-classified-yellow | `#fff8e1` |
| 巷帖 | skin-tieba-floors | `#eef2f5` |
| 邮箱 | skin-mail-web-2010 | `#eef2f5`（与巷帖同底，Tahoma 三栏 vs 楼层蓝头，仍可指认） |
| 旧俗抄 | skin-baidu-baike | `#fff` 靛蓝头 |

九 class 全不同。无顶栏搜词。`shop-local-2010s.css` 仍写 `#search-form { display:flex }`，但对外 HTML 无该节点，画面无搜框，不构成借搜词顶栏。

---

## 6. 无障碍 7 步烟测（本评）

| # | 步 | 结果 |
|---|---|---|
| 1 | 缩到手机宽主按钮可点 | **PASS**。320 试记：回台 56×46，三选 286×46，交试记 83×44，overflow=false。320 引言进台 256×44。320 台面三钮 56×46。390 四栏 select 与交建议均为 44 高，overflow=false。 |
| 2 | 只键盘走到第一个主线点 | **PASS**。真实 `Input.dispatchKeyEvent` Tab：回台 → `input[value=step]` → 交试记，outline 均为 `rgb(26, 42, 48) dotted 3px`。截图 07 中英可见点线框。未要求拍完全程（制作人 did_not_prove「只键盘走完试记到 hold」成立，不挡本步）。单选组内其余项走方向键，属 HTML 常规，不记缺陷。 |
| 3 | 只触控走同一段 | **PASS（模拟）**。390 主控件 ≥44；主循环不依赖 hover。**未真机手指**，见 §7。 |
| 4 | 关声走同一段 | **N/A**。包内无音频。 |
| 5 | 减弱动效后主反馈仍在 | **PASS**。`prefers-reduced-motion: reduce` 下钥匙室 `.stamp` `transform:none`，红框 `2px solid rgb(164,0,0)` 与「未归还 / Copy」字仍在。 |
| 6 | 状态不唯色 | **PASS**。板=2px 实线+左 10px 木条+横纹；证=虚线+■；打回=双线+×；演练=点线+○；已选=实线左条加粗；空栏=虚线+（空）+栏号 1–4；hold 回单=实线+■；let-in=双线+×。强调色不绑四栏对错。 |
| 7 | 无整屏频闪 | **PASS**。无循环动画；减动规则在。 |

D11 烟测已跑。设置名未出现「防癫痫」违规用语。

### 320/390 主操作实测（本评）

夜岗主循环（试记三选、交钮、回台、四栏下拉、引言进台）**最短边均 ≥44 CSS px**，核心页无横向溢出。

卫星皮残余（不阻断主动词，不写入 largest_gap）：

- 旧俗抄静帧 `IMG` 在 320 为 340 宽、390 为 410 宽（溢出 20px）
- 黄页正文「夜岗从这边回」约 17px 高；邮箱列表 `a` 约 14px 高（文件夹/顶栏回链已 44）
- 功能机「回台面」宽 36、「返回」宽 40（高度 44；短信主链 ≥44）
- 对外 logo 高 40（灰 Tab 主链 44）

---

## 7. 制作人自报「真机手指未测」是否发布阻断

**不是。不构成 HOLD / REWORK，不采为 largest_gap。**

依据：

1. D9 问的是 **320 CSS px 能否完成主动词**，以及触控是否可点、是否不依赖 hover。本评在 320/390 设备度量下走完试记与四栏，主操作 ≥44，overflow=false。规范把未真机证明的数字放进 `did_not_prove`，不当成缺证。
2. 7 步第 3 步「只触控走同一段（宣称移动则必测）」：本局主动词是点选 / 提交，**不宣称移动、滑动或手势**。Chrome 390 模拟 + 44px 目标满足本步。真机手指记入 did_not_prove。
3. 与 D10 未测帧率同类：预算内已测体积，未测项清单化，不挡总评。
4. 制作人把此项写成自检 largest_gap 是诚实，但是独立评审不自动继承「未测真机 = 不通过」。

若下一局主动词改为拖、滑、多指，或主按钮实测 <44，再把真机触控升为阻断。

---

## 8. dimension_verdicts

| 维 | 判定 | 证据 |
|---|---|---|
| D1 首帧类型识别 | **PASS** | 引言 3 秒可读夜岗人、交放行建议、试记/打回留下；无搜框、无 HUD、无边检桌。类型不靠标题喊「游戏」。 |
| D2 信息层级 | **PASS** | 主操作在试记三选与四栏建议；值班板/已证实为状态区；加厚段为叙事，不挡交钮。 |
| D3 风格合同一致 | **PASS** | 正视 2D；夜岗闷青+木色交钮；九皮相机与材质跟圣经；抽 14/15/16/19–23 可指认同一批所有者差。 |
| D4 资产完整度 | **PASS** | 合同资产均有文件与引用。无临时代换矩形冒充主循环对象。静帧未改像素。登录为贴台方框（`#dceaf0`、无圆角阴影），不是 Bootstrap 白卡。 |
| D5 关键状态可读 | **PASS** | 空栏、打回、演练、hold、let-in、rewrite、错工号已拍且形状/字可分。无暂停/加载：N/A 成立。成功失败不同帧。 |
| D6 动作反馈 | **PASS** | 点踩踏交试记 → 即时 `try-reject.html` 并写 `verified.broke`。交建议 → hold 回单 / let-in 另匣 / bounce 留页。近答案不是正解也不是死档。反馈不唯色、不靠音频、不靠 hover。 |
| D7 色/光/字形 | **PASS** | 三锚 + `material_id=county-night-duty-2010`。强调色绑交钮与打回，不绑四栏对错。非 VP5。 |
| D8 空间维度 | **PASS** | 2D 写死。`true3d_exception.enabled=false`。 |
| D9 响应式可操作 | **PASS** | 320/390 能完成主动词；主操作 ≥44；不依赖 hover。真机手指未测 → did_not_prove，见 §7。卫星皮小链/旧俗抄图溢出见 §6 残余。 |
| D10 性能预算 | **PASS** | 媒体 2.16MB、整包约 2.5MB，低于 VP1 8MB。本地 http 可交互。帧率/秒表未测 → did_not_prove。 |
| D11 无障碍底线 | **PASS** | 7 步已跑。关键状态不唯色。静音 N/A。键盘焦点真实。无整屏 >3 次/秒闪烁。减动戳仍在。 |
| D12 原创与许可 | **PASS** | 许可表有文件名/来源/许可证/AI 栏。无商标主视觉。本轮无新 AI 图。旧 JPEG AI 血统未重证 → did_not_prove。 |
| D13 截图证据链 | **PASS** | 通用 12 齐；专属 ≥8 齐；http 运行帧；路径符合 `visual/{genre_profile}/{build_id}/`。五套单帧未合成，序列+现场可指认，不 HOLD。 |
| D14 管线匹配 | **PASS** | VP1 真网页壳，无插画房冒充公文，无 Canvas 粒子。无新 AI 图，无逐张提示词。有柜台未改道岗位模拟，书面理由成立。 |

总评 **PASS** 条件：D11 烟测已跑；`reviewer_role ≠ producer_role`；非 VP5。均满足。

---

## 9. did_not_prove（本评清单，非阻断）

- 真机手机手指触控
- 色觉模拟
- 运行帧率 / 首包秒表
- 只键盘走完试记 → hold 全路径（已证第一主线点）
- 五套 chrome 蒙太奇单帧
- 旧 JPEG 的 AI 血统重证
- 旧俗抄 320/390 静帧比视口宽 20px 是否在真机可感知
- Cursor 内嵌浏览器（本评与制作人均用本机 Chrome CDP）

---

## 10. 结论

review_status: **PASS**

largest_gap: 无

制作人自报「真机手指未测」**不是发布阻断**。中英玩法锁完好；值班板 / 已证实 / 打回 / 四栏均非唯色；九皮可指认；320/390 主操作 ≥44；真实 Tab 3px 点线；减动后戳仍在。

报告路径：`/Users/jianglong/Desktop/游戏美化/门坎试法/_视觉美化/独立评审.md`
