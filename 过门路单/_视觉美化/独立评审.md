# 独立评审　过门路单　polish-20260824

review_status: PASS
contract_status_seen: CANDIDATE
reviewer_role: 独立评审人（未参与本局主界面 / 主 CSS 公文皮 / 主图集）
producer_role_seen: 制作人机器人（本局画驾驶座壳 / 主 CSS 公文皮）
genre / genre_profile: html-game-rule-horror / 驾驶座多份公文互斥压单
pipeline_id: VP1
space_dimension: 2D
http_used: http://127.0.0.1:8856/ （中文版根）；http://127.0.0.1:8857/ （英文版根）。8856/8857 已在听，未改用 8956/8957。非 iframe。
build_id: polish-20260824
screenshot_dir:
  - visual/html-game-rule-horror/驾驶座多份公文互斥压单/polish-20260824-zh/
  - visual/html-game-rule-horror/驾驶座多份公文互斥压单/polish-20260824-en/
  - 辅证（不替代合同路径）：visual/html-game-rule-horror/polish-20260824-zh|en/

## 总评

本地 http 上主循环可走完：摊副驾公文、点真互斥对、按路单身份长按压单、交钥匙。中英均可走到 `xi-pass`（喜车过门 / Joy car through）与近答案 `listen-guest`（听客人 / Heard the guest），工钱句不同，不是同一收口。

制作人自报 D1「第五晚四份纸要下滚」**不构成本局 largest_gap**：1280×800 第五晚，身份钮、压源、长按、交钥匙与两对互斥条款同屏可点；铅印/铅笔纸身顶部被驾驶座静帧吃掉，不等于主操作不能同屏完成。按开评指令：能同屏完成摊纸·标对·压单，不因未同框四张纸全文就 REWORK。

## 开评条件

- 运行截图：有。合同路径中英各通用 01–12 + 专属 13–22，且有 before 成对。
- 许可表：`_视觉美化/许可证.md` 非空。
- 烟测 7 步：制作人有记录；本局在 8856/8857 独立复跑关键步。
- 制作人 `contract_status=CANDIDATE`，`review_status` 留空，`dimension_verdicts` 全 UNTESTED。开评后不再留 UNTESTED。

## 玩法标本相对基线

对照 `/Users/jianglong/Desktop/新游戏3-视觉美化前备份-20260824-2.zip`：

| 项 | 磁盘 | 判定 |
|---|---|---|
| 中英 `js/app.js` `js/data.js` `js/engine.js` sha256 | 与 zip 一致 | 七晚 DAG、互斥对、结局机未改 |
| 存档键 | 中 `guomen-state`；英 `guomen-state-en` | 与 zip 内 data.js 相同，无新键 |
| 互斥 | A `fleet-listen`×`slip-sang` 第4晚；B `sms-night`×`pencil-zishi` 第5晚 | 运行可立对 |
| jpeg | 11 张中英 sha256 与 zip 一致 | 本局未换静帧 |
| `index.html` | 相对 zip 只加装饰 class 与一层公文包裹 `section#paper-fleet` | 交互 id 未改；无玩法节点 |

控件：点条款、标对、`data-press` 压源、`#btn-hold` 长按 1500ms、交钥匙，与基线同一套。无 hover 才显主操作。

## 抽查环境（本评审自跑）

- 中文 `http://127.0.0.1:8856/` Python http.server，根=中文版
- 英文 `http://127.0.0.1:8857/` 根=英文版
- 系统 Chrome headless 走进站；未写入游戏目录；未补截图

实测（1280×800）：

- 第1晚：只亮车队铅印 + 空格「这一格还没纸」；铅笔/流程单/短信 `display:none`
- 第5晚点 A/B：`pairMark` 立住；句「墨水咬住车队守则和流程单，门禁短信和上一班铅笔」
- 喜车 + 压 `xi_slip` 走七晚 → ending `xi-pass`，层文「喜车过门…工钱 ¥36」
- 全程压车队、不标对 → ending `listen-guest`，「听客人」不是喜车过门句
- 越权「改金桂苑门禁短信原文」→ 回句「不改金桂苑门禁短信的原文。」
- 长按中：`#btn-hold.holding` 虚线描边 + 点线 outline + 左边 inset 条 + 斜纹 `repeating-linear-gradient`，非唯色
- Tab：`#btn-enter` / `#fleet-listen` / `#btn-hold` 均为 `3px dotted rgb(230,220,200)`（`:focus-visible`）

## 六种公文皮（不读标题）

运行计算样式，中英同壳：

| 所有者 | 可指认差（涂黑标题仍在） |
|---|---|
| 车队 | 底 `rgb(216,196,138)` + 宋体 + 2px 墨框 + 歪红方印 |
| 铅笔 | 底 `rgb(239,228,196)` + 楷体 + 撕边 clip + 顶铁夹 |
| 喜条 | 底 `rgb(243,221,208)` + 左 8px 红口 |
| 丧条 | 底 `rgb(230,228,220)` + 2px 黑挽边 |
| 短信 | 底 `rgb(34,36,40)` + 厚机壳边 + LCD 绿灰 |
| 路单 | 夹板 `rgb(196,160,106)` + 红栏表 |
| 后视镜 | 5px 镀铬椭圆，不是顶栏搜词 |

不是边检亭、不是全站圆角卡、不是搜词顶栏。假互斥「同一段换标题」专拍（19）仍是第五晚桌面，不是换题实验；六皮 chrome 已互异，不升为本条返工。

## D1 第五晚四纸（不照抄制作人 gap）

制作人 largest_gap：1280×800 四份公文仍要下滚才能同时看完铅印全文。

本局核过 1280×800 第5晚：

- `#btn-hold` top 376–425 在视口内；`#role-xi`、压源、`#btn-key`（底边 800）在视口内
- `fleet-listen`、`pencil-zishi`、`slip-sang`、`sms-night` 可点矩形在视口内
- 铅印/铅笔纸身 top 为负，上半被驾驶座静帧挡住——看全文要滚
- 主操作（摊开后的条款、标对、身份、压源、长按）同屏可完成

结论：四纸未同框全文 **不阻断**「摊纸标对压单」。D1 PASS。返工禁止去砍玩法或为塞四张纸改 DAG。

## 十四维正式判定

制作人自检不得写 PASS。下表仅为独立评审人填写。

| 维 | 判定 | 依据 |
|---|---|---|
| D1 首帧类型识别 | PASS | 01 进站：驾驶座静帧 + 锁句「没有一份全对」+「进站」。进站后 02：副驾铅印与路单夹板同屏。3 秒能说出摊纸对照再压单，不靠标题声明类型。制作人 D1 四纸滚动见上，不阻断。 |
| D2 信息层级 | PASS | 镜/波段/静帧为舱；人·晚·维在上；主循环在副驾纸 + 路单栏；对讲句在下。不是一张奶油卡。 |
| D3 风格合同一致 | PASS | 抽 01 / 02 / 15：同一正视驾驶座、同一乙烯底 `#141612`、纸色即材质。六皮见上表。 |
| D4 资产完整度 | PASS | 合同 CSS 两份在盘；jpeg 11 张有效引用且相对 zip 字节未改。主循环对象是 DOM 公文+钮，无临时代换矩形冒充。 |
| D5 关键状态可读 | PASS | 空：第1晚 `slot-empty`。错：越权回句。成：`xi-pass` 收口层。败：辅证 `12-late-ending` 来不及。选中：条款折角+左边条；喜圆点/丧横杠。禁用：未亮槽斜纹虚线。加载/暂停合同 N/A。成功/失败不同句。合同路径 12 实为第1晚空桌，错误态由 http 复核，不单开返工。 |
| D6 动作反馈 | PASS | 点条款 → 袋/折角；点成对 → 墨句；长按 → 斜纹铺满（预算内 CSS 过渡 1500ms）；交钥匙进下一晚或收口。近答案 `listen-guest` 另有画面与句，不是 `xi-pass`。不唯色、无音频依赖、不靠 hover。 |
| D7 色/光/字形 | PASS | 三锚钉在 CSS；强调 `#c45c3a` 只绑长按。字形不是玩法单位（非 VP5）。 |
| D8 空间维度 | PASS | 合同 2D；`true3d_exception.enabled=false`。无伪 3D 主循环。 |
| D9 响应式可操作 | PASS | 320：主钮 `min-height` 44–48，实测 hold 48.8×274、条款 ≥44。390 同。窄屏改为单列，hold 在叠放下方，滚动可及，不是 hover 才出现。真机手指未测。 |
| D10 性能预算 | PASS | 中英 jpeg 各约 1.0MB，整包可玩目录各约 1.1MB，落在 VP1 媒体锚 ≤8MB。无远程字库。首包秒表未测，见 did_not_prove，未超预算。 |
| D11 无障碍底线 | PASS | 本局烟测 7 步见下。设置未写防癫痫承诺。色觉模拟器未测。 |
| D12 原创与许可 | PASS | 许可表有路径/来源/许可证/可否再分发/是否 AI。CSS 本局手写；jpeg 既有随包。无商标/品牌字/官方切图当主视觉。 |
| D13 截图证据链 | PASS | 通用 12 + 怪谈专属 8 中英均在合同路径，为 http 运行 PNG，有 before/after。路径多 `html-game-rule-horror/` 前缀，按同类交卷惯例不单独 HOLD。若干专属帧复用（13=16=17 中文 after 等）仍能看出并排皮与四纸。07 静帧焦点环不醒目，本局 Tab 已量到 3px 点线，不升为返工。 |
| D14 管线匹配 | PASS | VP1 CSS 文书壳；无逐张新 AI 提示词；静帧未重绘。未画成边检桌或搜词顶栏。 |

## 截图矩阵（合同路径）

通用 12：01 进站、02 主动词、03 成功（视口裁到第7晚桌；收口全文见辅证 `03-xi-pass-ending` 与本局 http）、04=20 近答案听客人、05 恢复、06=08 320、07 键盘（静帧弱、http 补）、09=10 减动/非色（长按形差以 15 为准）、11=12=21 第1晚空/未摊全、专属 13/14/15/17/18/19/22 齐或复用仍可读。

before/after 成对。并排看，去掉驾驶座装饰静帧不会丢掉条款与压单——静帧不是本轮新加的主循环对象。

## 体积

- 媒体 jpeg：中英各约 1004KB
- 可玩整包（中文版/英文版目录）：各约 1.1MB
- CSS ≈15KB；js 与 zip 同位
- visual 证据不计游戏媒体
- first_interactive 秒表未测

## 烟测 7 步（独立复核，8856/8857）

1. 320：条款/身份/hold/key ≥44 CSS px，可点。hold 在单列下方，需滚到，仍可完成压单。
2. 只键盘：进站钮 Tab 可见 3px 点线；进桌后 Tab 到 `fleet-listen`、再落到 `btn-hold`，同一套点线。
3. 真机触控未测。窄屏点击代替。
4. 包内无音频；静音同路径。
5. `prefers-reduced-motion: reduce`：CSS 取消 hold width 过渡，斜纹立刻满，左边条与虚线仍在。
6. 长按：斜纹+虚线+inset 条；选中：折角。不唯色。
7. 无整屏 >3 次/秒闪烁。

## largest_gap

无。

制作人交卷仍写 D1 四纸滚动。本评审按 **1280×800 第5晚运行几何与 15/17 运行图** 判定：摊纸标对压单可同屏完成，D1 通过。不把制作人自谦自动写成正式缺口。

## 证据链残留（不构成未通过，不另开返工）

- 合同路径 03 视口未框住收口层全文；`xi-pass` 由辅证全页与 http 文案承担。
- 12 与 11/21 字节级相同，不是越权回句帧；错误态以 refuse 句为准。
- 19 不是「同一段换标题」实验。
- `index.html` 相对 zip 多了公文 `section` 包裹，属装饰结构，js 未改。
- 英文本地化存档键 `guomen-state-en` 是基线既有，不是本局新迁。

返工禁令：以上残留不得拿去「顺便再美化全局」、不得改 js、不得为四纸同框改玩法。

## did_not_prove

- 真机 Safari / 真机触控
- 色觉模拟
- 首包可交互实验室秒数
- 真机帧率
- 假互斥「同一段换标题」单独实验
