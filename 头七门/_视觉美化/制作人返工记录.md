# 头七门　制作人返工记录　20260824-d1　D13

contract_status: CANDIDATE（未升 PASS）
review_status: 不改。独立评审文件仍 REWORK；制作人未填评审结论。

largest_gap 本轮只修：D13　开评矩阵 `07-keyboard-focus.png` 与 `04-near-fail.png`、`21-near-fail-recoverable.png` 字节相同，画面无 :focus-visible 点线。

## 未改

游戏 HTML / CSS / JS / jpeg 静帧、玩法、存档键、除 `07-keyboard-focus.png` 以外的全部截图一律未动。未扩面、未改 `独立评审.md` 的 review_status。

## 做了什么

8842/8843 开评时已空，从原目录重启：`http://127.0.0.1:8842/index.html`（中文版）与 `http://127.0.0.1:8843/index.html`（英文版）。Google Chrome persistent profile `/tmp/touqi-d13-kb-profile`。未调用 `element.focus()` / `page.focus()` / JS 设焦。

开评矩阵（d1，视口 1280×900，fullPage）：清 `touqi-state` → 进站 → 对照流程页 → 拒之吴成山（与 04 近失败同输入态）→ 鼠标点非控件 `#lock-line` 卸焦 → **键盘 Tab** 走到 `#btn-next`（下一晚）。`matches(':focus-visible')===true`，计算样式 `outline: rgb(232, 223, 208) dotted 3px`、`outline-offset: 2px`。像素取样按钮上沿外可见纸色点 `(232,223,208)`。

双语矩阵两边都有同名 07，各重截一张（视口 1280×800，封面）：点 `p.lock-line` → **键盘 Tab** 一到 `#btn-enter`（中文「进站」／英文 “Clock in”），同样 3px 纸色点线。

只覆盖：

- `visual/告别厅门岗成组放进拒之/20260824-d1/07-keyboard-focus.png`
- `visual/html-game-rule-horror/polish-20260824-zh/07-keyboard-focus.png`
- `visual/html-game-rule-horror/polish-20260824-en/07-keyboard-focus.png`

d1 无独立英文 `07`，未另造文件。

## 哈希对照（开评矩阵）

| 文件 | MD5 | 相对近失败 |
|---|---|---|
| 07-keyboard-focus.png | `51cc5865b644b42f18f51df652f6fe44` | 不同 |
| 04-near-fail.png | `28669f4bf9b983cd256195963b920ab0` | 开评原字节，未改 |
| 21-near-fail-recoverable.png | `28669f4bf9b983cd256195963b920ab0` | 与 04 仍相同（本轮不重截） |

旧 07 曾与 04 同 MD5 `28669f4bf9b983cd256195963b920ab0`。新 07 SHA256 `77ce8eac4038ff7db2ecb3e59c8e868699fee8f47087c7c5fa0de2b1553501e4`；04/21 SHA256 `acf441dce30805173af3ec135fda184b2a9a428ad913662803ff5d8341fada0a`。

双语 png：zh 07 `e59f033869d64d98ad611334a072b39c` ≠ 04 `ca6b676e600a69b06a6b4455a081c563`；en 07 `9d3430f20acfa955b2a57576f370a8f5` ≠ 04 `75e663e53b1068a8d72f6f43f32a49a1`。

## 交卷栏

- contract_status 仍 CANDIDATE
- review_status 空栏未填
- 制作人不得写 D13 PASS

制作人只证明：07 已是运行中 Tab 焦点帧，且与近失败帧字节不同。正式维判定留独立评审人。
