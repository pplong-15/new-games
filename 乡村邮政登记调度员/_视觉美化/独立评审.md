# 乡村邮政登记调度员　独立评审　polish-20260823

reviewer_role: 独立评审人（未参与本局制作；未改 HTML / CSS / JS / 图）
producer_role: 制作人机器人（合同所载；与评审人不是同一角色）
contract_status_observed: CANDIDATE
review_status: **PASS**
genre / genre_profile / pipeline_id: html-game-job-sim-explorer / 乡镇邮政 MIS 六站夜班 / VP1
space_dimension: 2D
distribution_form: A（单文件 HTML）
play_specimen: Papers, Please（只借规则/证据/裁决同屏；不借边检护照格）
largest_gap: 无

本评只读实现与交卷证据，只写本文件。未改 HTML / CSS / JS / 图片 / ref，未替制作人补截图。制作人 `CANDIDATE` ≠ 通过。

---

## 0. 开评范围与方法

审核对象：`/Users/jianglong/Desktop/继续美化/乡村邮政登记调度员`（中文版 / 英文版 / `_视觉美化/` / `visual/`）。  
对照：视觉合同、制作人交卷、licenses.md、风格圣经、截图矩阵；基线 zip `/Users/jianglong/Desktop/继续美化-视觉美化前备份-20260823.zip`；ref `/Users/jianglong/Desktop/继续美化/_视觉美化工作/ref/乡村邮政登记调度员-未引用图/`。

制作人已开本地 http（无 iframe）：

- 中文 `http://127.0.0.1:8834/`（Content-Length 317924，对上现 `中文版/index.html`）
- 英文 `http://127.0.0.1:8835/`（Content-Length 322513，对上现 `英文版/index.html`）

本评未另开端口。用本机 Chrome headless CDP 走中英登录、键 1–6 切站、扫描、320/390、focus-visible、减动。制作人自报只作对照，不以自证代替抽查。

---

## 1. 合同与交卷完整性

| 项 | 判定 |
|---|---|
| 合同必填字段 | 齐。genre / VP1 / 标本 / 三色 / 材质 / 分发 A / 2D。制作人未写 PASS。 |
| 制作人交卷 | 齐。`contract_status=CANDIDATE`。 |
| 许可表 | 非空。见 D12。 |
| 体积 | 本评 `du`：中文版玩家目录 508K（index 317924 B）；英文版 524K（index 322513 B）。远低于 VP1 媒体锚 8MB。 |
| 烟测 7 步 | 交卷有记录；本评按步复跑。见 D11。 |
| 通用 12 | 01–12 中英均有 before/after 成对文件。 |
| 岗位专属 ≥8 | 13 四区、14 结局A、15 班后第二夜、16 citation、17 警告可改、17b–20b 六站壳、14b 案卷离开、18b/20c 辅助。去重后仍 ≥8。 |

CANDIDATE 前置满足。开评后 D1–D14 不再留 UNTESTED。缺运行图 / 许可表 / 烟测才会 HOLD：三者都在。

---

## 2. 玩法锁：相对基线 zip

对 zip 内 `继续美化/乡村邮政登记调度员/{中文版,英文版}/index.html` 拆 `<style>` / `<script>` / 其余：

| 块 | 中文 | 英文 |
|---|---|---|
| `<script>` | **与基线逐字相同**（252331 B） | **与基线逐字相同**（275874 B） |
| `<style>` | 仅增 `polish-20260823` 段（+4458 B） | 中英 polish 段 **逐字相同** |
| body 站壳 | 六站补 `chrome-{scan,wms,roster,crt,oa,tl}` class | 同 |

抽查未改：

- `data-act` 集合中英相同，相对基线 **zero extra / zero missing**（含 `login` `scan` `confirm-mail` `decide-cancel` `decide-pend` `tab` `ending-A` 等）。
- `showEnding("A"|"B"|"C"|"D")` 分支仍在；`data-act="ending-A"` 仍在。
- `localStorage` / `sessionStorage` / `indexedDB` 源码计数 0。现场抽查 `localStorage`/`sessionStorage` 键数 0。
- 内嵌 `data:image/jpeg` **10 条**，中英相同，与基线 **逐条相同**（未替换证件照 / 结局旧照）。
- 键：`e.key >= "1" && e.key <= "6"` → `switchStation`；Esc / 空格仍在 script（script 未改）。

未引用大图：玩家包 PNG/JPG **0**。ref `MANIFEST.json` **40/40** 与 zip 同源 SHA 一致、缺 0。HTML 不引用 `assets/*.png|jpg`。迁出成立、可逆。

---

## 3. 本评 http 抽查（8834 / 8835）

| 项 | 中文 8834 | 英文 8835 |
|---|---|---|
| 登录 | 开机结束后「青石镇邮政所 · 登记调度系统 v2.31 / 夜班终端登录 / 工号 / 登录」。登录钮高 **44px** | 「Qingshi Town Post Office · Registry Dispatch System v2.31 / Night-shift terminal login / Log in」。钮高 **44px** |
| 入班 | 点登录 → 桌面。「扫描条码」**44px**。队列可见王桂兰等培训件 | 六页签 Inbound Scan / Sort / Dispatch / Address Book / Returns / Delivery Receipts。「Scan barcode」 |
| 六站壳 | 键 1–6 切站，实测底/标题栏/字体至少三项差：扫描冷蓝灰 Tahoma；分拣仓绿 Segoe；花名册牛皮纸宋体；CRT 黑底绿字 Courier；OA `#ece9d8` + `#003c74`；回执运单蓝 Arial | 同套 `chrome-*` class |
| 扫描 | 点 `data-act=scan` 后可点「确认处理」**44px**（培训件，非注销件） | — |
| 注销/近失败 | 本评未把时钟推到 23:14 沈秀兰件。以运行截图 04/10/16 为准：确认注销 vs 标记待核验，非死档 | 英 04 成对存在 |
| 结局 A | 源码 `showEnding("A")` + 截图 14「干净的记录」+「已执行批量注销」 | 英 14 成对 |
| 320 | `innerWidth=320`；页签/扫描/确认 **44–45px**；列表行 56px；`overflowX=false`；页签可横滑 | 截图 06 成对 |
| 390 | 主钮同样 ≥44；速度条 22px 为次级控件，不挡本维 | 截图 08 成对 |
| 键盘 1–6 | 本评 `Input.dispatchKeyEvent` 1–6 切到 st1–st6 | script 未改 |
| focus-visible | Tab 后 `.tab` outline `rgb(10, 36, 106) dotted 2px` | 截图 07 成对 |
| 减动 | `prefers-reduced-motion: reduce` → `.scanline` `animationName=none` 仍 `display=block`、停在床中 | 截图 09 |
| 存档 | ls/ss = 0 | — |

未用 `showEnding()` 注入、未改 Runtime 状态机。扫描走真实 `data-act=scan` 点击。

---

## 4. 截图矩阵（D13）

路径：`visual/html-game-job-sim-explorer/polish-20260823-{zh|en}/`，符合 `visual/{genre}/{build_id}/`。构建闸为 http。成对文件名为 `{nn}-{slug}-before.png` / `-after.png`（本批惯例，不挡）。

- 通用 12：中英 before/after 齐。01 boot before **等于** after（开机层本轮未改壳，可接受）。
- 岗位专属：13 四区、14 结局A、15 第二夜桌、16 规则+字段+裁决句、17 待核验可改、17b–20 / 20b 六站、14b 案卷离开，去重后仍 ≥8。
- 残余（不升 HOLD）：中文缺 `14b` / `18b` / `20c` 的 before；`18b-options-not-color-spoiler-after` 与 `20c-next-item-gated-after` **同 SHA**（英文 18b/20c 的 before 亦同哈希）。选项不唯色已由 04/10 的虚线边+文案+删除线承担；上件未交下件由 20c 文案与玩法锁证明。

before/after 同机位对照：改的是站壳/44px/焦点，不是玩法标本。

---

## 5. D12 逐项（不机械升 HOLD）

对照 skill 第 9 节停止条件，以及上一批《夜班》口径：诚实 UNSET + 无商标主视觉 + 本局无新图 → 本维 PASS，来源链进 `did_not_prove`。

| 行 | 判定 | 理由 |
|---|---|---|
| 中英 `index.html` 本局 CSS 窗框/六站壳 | 可随包 | 本局新写样式，许可表已登记 |
| 系统字体栈 | N/A | 不随包嵌字形 |
| 迁出 PNG/JPG | N/A（不进玩家包） | 40 项 SHA 与 zip MATCH；HTML 零引用 |
| 内嵌证件照 / 结局旧照 / 安检 JPEG data URL | **UNSET 来源链** | 表已登记；许可证 UNSET；本轮 **未替换**（与基线 10 条 data URL 相同）；无中国邮政等商标/官方切图/品牌字当主视觉；主画面是 Win2K MIS 壳。未见主视觉盗用或商标成立 |

D12 **PASS**。发行渠道若要可再分发证明，UNSET 人像仍须另取授权；那是渠道闸，不是本维 largest_gap。

---

## 6. 无障碍 7 步烟测（开评后复跑）

| 步 | 判定 | 依据 |
|---|---|---|
| 1 缩到手机宽主按钮可点 | PASS | 320：页签/扫描/确认 44–45px，overflowX=false |
| 2 只键盘走到第一个主线点 | PASS | 键 1–6 切站；Tab 后 dotted focus。未要求拍完全程无鼠标通关（制作人已写入 did_not_prove） |
| 3 只触控走同一段 | PASS（桌面代理） | 390 主目标 ≥44。本局主动词是点选/扫描，不宣称移动手势。真机手指 → did_not_prove |
| 4 关声走同一段 | N/A | 无音频 |
| 5 减弱动效后主反馈仍在 | PASS | 扫描线停中仍可见，不靠闪完成 |
| 6 状态不唯色 | PASS | 待核验虚线边+文案；注销实线/加粗；完成删除线 |
| 7 无整屏 >3 次/秒闪 | PASS | 无整屏频闪；减动后动画 none |

---

## 7. D1–D14（正式）

| 维 | 判定 | 依据 |
|---|---|---|
| D1 首帧类型识别 | **PASS** | 开机日志即「邮政所登记调度」；登录后扫描队列+扫描条码，不靠标题也能读出夜班柜台叫件裁决 |
| D2 信息层级 | **PASS** | 页签/队列主操作，时钟与待核验为状态，通讯为叙事；主循环在扫描/分拣台 |
| D3 风格合同一致 | **PASS** | 正对 2D；窗框蓝+系统灰+CRT 绿；抽扫描/花名册/CRT 三站同一 Win2K 家族、角色:证件照服从窗框 |
| D4 资产完整度 | **PASS** | 主循环对象是 DOM 控件+原内嵌照，无灰盒冒充；未引用大图已迁出 |
| D5 关键状态可读 | **PASS** | 开机、登录、扫描中、合规确认、异常裁决、暂停（12）、结局 A（14）可分；成功/失败不同帧 |
| D6 动作反馈 | **PASS** | 点扫描条码 → 扫描床/状态变；点确认处理 → 队列「已处理」。近失败 04 注销/待核验不是正解也不死档（05 可继续） |
| D7 色/光/字形 | **PASS** | 三锚+`css-win2k-mis-shell`；强调绿绑站4；非唯色边框/删除线。非 VP5 |
| D8 空间维度 | **PASS** | 合同 2D；`true3d_exception.enabled=false` |
| D9 响应式可操作 | **PASS** | 320/390 能完成扫描/确认；主操作 ≥44；不依赖 hover。真机手指 did_not_prove |
| D10 性能预算 | **PASS** | 玩家包 ≪ 8MB；本地 http 可交互；无远程大资源。秒表/帧率未测 → did_not_prove |
| D11 无障碍底线 | **PASS** | 7 步见上；静音 N/A |
| D12 原创与许可 | **PASS** | 见 §5。人像链 UNSET → did_not_prove，不升 HOLD |
| D13 截图证据链 | **PASS** | 通用 12 齐；专属 ≥8；http 运行帧。ZH 三张 before 缺与 18b/20c 同哈希为残余，不阻断 |
| D14 管线匹配 | **PASS** | VP1 真实界面；本局无新 AI 出图；未改道 |

---

## 8. did_not_prove（非阻断）

- 真机触控 / 色觉模拟 / 首包秒表 / 运行帧率
- 内嵌证件照与结局旧照的完整库存或拍摄授权链（许可表 UNSET）
- 键盘 1–6 以外的全程无鼠标通关
- 本评未亲手把时钟推到沈秀兰注销件；近失败以 04/05/16 运行帧 + 源码 `decide-cancel`/`decide-pend` 为准
- 中文 14b/18b/20c 缺 before；18b 与 20c after 同哈希

---

## 回传

- **review_status: PASS**
- **largest_gap: 无**
- **报告路径:** `/Users/jianglong/Desktop/继续美化/乡村邮政登记调度员/_视觉美化/独立评审.md`
- **证据:** `visual/html-game-job-sim-explorer/polish-20260823-{zh,en}/`；`licenses.md`；本评 8834/8835 CDP 抽查；基线 zip script/人像 MATCH；ref 40 项 SHA MATCH
