# 地券密级　独立评审　polish-20260823

reviewer_role: 独立评审人（未画本局主界面 / 主图集 / 主 SVG）
producer_role: 制作人机器人（本局处理静帧 / 改 CSS 与装饰 class / 交运行截图）
review_status: PASS
contract_status_seen: CANDIDATE
reviewer_role ≠ producer_role: 是

抽查运行：http://127.0.0.1:8924/ （中文版根）与 http://127.0.0.1:8925/ （英文版根）。无 iframe。Cursor 内嵌浏览器本局无法开 `127.0.0.1`（navigate/lock 均报无 tab），不计入制作证据。评审抽查用本机 HeadlessChrome CDP（调试口 9364），同源 8924/8925。

---

## 交卷门

- 视觉合同存在。`genre=html-game-puzzle`，`genre_profile=三密级字段对照`，`pipeline_id=VP1`，无改道。`space_dimension=2D`，`true3d_exception.enabled=false`。`visual_capabilities` 是眼睛完成的对照句，不是氛围词。`contract_status=CANDIDATE`。制作人未填 `review_status=PASS`。
- 通用 12 中英 PRESENT 成对；专属 13–35 PRESENT 成对；`搜索命中另开页=N/A` 且有理由（死检索）。许可表非空。制作人 D1–D14 写自检待评，无 PASS。
- 制作人不得写、也未写总评通过。

## 许可

许可表非空：`game-extra.css` 本局手写、随包、非 AI；中英 HTML 装饰 class 同口径；9 张 JPEG 原包可再分发，本局仅 `flatten_stills.py` 离线压饱和/顶光灯箱，无新提示词。无远程 CSS/字体/图（中英 html/css/js 无 `http(s)` / `@import` / Google fonts）。无商标切图、无品牌字当主视觉。静帧无可读口令/工号（24 / VISUAL 清单）。

## 体积（玩家包 vs 证据目录）

独立复算，口径与合同一致：**不含** `visual/` 与 `_视觉美化/`。

| 口径 | 字节 | 文件数 |
|---|---|---|
| 中文版 | 501365 | 79 |
| 英文版 | 526919 | 81 |
| 玩家包 zh+en | **1028284**（与 `bundle_target` 逐字节一致） | 160 |
| JPEG 媒体 | 中英各 **318849** | 9+9，像素哈希中英全同 |
| `visual/` 截图 | 19266928 | 140 |
| `_视觉美化/` | 3441493 | 18 |
| 整棵游戏目录 | 23736705 | 318 |

若把证据目录算进发布包，会把 **~1.0MB 玩家包** 报成 **~22.7MB**。制作人没有这样做。VP1 媒体锚 8MB；实测 JPEG ≪ 锚。帧率/首包秒表未测 → `did_not_prove`，不把预算当实测。

## 逻辑哈希与锁（相对基线）

对照 `/Users/jianglong/Desktop/github游戏/_视觉美化工作/基线清单.sha256`。基线 161 项中 **121 MATCH / 40 CHANGED / 0 缺失**。新增仅 `visual/` 140 + `_视觉美化/` 18，没有混进玩家页。

| 锁 | 结果 |
|---|---|
| 中英 `js/state.js` | **MATCH**。键 `anpu-diquan-miji-v1` / `anpu-diquan-miji-v1-en`；`CRED` / `BLOCKS` / `pickEnding` 未改 |
| 中英 `desk/home.html` | **MATCH**。死检索 `action="home.html"` |
| 中英 `css/game-extra.css` | CHANGED（预期，且中英哈希相同） |
| 中英 `img/*.jpg` | CHANGED（灯箱处理，预期） |
| 列出的 HTML | 仅装饰 class；`data-extract` 值未改。`deed-012` 无写入钮、无 `paper.jpg`、无 `dq-scan` |

相对备份 zip 的 HTML 差分：引言只加 `dq-scan-hero`；047 三层只加 `dq-scan` / `dq-layer-*` / `dq-scan-table` / `dq-extract-row`；012 只加 `dq-layer-other`；登录/锁/denied/note 只加匣 class；`result.html` 只按已有 `s.ending` 写 `dq-end-*`，不改判定句。

死搜框抽查：登录页 `action=login.html` GET；提交 `q=047` 后仍停在 `/desk/login.html?q=047`，登录盒还在，不是结果页、不是模态。

## 通用 12 + 解密专属 + before/after

路径：`visual/三密级字段对照/polish-20260823-{zh|en}/{nn}-{slug}.png`，成对 `-before`。中英各 35 after + 35 before = 140。`measures-after.json` 70 条路径/字节/宽高与文件 **0 mismatch**。截图来自 http + HeadlessChrome，不是 file://、不是设计稿。

- 01 / 13 字节全同：引言无搜框、抽屉灯箱。进站钮在 07 帧（01 未滚到）。CDP：引言无搜控件，进站钮高 44。
- 02 公开 047：券表 + 五枚写入同屏。
- 03 three：`■已收　三层对照进表` + 左实条（`dq-end-three` 的 inset 条活着）。英 03 同句。
- 04 overclaim：`×退回　请改亡者栏`；复评后计算样式为 `3px double`。英 04 同。
- 05 overclaim 后备注台仍可改；限制选项有选中 ■。
- 06 320 公开 047：`overflowX=false`，`scrollW=clientW=320`。写入钮高 44，但 **y≈1197，不在 720 视口内**——06 帧本身没拍到写入行。320 可点由 32 + 本局 CDP 补证。
- 07 引言「打开信息公开网」3px 点线焦点。相对 01 为滚到按钮区，不是同像素焦点差。
- 08 / 32 / 33：390/320 登录与 390 备注。CDP：工号/口令/进入/死检索/备注 `label.dq-opt` 高均 44；原生 radio 18px 包在 44 的 label 里。
- 09 vs 17：全图像素差 bbox `(997,125)-(1055,170)`，即戳区。CDP：无减动 `rotate` 矩阵；`prefers-reduced-motion: reduce` 时 `transform=none`，「公开」戳仍在。
- 10 备注选项：公开实线 / 内部虚线+左条 / 限制双线 / 对照点线，不唯色。
- 11 空备注五栏「无可写的句子」，不是白屏。
- 12 错口令：句 + toast。本局复跑 `xx/xx`：中英均停在 login，有错句和 toast。
- 14 公开目录 012 与 047 分列，文案禁止把 012 抄进 047。
- 15 值班台死检索同页。
- 16 denied：题「这扇门还没开」，非黑红 CRT；复评后计算样式为 `2px dashed`。
- 17/18/19 同源 047：公开 1px 实线+纸纹+斜戳；内部 2px 虚线+左条+灯箱条；限制 3px 双线+四角 L（`background-size` 18×3 / 3×18）。字段各层不同句。
- 20 文保所红头；21 乡谈蓝头；22 邮箱 2010 三栏；23 百科；28 晚报；29 黄页；31 橙博客。所有者可分家。无单帧五套蒙太奇（制作人已写入 did_not_prove）。
- 24 047 静帧无可读 token。25 012：`dq-layer-other` 点线表，无灯箱 `::before`，无 `data-extract`。26 内部登录虚线+左实条。27 限制登录双线+四角 L（复评落地）。
- 30 旧档：`time-public` / `money-public` 仍勾，n=2。本局复跑一致。

类型追加：两源并排做成 20 单源红头 + 14 公开卷分列，不是同帧并排。搜索命中 N/A 理由成立。隐藏页异皮有 21/22。静帧不露 token 有 24。

## 7 步烟测（制作人记录 + 本局 8924/8925 复跑）

1. 缩到 320 主按钮可点：06/32 已拍。本局 320：写入 79×44、登录提交 254×44、链高 44，`overflowX=false`。
2. 只键盘第一个主线点：07 点线焦点。未拍 Tab 走完三层对照（制作人已写入 did_not_prove）。脚本 `.focus()` 不代替 Tab。
3. 只触控同一段：08/33 为 390 模拟。备注 label 高 44，radio 视觉 18。真机手指未测。
4. 关声：无音频，N/A。
5. 减弱动效：09 像素差在戳；本局减动下戳 `transform=none`，字仍在。
6. 状态不唯色：三层券表线型差已落地；备注选项线型差已落地；回执 ■ 对 ×。数字化室匣线型复评已落地。
7. 无整屏频闪：`getAnimations()=[]`。

开评后 7 步有记录且本局复跑关键步，D11 不因匣线型失败而 HOLD。

## 抽查：中英 three / overclaim / 错登录 / 旧档

| 步 | 中文 8924 | 英文 8925 |
|---|---|---|
| 公开/内部/限制各写入 5 | extracts `[5,5,5]` | 同 |
| 内部登录 | → `/desk/home.html` | 同；中文键未被写入 |
| 限制 047 | 标题「限制层　047」 | Internal/Restricted layer · 047 |
| 对照交 three | `ending=three` `closed=true` 类 `dq-end-three` | 同句英文 |
| 亡者 `dead-unique` | `ending=overclaim` `closed=false`；再开 note 表仍在 | 同 |
| 错口令 | 句+toast，仍在 login | 同 |
| 旧档 time/money-public | UI 仍勾，n=2，键仍是 `anpu-diquan-miji-v1` | `…-v1-en` |

玩法未回归。不 HOLD 玩法。

## 抽查：047 同源且密级非唯色；012 未误标

本局 CDP 计算样式：

- 公开表 `border=1px solid rgb(51,51,51)` + `paper.jpg` + 戳「公开」且默认有旋转矩阵。
- 内部表 `2px dashed` + inset 10px 实条 + 灯箱 `::before` 引用 `paper.jpg`。
- 限制表 `3px double` + 四角 L 的 `background-size`。
- 012：`articleClass=record dq-layer-other`，点线表，无 `dq-scan`、无灯箱、无写入、无 `paper.jpg`。

中英同一套 chrome。密级主证据在券表与备注选项，不靠换色。

## 抽查：320 / 390 / 44px / 键盘 / 减动

已见上。320 写入在折行后仍 44、无横滑。390 备注 20 条 label 最小高 44。减动戳不斜。07 证明第一主线点焦点可见。

---

## dimension_verdicts（正式，仅评审人）

D1: PASS　开局人/今晚差事/047 五栏/进公开网，不靠标题声明类型。证据 01/13 + CDP 无搜框。
D2: PASS　主操作在券表写入与备注选项；值班台是驾驶座；皮上检索死摆设（15/死搜抽查）。
D3: PASS　正视 2D 与所有者分家成立。D3 复评：限制登录 `3px double` + 四角 L 渐变、denied `2px dashed`、overclaim `3px double` + `×` 在 8924/8925 计算样式稳定出现；中英 27/16/04 after 帧能指认。内部登录左条未回归。
D4: PASS　无新临时代换矩形。主循环对象仍是旧表+写入钮。灯箱 JPEG 有文件与引用。匣规则写在 `game-extra.css` 但被壳盖掉，记在 D3，不另开资产缺失。
D5: PASS　空备注、登错句+toast、three/overclaim 题形 ■/×、选中 ■、无暂停无加载（合同 N/A）。成功失败不同帧。denied 虚线匣与 overclaim 双线匣复评已落地。
D6: PASS　点写入有 toast；交 three / overclaim 立刻换回执句；近答案可回改。不靠 hover、无音频依赖。
D7: PASS　三锚已钉；强调色绑登错/退回，不绑哪一层为真。字形非玩法单位。
D8: PASS　2D；true3d_exception false。
D9: PASS　320/390 模拟下主动词控件高 44、可点、不靠 hover。06 未拍到写入行，以 32 与本局 CDP 为准。真机触控未测 → did_not_prove。
D10: PASS　玩家包 1028284、JPEG 318849，远低于 VP1 锚；有本地 http。证据目录未计入玩家包。帧率/秒表未测 → did_not_prove。
D11: PASS　7 步有记录；本局复跑 320/减动戳/不唯色券表/无频闪/死检索。静音 N/A。未证明只键盘通关、未真机触控。
D12: PASS　许可表非空；随包可分发；无商标主视觉；无新远程；无新 AI 图。
D13: PASS　通用 12 齐、专属 ≥8 齐、搜索命中 N/A、路径合规、为运行截图、before/after 成对。五套并排无单帧蒙太奇，制作人已写入 did_not_prove；序列 20/21/22/23/31 可指认所有者，不升为本局唯一 gap。
D14: PASS　VP1 真网页多所有者壳；无新 AI 图；未画成边检亭或全站圆角卡。死检索未做成搜词主循环。

---

## D3 复评（本轮只核匣线型与 16/26/27/04）

8924/8925 现服 `game-extra.css` 哈希 `2310359b72f3ae39`，与磁盘中英同一份，含 `#wrap form.login-box.dq-layer-restrict` / `#wrap div.notice.dq-denied` / `#wrap div.notice.dq-end-overclaim`。HeadlessChrome CDP 9364 复测：

| 匣 | 中文计算样式 | 英文计算样式 | after 帧 |
|---|---|---|---|
| 限制登录 27 | `3px double rgb(51,51,51)`，`background-image` 有 linear-gradient，`background-size` 16×3 / 3×16 | 同 | 双线 + 四角 L + 浅斜纹 |
| denied 16 | `2px dashed rgb(51,51,51)`，题「这扇门还没开」 | 同，题 This door is still shut | 黑虚线匣，非 CRT |
| overclaim 04 | `3px double`，`h2::before="× "`，题「退回　请改亡者栏」 | 同，Returned · rewrite the wangzhe box | 双线匣 + × |

未回归：内部登录 26 现为 `2px dashed` + inset 8px 左条（比首评更完整）；公开/内部/限制券表仍为 1px 实线 / 2px 虚线+左条 / 3px 双线+L；012 仍 `dq-layer-other`、无灯箱、无写入；320/390 `overflowX=false`、写入与登录高 44；中英 `state.js` / `desk/home.html` 相对基线 MATCH。

中英 16/26/27/04 的 `-before.png` 与 after **字节全同**（差分 bbox 空）。成对不再能对照旧匣，但 after 与本局 http 计算样式一致，不升新 gap。

## largest_gap

无

## did_not_prove

- 真机手机触控（仅 Chrome 320/390 模拟）
- 色觉模拟
- 运行帧率 / 首包秒表
- 只键盘走完三层对照全路径（07 只证明引言进站钮焦点）
- Cursor 内嵌浏览器开页（制作人未依赖它；评审同样失败）；抽查用 HeadlessChrome + 8924/8925
- 五套所有者同帧蒙太奇（序列帧已能分家，不构成本局唯一 gap）
- 两源同帧并排（20 为单源红头）
- 16/26/27/04 中英 before 与 after 字节全同，成对差分被覆盖；after 与运行匣线型一致

## 下一步

PASS。D3 匣线型已在运行中稳定显示。无新的 `largest_gap`。
