# 地券密级　制作人返工　D3 匣选择器　polish-20260823

contract_status: CANDIDATE（不升 PASS）
review_status: 不改。独立评审仍 REWORK；制作人未填评审结论。

unique_gap 本轮只修：D3　`html.skin-service-cyan-desk .login-box` / `.notice` 盖掉限制登录双线+四角 L、denied 虚线匣、overclaim 双线匣。

## 未改

HTML / JS / state.js / CRED / BLOCKS / pickEnding / data-extract / 012 字段 / 死搜框 action / 存档键 / 其它视觉区。内部登录选择器 `.login-box.dq-layer-internal`、券表 `!important` 三层、`.dq-layer-other table` 原样。中英 `state.js`、`desk/home.html` SHA256 与基线 MATCH。

## CSS（中英同一份）

只抬这三条局内匣规则，落到 `#wrap` + 元素名，并给描边/L 标 `!important`（与券表同层）：

- `html.skin-service-cyan-desk #wrap form.login-box.dq-layer-restrict`　3px double + 四角 L 渐变
- `html.skin-service-cyan-desk #wrap div.notice.dq-denied`　2px dashed
- `html.skin-service-cyan-desk #wrap div.notice.dq-end-overclaim`　3px double

未动 `html.skin-service-cyan-desk .login-box` 通用规则、未动内部登录左条、未动 012。

## 重拍

http://127.0.0.1:8964/ 中文根；http://127.0.0.1:8965/ 英文根。无 iframe。HeadlessChrome CDP，profile `/tmp/dq-miji-d3-cdp-8964`。viewport 与原机位一致（04=1280×900，16/26/27=1280×800），无点击、无填口令、无写盘到游戏目录。04 只种 ending=overclaim 存档后打开 result；16 清档开 restricted-047 → denied.html。

| 帧 | 中英 after | 中英 before |
|---|---|---|
| 16 denied 虚线匣 | `16-denied-gate.png` | `16-denied-gate-before.png` |
| 26 内部登录左条（对照，规则未改） | `26-login-internal.png` | `26-login-internal-before.png` |
| 27 限制登录双线+L | `27-login-restrict.png` | `27-login-restrict-before.png` |
| 04 overclaim 双线匣 | `04-near-fail.png` | `04-near-fail-before.png` |

路径：`visual/三密级字段对照/polish-20260823-{zh\|en}/`

## 计算样式（8964/8965 after）

- 限制登录：`border=3px double rgb(51,51,51)`，`background-size` 含 `16px 3px, 3px 16px`（四角 L）
- denied：`border=2px dashed rgb(51,51,51)`
- overclaim：`border=3px double rgb(51,51,51)`，class `notice dq-end-overclaim`
- 内部登录（未改选择器）：`2px dashed` + `box-shadow inset 8px 0 #333`
- 券表：公开 1px solid / 内部 2px dashed + inset 10px / 限制 3px double；012 `record dq-layer-other`、表 1px dotted、无 `dq-scan`

## 抽查

- three 路由（种 ending=three 开 result）：class `dq-end-three`，`border-style=solid`，中英同。
- overclaim 路由（种 ending=overclaim 开 result）：class `dq-end-overclaim`，`border-style=double`，中英同。
- 320 登录 / 公开 047：`overflowX=false`
- 390 备注：20 条 `label.dq-opt`，min-height 44，`overflowX=false`
- 逻辑哈希：中英 `js/state.js`、`desk/home.html` 与 `_视觉美化工作/基线清单.sha256` MATCH。012 html 未改。

制作人不得写 D3 PASS。合同仍 CANDIDATE。

---

# D13 备份重建（本轮只补证据，不改实现）

contract_status: CANDIDATE（不升 PASS）
review_status: 不改。

unique_gap：D3 返工把中英 04/16/26/27 `-before.png` 写成 after。本轮只从美化前 zip 解出中英玩家目录到 `/tmp`，拟用 8740/8741 拍 before、8742/8743 拍 after。

**未改**当前游戏 HTML/CSS/JS/图片/玩法/存档。玩家文件相对本轮快照 `impl_changed=[]`。中英 `state.js` / `desk/home.html` 仍与基线 MATCH。

**解包成功**：zip SHA256 `4e52f66fff02b421b5da20c0974946339f3b54df9d0fbb99744b2cddd380a8c3`；只解 160 个中英玩家文件。详见 `D13-备份重建证据.md`。

**拍摄失败（两次后停止，未覆盖盘上 PNG）**

1. `Google Chrome 151.0.7922.170 --headless=new --remote-debugging-port=9388` → SIGABRT（poll=-6），CDP 未起。拟开页 `http://127.0.0.1:8742/desk/restricted-047.html`（16）。
2. Chrome for Testing 151.0.7922.34 `--screenshot` 同页 → exit 134；`chrome-headless-shell` 同页 → SEGV_ACCERR / exit 139。无 PNG。

盘上 8 组 before 仍与 after SHA256 全同。制作人不得写 D13 PASS。

## D13 父进程无座舱重拍（成功）

父进程已停止 8740–8743 的残留服务，并在无座舱环境使用系统 Chrome 151 + Selenium 4.36.0 重拍。当前游戏实现未改。

- before：不可变桌面备份只解到 `/tmp/diquan-baseline-browser/` 后经本地 HTTP 8990 / 8991 运行
- after：当前中英玩家目录经本地 HTTP 8992 / 8993 运行
- 同机位：04 为 1280×900；16 / 26 / 27 为 1280×800；每对 URL、输入态、scrollY 与浏览器一致
- 中英 04 / 16 / 26 / 27 共 8 对均满足 before SHA256 ≠ after SHA256，改变像素比例 11.17%–20.68%
- 04 用原存档结构种 `overclaim` 后正常打开 result；16 / 26 / 27 清档后正常打开 denied / internal login / restrict login
- 无滤镜、后期描边、DOM/CSS 注入或当前实现改动

详细哈希、包围盒、URL 与 viewport：

- `_视觉美化/D13-备份重建证据.md`
- `_视觉美化/d13-selenium-capture.json`
- `_视觉美化/d13-image-diff.json`

被覆盖的错误 before 已归档到 `_视觉美化/d13-overwritten-before-20260823/`，不进入截图矩阵。contract_status 仍为 CANDIDATE；制作人不写 D13 PASS。
