Start at `introduction.html`. Open the ticket page first, then the county troupe visit notice. Search is a helper, not the first move.

See `HOW_TO_PLAY.txt` for the local server command, ticket lookup IDs, and search tokens.

Ticket lookup accepts ticket number `YX-0821-19` or phone `13972810834`.
