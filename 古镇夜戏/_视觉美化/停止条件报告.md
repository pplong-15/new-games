# 古镇夜戏　停止条件报告　hold-20260823

**当前状态（2026-08-23 续闸）：停止条件已解除。** 用户授权后，中英 `pages/order.html` 各加一条相对链到既有 `pages/ending.html`。http 8970/8971 从 introduction 自然走到 order→ending，appeal 闭环成立；另三按钮在对应既有 token 状态下仍进原结局。下文第 1–9 节是解除前的 HOLD 原文，保留作历史，不删。

判定（历史）：触发技能第 9 节停止条件（主动词 / 胜负闭环无法完成）。制作人 **停止加装饰、停止出图、停止扩场景**。未改任何游戏 HTML / CSS / JS / 图片，未补链接，未删/移动 `assets/base`。

建议独立评审人开评后将 `review_status` 定为 **HOLD**。制作人 `contract_status` 只能写 DRAFT，本文件不代替评审签字。

`largest_gap`（本局唯一）：**原主动词闭环/结局不可达**

## 1. 结论（先写）

中英两版从 `introduction.html` 出发，不手输 URL，**无法到达** `pages/ending.html`，因此四结局页也不可达。

`pages/ending.html` 本体存在（HTTP 200），四按钮与 `js/ending.js` 判定都在。缺的是入链：全站 href、JS `location.href` 字符串、关键词表都没有指向它的合法入口。写「去交割那页」的 `pages/help.html` 自己也只有从结局页链回来，形成死结。

审计「结局只能手输 URL」**成立**，不是误报。本局不得进入 VP1 美化，也不得自行修 D10。

## 2. 运行入口

| 语种 | 本地 http | 起始 |
|---|---|---|
| 中文 | http://127.0.0.1:8910/introduction.html | 购票短信回执 →「打开购票页」 |
| 英文 | http://127.0.0.1:8911/introduction.html | 同结构 |

无 iframe。截图全部来自上述 http + 本机 Chrome headless，不是 `file://`。

## 3. 实走链（不手输 ending）

### 3.1 中文 8910

1. 打开 introduction：唯一 `<a href="index.html">打开购票页</a>`。dump-dom 证实 href 列表只有 `index.html`。
2. 点进首页后可走文旅壳（购票 / 夜戏 / 留言 / 剧团）与茶馆帖。
3. 顶栏检索「交割」→ 另开结果页句子：`抱歉，没有找到与"交割"相关的结果。` 零条 `<a>`。
4. 对照：检索「龙套」→ 2 条茶馆帖，仍无 ending。
5. 检索「今晚」→ 未命中。关键词表无「交割 / ending / 今晚交割」。
6. 购票页可授 `t_ticket`；剧团导航可到参观须知授 `t_visit`；票号 `YX-0821-19` 可登录订单授 `t_order`。**token 能攒，交割页仍进不去。**

Live href BFS（只跟 `<a href>` / form action，不含搜词 JS）：从 introduction 到达 **21** 个 html，集合里没有 `pages/ending.html` 或 `pages/end-*.html`。没有任何页面的 href 列表含 ending。

静态图把搜词表与 `login.js` 算进去后，从 introduction 可达 **32 / 44** 个 html，**仍无** ending。

### 3.2 英文 8911

镜像同一张图。检索 `Settlement` → `Sorry, no results related to "Settlement".` 零条链。检索 `Extra` → 两条茶馆帖，无 ending。关键词表无 Settlement / Tonight / ending。存档键 `guxz-v1-en`，与中文 `guxz-v1` 分开。

### 3.3 手输 URL 对照（只作反证，不算合法走链）

| URL | HTTP | 画面 |
|---|---|---|
| http://127.0.0.1:8910/pages/ending.html | 200 | 今晚交割 + 四按钮 + 链到 help.html |
| http://127.0.0.1:8911/pages/ending.html | 200 | Tonight's settlement + 四按钮 |
| 四结局 html | 200 | 仅 `js/ending.js` 的 GO 表从 ending 跳入 |

## 4. 全站导航证据

### 4.1 JS 导航（字符串目标）

| 文件 | 行为 | 目标 |
|---|---|---|
| `introduction.html` 内联 | 清档后 `location.href` | `index.html` |
| `js/login.js` | 票号/手机号命中 | `order.html`（相对 pages/） |
| `js/keyword-search.js` | 表单提交 / 空词 | `search-results.html?q=` |
| `js/ending.js` | 四按钮 `location.href = GO[kind]` | 仅当已在 `pages/ending.html`：`end-appeal.html` / `end-strike.html` / `end-enter.html` / `end-day.html` |

`ending.js` 的 GO **不是**从别的页跳进 ending 的入口。

### 4.2 关键词入口（中文 `js/keywords.js`）

合法词：龙套、应工、狄厚、沈南、香火、老郎、目连、夜戏、白戏、程石、马句、豆皮、检场、开锣、后台、戏单、乔干、吴窗。  
禁止词：源码、管理员密码（异皮禁止页，不是结局）。  
**无「交割」。** href 全部落到剧团/茶馆/购票/文献等，**零条** `pages/ending.html`。

英文另加 CamelCase 别名（Extra、DutyBook、NightPlay…），同样没有 Settlement。

### 4.3 `pages/ending.html` 入链

- HTML `href`：无
- JS 字符串导航：无
- 关键词表：无
- inbound 集合：空

`pages/help.html` 正文写「去交割那页」，但该页唯一入链是 ending 页脚的「票务留言」。玩家从未到过 help，也看不到这句。

其它无入链孤儿（不升格为第二条 largest_gap）：`stay.html`、`pages/contact.html`、`pages/leave-note.html`、`pages/mp.html`、`pages/photo.html`、`pages/zhidao.html`。

### 4.4 四结局 token（`js/ending.js` NEED）

存档对象键：中文 `guxz-v1`，英文 `guxz-v1-en`。字段为 token id → `1`，另有 `lock`。

| 结局 | 按钮 id | 需要 token | 授 token 的页（均可经购票/剧团/搜词到达） | 结局 html |
|---|---|---|---|---|
| appeal | end-appeal | t_ticket, t_visit, t_order | ticket.html, pages/visit.html, pages/order.html | pages/end-appeal.html |
| strike | end-strike | t_yinggong, t_xidan, t_xianghuo | roster.html, playbill-scan.html, incense.html | pages/end-strike.html |
| enter | end-enter | t_ticket, t_visit, t_forum | ticket.html, visit.html, thread-doupi.html | pages/end-enter.html |
| day | end-day | t_baixi, t_yinggong | baixi.html, roster.html | pages/end-day.html |

附加 grant（与四结局 NEED 无直接关系）：t_home、t_login、t_cheng、t_maju、t_dihou、t_xidan_pub、t_endpage（ending 页自己授，进不去就拿不到）。

闭环断点在 **交割页入链**，不在 token 公式。

## 5. assets/base 与体积（次要，不升格 largest_gap）

中英各 16 张 `assets/base/*.jpg`，各 **35717635 B（34.06 MB）**。HTML / CSS / JS **零处**写 `assets/base`。运行页只用 `assets/stills/`（48 张，各语种 1.79 MB）。

| 包 | 文件数 | 整包 | 媒体（base+stills） | VP1 媒体预算锚 |
|---|---|---|---|---|
| 中文版 | 134 | 36.15 MB | 35.86 MB | 8 MB |
| 英文版 | 133 | 36.12 MB | 35.86 MB | 8 MB |

未引用的 34 MB base 会把 D10 打穿，但是 **第二项**。本局唯一 `largest_gap` 仍是结局不可达。技能禁止为美化删主循环对象；base 是否发布线剔除须总控另授，制作人不得自行删/移。

## 6. 运行截图（HOLD 证据）

路径根：`visual/html-game-puzzle/hold-20260823-{zh|en}/`

| 文件 | 输入 | 画面变化 | 维 |
|---|---|---|---|
| zh/01-boot-first-screen.png | 打开 8910/introduction.html | 短信回执；可点「打开购票页」 | D1 |
| zh/02-core-verb-index.png | 从引言进首页 | 文旅壳+检索；退票栏 void | D2 D6 |
| zh/03-ticket.png | 打开购票页 | 票号 YX-0821-19；授 t_ticket | D6 |
| zh/04-visit.png | 剧团导航进须知 | 后台禁客文；授 t_visit | D6 |
| zh/05-search-jiaoge-miss.png | 检索「交割」 | 未命中句，无链 | 闭环 |
| zh/06-search-longtao-hit.png | 检索「龙套」 | 2 条茶馆帖，无 ending | 对照 |
| zh/07-login.png | 打开票务查询 | 票号框，无交割入口 | D6 |
| zh/08-order.png | 打开订单页 | 订单在，无跳转 ending | D6 |
| zh/09-narrow-320-intro.png | 320 宽引言 | 开局链仍在 | D9 基线 |
| zh/10-typed-url-orphan-ending.png | **手输** pages/ending.html | 页存在（视口未见齐按钮） | 反证 |
| zh/10b-typed-url-ending-buttons.png | 同 URL，2200 高 | 四结局按钮 + 票务留言 | 反证 |
| en/01、02 | 8911 引言/首页 | 英文镜像 | D1 |
| en/05-search-settlement-miss.png | 检索 Settlement | 未命中 | 闭环 |
| en/06-search-extra-hit.png | 检索 Extra | 2 帖，无 ending | 对照 |
| en/10、10b | **手输** ending | 四按钮英文 | 反证 |

10 / 10b 标明 typed-url，**不是**合法走链。通用 12 + 解密专属未拍满：停止条件打断工作流第 1 步之后，禁止为凑矩阵继续出图。

原始 JSON：`_视觉美化/link-audit.json`、`live-walk.json`、`screenshot-manifest.json`；搜词 dump：`_视觉美化/dumps/`。

## 7. 对应技能条款

- 工作流第 1 步：旧版必须已经能玩。本局主动词（逛站对照 → 交割四选）在交割处断开。
- 停止条件：「主动词、胜负、关卡逻辑与旧版不一致」——胜负页不可达，等价于胜负未闭合。
- 「修画面必须改玩法标本才能好看」：补 ending 入链是玩法修复。本闸授权明确禁止改现有 HTML，故停。
- 正式判定无 FAIL；该停写 HOLD。制作人不得写 `review_status: HOLD`，只在本报告建议评审人写 HOLD。

## 8. 明确没做

- 不美化 CSS / 静帧 / 引言壳
- 不补 `pages/ending.html` 入链
- 不删、不移动 `assets/base`
- 不改 token、lock、四结局公式、搜词表
- 不把体积超标写成第一条差距

## 9. 下一步最小用户授权

在授权补链并经 8910/8911 从 introduction 再走通之前，视觉扩面保持 HOLD。

最小授权（要继续本游戏视觉线，只需这一条）：

> 允许修改中英现有 HTML，各加 **一条** 从已可达页指向 `pages/ending.html` 的相对链接（建议挂在票务查询成功后的订单页，或剧团须知/票务留言且该留言页本身先有入链）。不改 token、不改四按钮判定、不改搜词表、不改 CSS 玩法。补完后必须再从 introduction 走链证明不手输 URL 可到交割页。

未给这条授权时：跳过本游戏美化，或总控另开玩法修复闸。  
**不要**授权「只修 D10 / 删 base / 继续画皮」——在闭环未修时这些都会违反停止条件。

## 10. 用户授权（2026-08-23 续闸）

用户已明确授权：中英文各加 **一条** 从已可达页 `pages/order.html` 指向既有 `pages/ending.html` 的相对链接；不改 token、ending.js、login、keywords、搜索规则、四结局或其它链接。补链后须用 http 8970/8971 从 introduction 自然走到 order→ending，验证至少 appeal 完整闭环，并验证另三按钮在对应既有 token 状态下仍去原结局。闭环恢复后才允许按 html-game-puzzle / VP1 / 2D 做受控美化，并按授权把零引用 `assets/base` 完整可逆移出玩家包。

验证（8970/8971，Chrome headless，不手输 ending URL）：中英 introduction→购票→剧团须知→票号登录→订单链→交割→申诉，到达 `end-appeal.html`。strike / enter / day 在对应既有 token 下仍进原结局 html。记录见 `_视觉美化/loop-verify.json` 与美化后 `playtest-run.json`。停止条件因此解除。
