# 古镇夜戏　独立评审　20260823

reviewer_role: 古镇夜戏独立评审人（未参与本局主界面 / 主图集 / 主 SVG / CSS 制作；未画本局皮）
producer_role: 制作人机器人（本局补 order 入链、改 CSS 壳、移零引用 base、交运行截图）
contract_status（制作人交卷）: CANDIDATE
review_status: **PASS**
reviewer_role ≠ producer_role: 是
build_id: 20260823
genre / genre_profile / pipeline_id: html-game-puzzle / 古镇夜戏网页解密 / VP1
space_dimension: 2D
distribution_form: OTHER（静态多页相对路径，中英分目录）
pipeline_override_reason: 无。未改道。

抽查运行: http://127.0.0.1:8970/ 中文版根、http://127.0.0.1:8971/ 英文版根（进程已在听，未改端口、未起 8980/8981）。无 iframe。非 file://。评审用本机 Chrome headless CDP（调试口 8997，profile `/tmp/guxz-review-cdp`，用后已删）。现场残帧只写 `/tmp/guxz-review/`，**不写入** `visual/` 或 `_视觉美化/`。

用户授权（停止条件报告 §10 + 制作人续闸交卷）：中英 `pages/order.html` 各加一条相对链到既有 `pages/ending.html`；不改 token / ending.js / login / keywords / 搜索规则 / 四结局或其它链接；闭环后才允许 VP1 美化并把零引用 `assets/base` 完整可逆移出玩家包。本局按该授权核验。

---

## 开评门槛

| 门槛 | 结论 |
|---|---|
| 运行中截图 | 中英 `visual/古镇夜戏网页解密/20260823-{zh\|en}/` 各 30 after + 30 before，成对齐。HOLD 历史帧在 `visual/html-game-puzzle/hold-20260823-{zh\|en}/`，不算本局 after。 |
| 许可表 | `_视觉美化/licenses.md` 非空：CSS / stills / 迁出的 base / 授权链均有来源、许可证、再分发、AI 栏。 |
| 7 步烟测记录 | `_视觉美化/playtest-run.json` 有记录。本局在 8970/8971 复跑关键步。 |
| 制作人未写 PASS | 合同 / 交卷 `review_status` 留空；D1–D14 写自检待评，无 PASS。 |

缺运行截图 / 许可表 / 烟测才 HOLD。本局三者都在，开评。正式判定无 FAIL。

---

## 授权补链（核验 1）

对照备份 zip `github游戏-视觉美化前备份-20260823.zip`，中英 `pages/order.html` **各只多一条**相对链，DOM 语气与原「应工簿抄件／客服记录」并列：

- 中：`href="ending.html"`　今晚交割
- 英：`href="ending.html"`　Tonight's settlement

全站其余 HTML 的 `ending.html` href：**仅此两页**。keywords 表无「交割 / ending / Settlement」。

基线 SHA256（`_视觉美化工作/基线清单.sha256`）：

| 文件 | 结果 |
|---|---|
| 中英 `js/tokens.js` `ending.js` `login.js` `keywords.js` `keyword-search.js` | **MATCH** |
| 除 `pages/order.html` 外全部玩家 HTML | **MATCH** |
| 中英 CSS | 各 9 份 CHANGED（字节两两一致）；`blog-personal-2008.css` / `gov-redbar.css` / `wechat-mp-article.css` MATCH 基线，窄屏与 44px 由已改的 `shots.css` 共享 |
| `GATE_C.md` | CHANGED（注明 base 已迁出，非玩法） |

制作人 `loop-verify.json` 的 `all_ok=false` **不构成闭环失败**：href-only BFS 到不了 `order.html`（须 login.js），故 `has_ending=false`。授权入口挂在订单页，本来就不在纯 href 图里。本局 **点击走链**（不手输 ending URL）：

| 路径 | 中文 8970 | 英文 8971 |
|---|---|---|
| introduction → 购票 → 参观须知 → 票号 `YX-0821-19` → 订单链 → 交割 → `#end-appeal` | `pages/end-appeal.html` 题「钱退了」，`lock=appeal` | `Money refunded`，`lock=appeal` |
| 既有 token 下 `#end-strike` | `end-strike.html`「划掉了」 | `Struck through` |
| `#end-enter` | `end-enter.html`「入场」 | `Admission` |
| `#end-day` | `end-day.html`「改挂白戏」 | `Refiled as WhitePlay` |
| 近答案：appeal 三 token 后点划名 | 仍停 `ending.html`，句「划名还对不上。应工簿、戏单边注、香火账要同一号。」，虚线匣 | （中文步已证公式未改；英 ending.js MATCH） |

另三按钮未改 NEED/GO。控件与旧版同一套，只多订单页一条交割链。旧档键仍是 `guxz-v1` / `guxz-v1-en`。写入 `{t_ticket,t_visit,t_order,t_home}` 后订单仍开、链仍在。

---

## 零引用 base 迁出（核验 2）

| 项 | 结果 |
|---|---|
| 玩家 `中文版/assets/base`、`英文版/assets/base` | 目录不存在 |
| 玩家 HTML/CSS/JS 字符串 `assets/base` | **0 处** |
| ref | `_视觉美化工作/ref/古镇夜戏-base/{中文版,英文版}/assets/base/` 各 16 张 jpg |
| `SHA256.txt` 现场复算 | 32/32 **MATCH**（中英对应文件哈希两两相同，两份都保留、未去重） |
| `SOURCE.txt` / `RESTORE.md` | 有；恢复指令指向本 ref，stills 声明未动 |
| 玩家包是否含 ref / visual / `_视觉美化` | 不含 |

体积（语言目录，不含 `visual/`、`_视觉美化/`、ref）：

| 口径 | 文件数 | 字节 |
|---|---|---|
| 中文版 | 118 | 2,195,695 |
| 英文版 | 117 | 2,167,495 |
| stills 媒体 | 各 48 jpg | 各 **1,881,365**（约 1.79 MB） |
| 迁出的 base（ref，不算玩家包） | 各 16 | 各 35,717,635 |

VP1 媒体锚 8 MB：玩家 stills ≪ 锚。stills 路径未改。许可表覆盖 CSS、stills、ref base、授权链。无远程字库 / 远程图 / 商标主视觉。本局无新 AI 图。

---

## VP1 所有者 chrome 与搜索另开页（核验 3）

现场 + 交卷 after：

- 引言说明书：无搜框、无 `<input>`（01=13）。进站链「打开购票页」高 44。
- 文旅小店：米黄细纹、橙检索钮、上圆下不圆 Tab；退票 `javascript:void(0)` **常驻**删除线+虚线框，不是焦点（02/14/30）。
- 票务后台：浅青底、检索钮 `#007bff`，不是小店橙（15/26）。
- 检索命中：另开 `search-results.html?q=`，非模态、非 HUD。中「龙套」2 条茶馆帖；英 Extra 2 条。编号+下划线链（03=16）。
- 未命中：虚线井 + `::before`「0 项」+ 句子（05=10=17）。空词另有「每次只搜索 1 个汉字词」句（11）。
- 禁页：搜「源码」→ `html.skin-forbidden`，`::before`「禁止」，4px 双线，非 dialog（18）。
- 剧团 778 蓝表（20）、茶馆蓝头（21）、地方号（22）、红头文献（23）、2008 博客（24）、问答（25）、发黄卷宗四钮左实边（19）可分家。五套单帧并排未合成 → `did_not_prove`，序列足够指认，不升第二标本。

320 / 390 本局实测（主控件高，undersize=[]）：小店、剧团、茶馆、登录、引言均无 undersize。交割四钮 390 高 44。主操作不依赖 hover。ending 390 `overflowX=true`（卷宗 92%+页边），钮仍满宽可点，不升为本条 gap。真机手指未测。

---

## D11 键盘焦点（制作人自报是否返工）

**不返工。D11 = PASS。**

制作人交卷 `largest_gap` 写：07 与 02 同构图；headless 强制 `:focus-visible` 后 computed `outline-style` 仍为 none；抢眼的是退票虚线；物理 Tab 未拍。

本局复核（未改实现、未补制作人证据）：

| 核验 | 中文 8970 | 英文 8971 |
|---|---|---|
| 真实 Tab×1 | 店名链「槐溪古镇文旅」，`outline: rgb(26,26,26) solid 3px` | 店名链 “NightPlay · Huaixi Old Town Tourism”，同上 |
| 真实 Tab×2 落点 | `#search-input`，`outline: rgb(26,26,26) solid 3px`（即圣经 `#1a1a1a`） | 同上（placeholder keyword） |
| 强制伪类 / 后期描边 / 页内注入 | 本局抽查 **未用** `CSS.forcePseudoState` | 同 |
| 评审人现场 Tab 帧 vs 交卷 07 | 1280×900 同尺寸；通道≥20 仅 **17 px**（光标级） | 同，17 px |
| 交卷 07 vs 02 | 差 bbox 在搜框 `(811,7)–(1041,61)`，ge20=1534 | 英搜框 `(795,7)–(1025,61)`，1534 |
| 退票虚线 vs 02 | 常驻 `.javascript:void(0)` 删除线+1px dashed，不是 `activeElement` | 同 |

交卷 07 采集脚本虽写了强制伪类，**画面与真实 Tab×2 一致**，搜框 3px 实线是与 02 的主差。制作人测到的 `outline-style:none` 是强制伪类的计算样式伪影，不是运行时键盘缺口。退票虚线是假入口身份，不是焦点证据。禁止项（只靠强制伪类且与真实 Tab 对不上）未成立。

其余烟测：无音轨 N/A；`getAnimations()=[]`；减动后未命中虚线井句子仍在；`.stamp` 未挂进 HTML（死 CSS），不挡本维。

---

## 7 步烟测（制作人记录 + 本局 8970/8971 复跑）

1. 缩到 320/390 主按钮可点：06/08/27/28 + 本局 boxes，undersize=[]。
2. 只键盘第一个主线点：真实 Tab×2 → `#search-input`，3px 实线。未拍 Tab 走完四结局（制作人已写入 did_not_prove）。
3. 只触控同一段：390 模拟提交「龙套」落到结果页。非真机手指。
4. 关声：无音轨，N/A。
5. 减弱动效：搜「西瓜」虚线井句子仍在；无循环动画。
6. 状态不唯色：未命中「0 项」+虚线井；禁页斜纹+禁止戳+双线；登录错「!」+虚线匣；退票删除线；交割报缺虚线井句子。
7. 无整屏频闪：`getAnimations()=[]`。

开评后 7 步有记录且本局复跑关键步，D11 不 HOLD。

---

## dimension_verdicts（正式，仅评审人）

| 维 | 判定 | 依据 |
|---|---|---|
| D1 | PASS | 01/13 说明书无搜框；进站后土导航+顶栏搜。类型不靠 HUD 字幕。 |
| D2 | PASS | 搜在各所有者顶栏；命中另开结果页，不是模态。主循环在读页→搜词→票号→交割。 |
| D3 | PASS | 14 小店米黄橙钮、15 浅青蓝钮、20 剧团蓝表、21 茶馆蓝头、19 发黄卷宗可指认。相机正视 2D。五套并排单帧 UNTESTED。 |
| D4 | PASS | 壳是 CSS；stills 有文件与 `img.shot` 引用。无临时代换矩形冒充主循环对象。base 零引用已迁出。 |
| D5 | PASS | 空串句+虚线井；未命中「0 项」；禁页戳+双线；登录错匣；交割报缺井；假入口删除线。无独立加载页（合同静态站）。成功/失败不同帧。 |
| D6 | PASS | 点搜「龙套」/Extra → 另开编号列表。未齐划名有句子且停在交割页、不死档。不靠 hover、无音频依赖。 |
| D7 | PASS | 三锚钉在合同；橙只留给文旅搜钮/现行 Tab；强调色绑红头与禁页。非 VP5。 |
| D8 | PASS | 2D 写死；true3d_exception false。 |
| D9 | PASS | 320/390 主控件高 ≥44 CSS px；引言/小店/剧团/登录可完成主动词段。ending 390 轻微横溢不挡点钮。真机手指未测 → did_not_prove。 |
| D10 | PASS | 玩家媒体约 1.79 MB/语种；本地 http 有记录。证据目录与 ref 未计入玩家包。首包秒表与帧率未测，不挡本维（静态 DOM）。 |
| D11 | PASS | 真实 Tab×2 搜框 3px 实线，与交卷 07 几乎逐像素相同。制作人自报 D11 **不构成返工**。其余 7 步见上。 |
| D12 | PASS | 许可表按文件填写；无商标/品牌字/远程字库；stills 基线已有、本局未新出图；base 不进玩家包。 |
| D13 | PASS | 通用 12 + 解密专属 ≥8 + 01–30 中英 before/after 路径符合 `visual/{genre_profile}/{build_id}/`。01=13、02=14、03=16、05=10=17、09=01 为同构图复用，矩阵可指认。HOLD 历史帧不计 after。 |
| D14 | PASS | 默认网页解密 VP1，无改道。无新 AI 图，无逐张自由提示词。未画成全站圆角卡或边检亭。 |

---

## largest_gap

无。D1–D14 均 PASS。

制作人自报的 D11（07 强制伪类 / 退票虚线抢眼 / 未拍物理键盘）经本局真实 Tab 复核，**不作为返工项**。

---

## did_not_prove

- 真机手指触控、色觉模拟
- 首包可交互秒表、运行帧率
- 只键盘走完四结局全路径（07 / 本局 Tab 只证明第一主线点：顶栏搜框）
- 五套所有者同帧蒙太奇（序列 14/15/20/21/19 已能分家）
- VP5 字形十局 N/A
- `.stamp` 旋转减动：CSS 有规则，HTML 未挂 `.stamp`（死规则）
- ending 390 卷宗区 `overflowX=true`（未升 gap）
- Cursor 内建浏览器本局未挂标签；抽查用 8970/8971 + HeadlessChrome
- 制作人 `loop-verify.json` 的 href-BFS `has_ending=false`（login 墙）；以点击走链为准

---

## 截图矩阵抽检（status 仍用 PRESENT / UNTESTED / N/A）

通用 12：01–12 中英 after+before 均 PRESENT。  
解密专属：13 引言无搜、14 公开壳、15 浅青台、16 命中另开、17 未命中句、18 禁页黑红非模态、19 卷宗、20 剧团第二源 PRESENT。  
追加 21–30（茶馆 / 地方号 / 红头 / 博客 / 问答 / 订单交割链 / 剧团 320 / 茶馆 390 / 购票页 / 退票假入口）PRESENT，成对 before。

---

## 下一步

PASS。不返工。不要改 token / ending.js / login / keywords。不要把 base 搬回玩家包。不要用更多滤镜或新 AI 图盖已经过闸的壳。
